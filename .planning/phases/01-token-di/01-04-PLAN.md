---
phase: 01-token-di
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - tsconfig.json
  - package.json
autonomous: true
gap_closure: true
requirements:
  - PLUG-04
user_setup: []

must_haves:
  truths:
    - "npx tsc-strict 退出 code 0，对 packages/core/di/ 目录下的源代码执行 strict 类型检查"
    - "pnpm run lint（即 tsc --noEmit && tsc-strict）的 tsc-strict 部分通过"
    - "pnpm run test（即 vitest run）仍然全部通过（34/34）"
  artifacts:
    - path: "tsconfig.json"
      provides: "compilerOptions.plugins[0].name 的值与 typescript-strict-plugin 的 PLUGIN_NAME 常量匹配"
      contains: '"name": "typescript-strict-plugin"'
    - path: "package.json"
      provides: "使用 pnpm 重装依赖，npx tsc-strict 从正确安装的包运行"
  key_links:
    - from: "tsconfig.json plugins[0].name"
      to: "typescript-strict-plugin/dist/common/constants.js PLUGIN_NAME"
      via: "getPluginConfig.js 中的 plugin.name === PLUGIN_NAME 比较"
      pattern: '"typescript-strict-plugin"'
---

<objective>
修复 tsc-strict CLI 配置不匹配：tsconfig.json 中 `compilerOptions.plugins[0].name` 的值从 `"tsc-strict"` 改为 `"typescript-strict-plugin"`，使其与 `typescript-strict-plugin@2.4.4` 包 `getPluginConfig.js` 中硬编码的 PLUGIN_NAME 常量匹配。同时清理 package.json 中未实际安装的 `tsc-strict@^2.4.5`（floklein 独立包）声明。

Purpose: 关闭 VERIFICATION.md 的 G-001 差距——使 `pnpm run lint` 的 tsc-strict 部分正确运行，完成 Phase 01 的最后一个 must-have 检查项
Output: 更新后的 tsconfig.json（plugin name 修正）、更新后的 package.json（移除未使用的 devDependency 声明）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-token-di/01-VERIFICATION.md — G-001 差距详情：PLUGIN_NAME 常量 = "typescript-strict-plugin"，tsconfig.json 当前值为 "tsc-strict"
@.planning/phases/01-token-di/01-03-PLAN.md — Plan 03 的 Task 2 创建了当前配置，遗留此不匹配问题
@tsconfig.json — 当前状态：plugins[0].name = "tsc-strict"，plugins[0].paths = ["./packages/core/di"]
@package.json — 当前状态：typescript-strict-plugin@^2.4.4 在 dependencies 中已安装，tsc-strict@^2.4.5 在 devDependencies 中声明但未安装

<interfaces>
<!-- 关键源码引用——执行者无需探索代码库 -->

来自 node_modules/typescript-strict-plugin/dist/common/constants.js:
```js
exports.PLUGIN_NAME = 'typescript-strict-plugin';
exports.TS_STRICT_COMMENT = '@ts-strict';
exports.TS_STRICT_IGNORE_COMMENT = '@ts-strict-ignore';
```

来自 node_modules/typescript-strict-plugin/dist/cli/getPluginConfig.js (第34行):
```js
return plugins?.find((plugin) => plugin.name === PLUGIN_NAME || ...);
```

来自 node_modules/typescript-strict-plugin/dist/common/isFileStrict.js (第30-34行):
```js
const configPaths = config?.paths ?? [];
const fileStrictByPath = isFileStrictByPath({ filePath, configPaths, projectPath });
if (configPaths.length > 0 && !fileStrictByPath) {
    return false;  // paths 白名单：文件不在 paths 中则不进行 strict 检查
}
```

当前 tsconfig.json plugins 配置：
```json
"plugins": [
  {
    "name": "tsc-strict",
    "paths": [
      "./packages/core/di"
    ]
  }
]
```

当前 package.json devDependencies 中：
```
"tsc-strict": "^2.4.5"
```
此包未实际安装，当前的 npx tsc-strict 二进制来自 typescript-strict-plugin@2.4.4 的子目录。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修复 tsconfig.json plugin name 并清理未使用的 tsc-strict devDependency</name>
  <files>tsconfig.json, package.json</files>
  <read_first>
read tsconfig.json — 查看 plugins 配置块的当前状态（name 值、paths 值、位置）
read package.json — 查看 devDependencies 中 tsc-strict 声明的确切行
read .planning/phases/01-token-di/01-VERIFICATION.md — G-001 差距详情和修复方向说明
  </read_first>
  <action>
修复 tsconfig.json 中的 plugin name 不匹配问题（方案 A——最小变更）：

在 tsconfig.json 的 compilerOptions.plugins 数组中，将第一个 plugin 对象的 name 字段从 "tsc-strict" 改为 "typescript-strict-plugin"。保留 paths 配置不变（`["./packages/core/di"]`）。

具体变更：第 27 行 `"name": "tsc-strict"` → `"name": "typescript-strict-plugin"`

paths 白名单保持不变的原因：isFileStrict.js 的逻辑是当 paths 存在且非空时，只有 paths 中匹配的文件才执行 strict 检查。保留 `["./packages/core/di"]` 确保 strict 检查仅作用于 DI 目录下的源码文件（per D-18：仅 Token 和 ServiceRegistry 源码文件使用 strict 模式），不影响项目中其他大量不符合 strict 的旧代码。

清理 package.json 中未实际安装的 tsc-strict devDependency 声明：从 devDependencies 中移除 `"tsc-strict": "^2.4.5"` 这一行。该包来自 floklein 独立仓库，不在当前的 pnpm-lock.yaml 中，且 npx tsc-strict 的实际二进制来源已是 typescript-strict-plugin@2.4.4 子目录——此声明是多余的。

不要运行 `pnpm install` 来同步 lockfile——只编辑 package.json 声明即可，pnpm-lock.yaml 中本就没有 tsc-strict 条目（该包从未成功安装），移除声明不会导致 lockfile 不一致。
  </action>
  <verify>
    <automated>npx tsc-strict 2>&1; echo "EXIT: $?"</automated>
  </verify>
  <acceptance_criteria>
    1. tsconfig.json 第 27 行的 name 值为 `"typescript-strict-plugin"`（grep 验证：`grep -c '"typescript-strict-plugin"' tsconfig.json` 输出为 1）
    2. tsconfig.json 中 paths 的值仍为 `["./packages/core/di"]`（grep 验证：`grep -c '"./packages/core/di"' tsconfig.json` 输出为 1）
    3. npx tsc-strict 退出 code 0，不再报告 "typescript-strict-plugin isn't configured in tsconfig.json"
    4. package.json devDependencies 中不再包含 `"tsc-strict"` 条目（grep 验证：`grep -c '"tsc-strict"' package.json` 输出为 0）
    5. pnpm vitest run packages/core/di/ 仍然全部通过（34/34 tests pass）
  </acceptance_criteria>
  <done>tsconfig.json plugin name 与 typescript-strict-plugin 的 PLUGIN_NAME 常量匹配，npx tsc-strict 正确执行 strict 类型检查，pnpm run lint 的 tsc-strict 部分不再失败</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无新增 | Plan 04 仅修改配置文件（tsconfig.json + package.json），不涉及代码执行路径变更，无新增信任边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | 无新增依赖安装。typescript-strict-plugin@2.4.4 已在 Plan 03 通过 Package Legitimacy Audit 安装，本 plan 仅修正其配置引用。不引入任何新包 |
</threat_model>

<verification>
pnpm run lint（tsc --noEmit && tsc-strict）的 tsc-strict 部分通过；pnpm run test 全部通过（34/34）
</verification>

<success_criteria>
tsconfig.json 中 plugin name 修正为 "typescript-strict-plugin"，npx tsc-strict 对 packages/core/di/ 目录执行 strict 类型检查并退出 code 0。package.json 不再声明未安装的 tsc-strict devDependency。Phase 01 的全部 5 个 must-have truths 均通过验证
</success_criteria>

<output>
Create `.planning/phases/01-token-di/01-04-SUMMARY.md` when done
</output>
