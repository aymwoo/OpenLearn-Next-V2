---
phase: 03-esm
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - packages/core/esm-loader/index.ts
autonomous: true
requirements: [PLUG-01, PLUG-02]

must_haves:
  truths:
    - "EsmLoader 子系统的 barrel 导出完整：EsmLoader 类、NodeEsmLoader、BrowserEsmLoader、PluginModule 类型、5 个错误类、manifestSchema + Manifest 类型"
    - "全部 esm-loader 测试（Plan 01 + Plan 02 创建的所有测试）在 vitest 全量运行中 green"
  artifacts:
    - path: "packages/core/esm-loader/index.ts"
      provides: "barrel 导出所有公共 API"
      min_lines: 20
  key_links:
    - from: "packages/core/esm-loader/index.ts"
      to: "packages/core/esm-loader/esm-loader.ts"
      via: "export { EsmLoader } + export type { PluginModule }"
      pattern: "export.*from.*esm-loader"
    - from: "packages/core/esm-loader/index.ts"
      to: "packages/core/esm-loader/errors.ts"
      via: "export { EsmLoaderError, ... }"
      pattern: "export.*Esm.*Error"
---

<objective>
创建 EsmLoader 子系统的 barrel 导出文件（index.ts），将 Plan 01 和 Plan 02 创建的所有公共 API 集中导出，并运行全量 vitest 测试验证所有组件协同工作正确。

Purpose: 为 Wave 3 的 integration（PluginRuntime + Kernel + DB 集成）提供统一的导入入口。遵循 packages/core/di/index.ts 的现有 barrel 模式。
Output: index.ts barrel 文件、全量测试 green
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-esm/03-CONTEXT.md
@.planning/phases/03-esm/03-RESEARCH.md
@.planning/phases/03-esm/03-PATTERNS.md

<interfaces>
From packages/core/di/index.ts (barrel pattern to follow EXACTLY):
```typescript
export { Token } from './token.js';
export { ServiceRegistry } from './service-registry.js';
export {
  DuplicateRegistrationError,
  MissingDependencyError,
  CircularDependencyError,
  HasDependentError,
  TokenError,
} from './errors.js';
export type { RegisterOptions, ServiceEntry, DepEdge } from './types.js';
```

Files created by Plan 01 (available):
```typescript
// esm-loader.ts: export interface PluginModule, export abstract class EsmLoader
// errors.ts: export class EsmLoaderError, EsmSyntaxError, EsmModuleNotFoundError, EsmLoadTimeoutError, EsmActivationError
// manifest-schema.ts: export const manifestSchema, export type Manifest
```

Files created by Plan 02 (available):
```typescript
// node-loader.ts: export class NodeEsmLoader
// browser-loader.ts: export class BrowserEsmLoader
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 EsmLoader barrel 导出 + 全量测试验证</name>
  <files>packages/core/esm-loader/index.ts</files>
  <read_first>
    - packages/core/di/index.ts — barrel 导出模式（export { Class }, export type { Type }, export { ... } from）
    - .planning/phases/03-esm/03-PATTERNS.md — index.ts 的精确模拟代码
  </read_first>
  <action>
    创建 packages/core/esm-loader/index.ts:
    - 遵循 packages/core/di/index.ts 的精确 barrel 模式
    - 头部 JSDoc 注释说明子系统用途（EsmLoader — 跨运行时 ESM 动态加载 + manifest 校验 + ZIP 包格式）
    - 使用 `export { ClassName } from './file.js'` 格式导出所有类
    - 使用 `export type { InterfaceName }` 导出类型
    - 导出列表:
      从 esm-loader.js: `export { EsmLoader }`, `export type { PluginModule }`
      从 node-loader.js: `export { NodeEsmLoader }`
      从 browser-loader.js: `export { BrowserEsmLoader }`
      从 manifest-schema.js: `export { manifestSchema }`, `export type { Manifest }`
      从 errors.js: `export { EsmLoaderError, EsmSyntaxError, EsmModuleNotFoundError, EsmLoadTimeoutError, EsmActivationError }`
    - 使用 `// ── ... ──` 分隔符按类别分组（参照 di/index.ts 的分组风格）
    - 所有导入使用 .js 扩展名（ESM 规范）

    实现后运行全量测试:
    ```bash
    npx vitest run packages/core/esm-loader/__tests__/ --reporter=verbose
    ```
    确保 Plan 01 的 manifest-schema.test.ts（≥8 tests）和 Plan 02 的 node-loader.test.ts（≥5 tests）+ browser-loader.test.ts（≥3 tests）全部 green。
  </action>
  <verify>
    <automated>npx vitest run packages/core/esm-loader/__tests__/ --reporter=verbose</automated>
  </verify>
  <done>index.ts 存在，正确导出 EsmLoader/NodeEsmLoader/BrowserEsmLoader/PluginModule/manifestSchema/Manifest + 5 个错误类。全量 vitest 测试全部 green（≥16 tests）。tsc --noEmit 无类型错误</done>
  <acceptance_criteria>
    - packages/core/esm-loader/index.ts 存在，完整的 barrel 导出（6 个 export + 2 个 export type 从 4 个源文件）
    - `npx vitest run packages/core/esm-loader/__tests__/ --reporter=verbose` 全部 green，总计 ≥16 个测试
    - `npx tsc --noEmit` 对 esm-loader/ 目录无类型错误
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
本 plan 仅为 barrel 导出 + 测试验证，无新增安全边界。

</threat_model>

<verification>
### Full Test Suite
```bash
npx vitest run packages/core/esm-loader/__tests__/ --reporter=verbose
```

### Type Checking
```bash
npx tsc --noEmit
```

### Barrel Export Verification
```bash
node -e "
  const { EsmLoader, NodeEsmLoader, BrowserEsmLoader, EsmLoaderError, manifestSchema } = require('./packages/core/esm-loader/index.js');
  console.log('Barrel exports OK');
"
```
</verification>

<success_criteria>
- [ ] packages/core/esm-loader/index.ts 正确 barrel 导出所有公共 API
- [ ] 全量 vitest 测试 green（manifest-schema + node-loader + browser-loader 全部）
- [ ] tsc --noEmit 无错误
- [ ] 导出结构符合 packages/core/di/index.ts 的模式
</success_criteria>

<output>
Create `.planning/phases/03-esm/03-03-SUMMARY.md` when done
</output>
