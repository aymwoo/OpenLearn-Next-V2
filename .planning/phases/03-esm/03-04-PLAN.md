---
phase: 03-esm
plan: 04
type: execute
wave: 4
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - packages/core/esm-loader/install-utils.ts
  - packages/core/plugin-runtime/index.ts
  - packages/core/kernel/index.ts
  - packages/core/db/index.ts
  - packages/core/esm-loader/__tests__/bundle.test.ts
  - packages/core/esm-loader/__tests__/fixtures/sample.zip
autonomous: true
requirements: [PLUG-01, PLUG-02]

must_haves:
  truths:
    - "esbuild 可以将多文件插件（含相对导入）打包为单 ESM bundle，保留 @openlearn/* Token import 为 external"
    - "ZIP 插件包（manifest.json + index.js）可以被 jszip 解压、zod 校验 manifest、esbuild 打包为单文件"
    - "plugins 表新增 loader_version（默认 'vm'）和 zip_package BLOB 列，ALTER TABLE 幂等执行"
    - "PluginRuntime.evaluateAndActivate() 根据 loader_version 分支到 vm（旧）或 ESM（新）加载路径"
    - "Kernel 构造函数创建 NodeEsmLoader 实例并注入到 PluginRuntime"
  artifacts:
    - path: "packages/core/esm-loader/install-utils.ts"
      provides: "esbuild 打包 + jszip 解压 + manifest 校验 + ZIP bomb 防护"
      min_lines: 60
    - path: "packages/core/plugin-runtime/index.ts"
      provides: "修改：接收 EsmLoader、ESM 加载分支、installPlugin 增强 ZIP 支持"
      contains: "evaluateAndActivateEsm"
    - path: "packages/core/kernel/index.ts"
      provides: "修改：创建 NodeEsmLoader + 注入 PluginRuntime"
      contains: "NodeEsmLoader"
    - path: "packages/core/db/index.ts"
      provides: "修改：新增 loader_version 和 zip_package 列"
      contains: "ALTER TABLE plugins ADD COLUMN loader_version"
    - path: "packages/core/esm-loader/__tests__/bundle.test.ts"
      provides: "esbuild 打包 + ZIP 解压集成测试"
      min_lines: 60
    - path: "packages/core/esm-loader/__tests__/fixtures/sample.zip"
      provides: "合法 ZIP fixture（manifest.json + index.js）"
  key_links:
    - from: "packages/core/kernel/index.ts"
      to: "packages/core/esm-loader/index.ts"
      via: "import { NodeEsmLoader } from '../esm-loader/index.js'"
      pattern: "import.*NodeEsmLoader"
    - from: "packages/core/plugin-runtime/index.ts"
      to: "packages/core/esm-loader/index.ts"
      via: "import { EsmLoader, EsmActivationError, EsmLoadTimeoutError } from '../esm-loader/index.js'"
      pattern: "import.*EsmLoader"
    - from: "packages/core/esm-loader/install-utils.ts"
      to: "esbuild + jszip + manifest-schema"
      via: "import * as esbuild from 'esbuild' + import JSZip from 'jszip' + import { manifestSchema } from './manifest-schema.js'"
      pattern: "import.*esbuild|import.*jszip|import.*manifestSchema"
    - from: "packages/core/esm-loader/install-utils.ts"
      to: "packages/core/plugin-runtime/index.ts"
      via: "plugin-runtime installPlugin 调用 install-utils 的 bundlePluginFromZip()"
      pattern: "bundlePluginFromZip"
---

<objective>
将 EsmLoader 子系统集成到 PluginRuntime、Kernel 和数据库层。实现 esbuild 多文件预打包、ZIP 包解压与校验、DB schema 扩展、PluginRuntime ESM 加载分支，以及 Kernel DI 注入。同时创建集成测试覆盖 esbuild 打包和 ZIP 解压流程。

Purpose: D-07（esbuild 安装时打包）、D-08（仅允许相对导入 + Token 服务）、D-09（loader_version 分支）、D-12（SQLite blob + 文本混合存储）、D-13（最小 DB 字段新增）、D-01（EsmLoader DI 注入）的完整实现。这是 Phase 3 的最终集成步骤。
Output: install-utils.ts（esbuild + jszip 工具函数）、修改 plugin-runtime/index.ts、kernel/index.ts、db/index.ts、bundle.test.ts、sample.zip fixture
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-esm/03-CONTEXT.md
@.planning/phases/03-esm/03-RESEARCH.md
@.planning/phases/03-esm/03-PATTERNS.md

<interfaces>
From packages/core/esm-loader/index.ts (created by Plan 03):
```typescript
export { EsmLoader } from './esm-loader.js';
export type { PluginModule } from './esm-loader.js';
export { NodeEsmLoader } from './node-loader.js';
export { BrowserEsmLoader } from './browser-loader.js';
export { manifestSchema } from './manifest-schema.js';
export type { Manifest } from './manifest-schema.js';
export { EsmLoaderError, EsmSyntaxError, EsmModuleNotFoundError, EsmLoadTimeoutError, EsmActivationError } from './errors.js';
```

From packages/core/esm-loader/esm-loader.ts:
```typescript
export interface PluginModule { ... }
export abstract class EsmLoader { abstract load(code: string): Promise<PluginModule>; }
```

From packages/core/esm-loader/manifest-schema.ts:
```typescript
export const manifestSchema: z.ZodObject<{...}>
export type Manifest = z.infer<typeof manifestSchema>
```

From packages/core/plugin-runtime/index.ts (current state to modify):
```typescript
export class PluginRuntime {
  constructor(private kernel: Kernel) {}  // Line 19
  public async installPlugin(sourceCode: string) { ... }  // Line 36
  private async evaluateAndActivate(sourceCode: string, pluginId: string) { ... }  // Line 150
  private deactivatePlugin(pluginId: string) { ... }  // Line 78
}
```

From packages/core/kernel/index.ts (current state to modify):
```typescript
export class Kernel {
  constructor() {
    this.pluginRuntime = new PluginRuntime(this);  // Line 51
  }
}
```

From packages/core/db/index.ts (ALTER TABLE pattern):
```typescript
try {
  db.prepare('ALTER TABLE classes ADD COLUMN lab_id TEXT').run();
} catch (e) {
  // column already exists
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 install-utils.ts（esbuild 打包 + jszip 解压 + manifest 校验 + ZIP bomb 防护）</name>
  <files>packages/core/esm-loader/install-utils.ts</files>
  <read_first>
    - packages/core/di/storage-service.ts — 独立服务/工具类模式
    - .planning/phases/03-esm/03-RESEARCH.md — Pattern 4（esbuild stdin + bundle + write:false）+ Pattern 5（zod manifestSchema.parse）+ jszip 使用模式 + Pitfall 1（data: URL 不支持相对导入）+ Pitfall 2（esbuild external 通配符）+ Pitfall 4（esbuild platform neutral）+ Common Pitfalls #1（ZIP bomb 防护）
    - .planning/phases/03-esm/03-PATTERNS.md — esbuild 打包模式 + jszip 使用模式 + 安装流程模式
  </read_first>
  <action>
    创建 packages/core/esm-loader/install-utils.ts:

    函数 1: bundlePlugin(entryCode: string, resolveDir: string): Promise&lt;string&gt;
    - D-07: 使用 esbuild.build() 将多文件插件打包为单 ESM bundle
    - 配置: { stdin: { contents: entryCode, resolveDir, loader: 'ts' }, bundle: true, write: false, format: 'esm', platform: 'neutral', target: 'es2022', external: ['@openlearn/*'] }
    - D-08: external: ['@openlearn/*'] 保留 Token 服务 import，禁止第三方 npm 包导入（esbuild 无法解析的包报错拒绝）
    - 返回 result.outputFiles[0].text
    - Pitfall 4 防范: platform: 'neutral' 不注入 Node/browser 特定代码
    - Pitfall 2 防范: 使用 external: ['@openlearn/*'] 已验证对 Token import 格式有效（RESEARCH.md Pitfall 2 验证）

    函数 2: validateAndBundleZip(zipBuffer: Buffer): Promise&lt;{ manifest: Manifest, bundledCode: string, entryFileName: string }&gt;
    - D-12: 接收 ZIP 原始字节（来自 SQLite zip_package BLOB 或 HTTP upload）
    - 步骤 1: ZIP bomb 防护 — 检查 ZIP 中所有文件的未压缩大小总和 ≤ 10MB（Security: 拒绝解压）。使用 Object.values(zip.files) 遍历检查
    - 步骤 2: jszip.loadAsync(zipBuffer) 解压
    - 步骤 3: 路径穿越检查 — 遍历 Object.keys(zip.files)，检查 name.includes('..') 或 name.startsWith('/')（Security: Tampering）
    - 步骤 4: 读取 manifest.json — zip.file('manifest.json')?.async('string')，不存在则抛错
    - 步骤 5: D-10 — manifestSchema.parse(JSON.parse(manifestJson)) 运行时校验，不合法抛出 ZodError
    - 步骤 6: 根据 manifest.main 读取入口文件 — zip.file(manifest.main)?.async('string')，不存在则抛错
    - 步骤 7: 从 zipBuffer 创建临时解压目录（使用 os.tmpdir() + uuid），将所有 ZIP 文件写入该目录以支持 esbuild 的 resolveDir 解析相对导入
    - 步骤 8: 调用 bundlePlugin(entryCode, tmpDir) 打包
    - 步骤 9: 清理临时目录（try/finally 确保清理）

    函数 3: extractManifestFromBundle(bundledCode: string): Promise&lt;Manifest&gt;
    - 备选：仅从已打包的 bundle 中重新提取 manifest（用于不需要 ZIP 的场景）
    - 暂不实现具体逻辑，保留函数签名或直接返回空（按需扩展）。当前由 validateAndBundleZip 一步完成

    所有导入使用 .js 扩展名。工具函数为纯函数（不依赖 Kernel 实例），便于单元测试。
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "install-utils\|error TS" || echo "No type errors in install-utils"</automated>
    <note>行为验证（esbuild 打包正确性、ZIP 解压、路径穿越防护）由 Task 3 的 bundle.test.ts（Tests 1-7）完整覆盖。Task 1 仅验证类型正确性，行为正确性通过 Task 3 集成测试确认</note>
  </verify>
  <done>install-utils.ts 存在，导出 bundlePlugin() 和 validateAndBundleZip() 函数。import 包含 esbuild、jszip、os、path、uuid、manifestSchema。TypeScript 类型检查通过</done>
  <acceptance_criteria>
    - bundlePlugin() 使用 esbuild.build({ stdin, bundle, write:false, format:'esm', platform:'neutral', target:'es2022', external:['@openlearn/*'] })
    - validateAndBundleZip() 包含 ZIP bomb 防护（≤10MB）、路径穿越检查（拒绝 .. 路径）、manifest 校验（manifestSchema.parse）、esbuild 打包
    - 临时解压目录在 finally 中清理
    - `npx tsc --noEmit` 对 install-utils.ts 无类型错误
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 修改 PluginRuntime（ESM 加载分支 + ZIP 安装增强 + EsmLoader 注入）+ Kernel（EsmLoader 创建 + DI）+ DB schema（ALTER TABLE）</name>
  <files>packages/core/plugin-runtime/index.ts, packages/core/kernel/index.ts, packages/core/db/index.ts</files>
  <read_first>
    - packages/core/plugin-runtime/index.ts — 完整文件（500+ 行），理解现有 evaluateAndActivate 的 vm 分支逻辑、wrapped* 安全包装器构建、installPlugin 流程
    - packages/core/kernel/index.ts — 完整文件（121 行），理解构造函数分层初始化和 PluginRuntime 初始化位置
    - packages/core/db/index.ts — ALTER TABLE 区域（318-404 行），理解 try/catch 幂等模式
    - .planning/phases/03-esm/03-PATTERNS.md — plugin-runtime/index.ts 修改模式、kernel/index.ts 修改模式、db/index.ts 修改模式
  </read_first>
  <action>
    ### A. 修改 packages/core/db/index.ts

    D-13: 在现有 ALTER TABLE 块末尾（404 行后）新增两个 ALTER TABLE:

    ```typescript
    try {
      db.prepare("ALTER TABLE plugins ADD COLUMN loader_version TEXT DEFAULT 'vm'").run();
    } catch (e) {
      // column already exists
    }

    try {
      db.prepare('ALTER TABLE plugins ADD COLUMN zip_package BLOB').run();
    } catch (e) {
      // column already exists
    }
    ```

    遵循现有 try/catch 模式，每条 ALTER TABLE 独立包裹。注释 "column already exists" 与现有风格一致。

    ### B. 修改 packages/core/kernel/index.ts

    - 顶部的 import 块新增: `import { NodeEsmLoader } from '../esm-loader/index.js';`
    - 在 public readonly 属性声明中新增: `public readonly esmLoader: NodeEsmLoader;`
    - 在 constructor 的 Layer 0（无依赖部分，line 37-40 之后）新增: `this.esmLoader = new NodeEsmLoader();`
    - D-01: 修改 line 51 的 PluginRuntime 初始化，传入 EsmLoader: `this.pluginRuntime = new PluginRuntime(this, this.esmLoader);`

    EsmLoader 放在 Layer 0（无依赖），与 EventBus/CapabilityGuard/StorageService 同级。

    ### C. 修改 packages/core/plugin-runtime/index.ts

    **C1. 构造函数修改（line 19）:**
    - 从 `constructor(private kernel: Kernel) {}` 改为 `constructor(private kernel: Kernel, private esmLoader?: EsmLoader) {}`
    - esmLoader 参数可选（向后兼容），添加类型导入

    **C2. 新增 ESM 加载分支方法 evaluateAndActivateEsm（在 evaluateAndActivate 方法上方）:**

    参照 PATTERNS.md "ESM 加载路径 — 新方法 evaluateAndActivateEsm" 的精确代码段实现:

    ```
    private async evaluateAndActivateEsm(sourceCode: string, pluginId: string): Promise<any> {
      this.deactivatePlugin(pluginId);

      // Step 1: import() 获取模块导出（D-03: 返回原始导出）
      const mod = await this.esmLoader!.load(sourceCode);

      // Step 2: 提取 manifest 和 activate（D-06: 支持两种导出格式）
      const plugin = mod.default ?? mod;
      const manifest = plugin.manifest ?? mod.manifest;
      const activate = plugin.activate ?? mod.activate;

      if (!manifest || !activate) {
        throw new EsmActivationError(pluginId, 'missing manifest or activate function');
      }
      if (!manifest.id || !manifest.name) {
        throw new EsmActivationError(pluginId, 'manifest.id and name are required');
      }
      if (typeof activate !== 'function') {
        throw new EsmActivationError(pluginId, 'activate must be a function');
      }

      // ... 复用现有 evaluateAndActivate 的 wrapped* 构建逻辑（lines 175-405）
      // 包括 actorId 创建、capability 授予、wrappedEventBus/CommandBus/ProcessManager/
      // ActionRegistry/Storage/AI 构建、createSafeFunction、原型链冻结
    }
    ```

    重要: 安全包装器逻辑（createSafeFunction、wrapped* 对象、原型链冻结）完全复用 evaluateAndActivate 中 lines 175-405 的代码。不重复编写。ESM 分支调用相同的 buildWrappedContext 逻辑。

    **C3. evaluateAndActivate 添加分支（line 150 上方）:**
    - 在方法开头（deactivatePlugin(pluginId) 之前或之后），从 DB 查询 loader_version。对于传入的 pluginId，从 plugins 表读取 loader_version
    - 或者：修改方法签名接收可选的 loaderVersion 参数。installPlugin（vm 路径）传入 'vm'，新 ZIP 安装传入 'esm'
    - D-09: `if (loaderVersion === 'esm' && this.esmLoader) { return this.evaluateAndActivateEsm(sourceCode, pluginId); }`
    - 现有 vm 分支标记 `// [legacy vm path — remove in Phase 8]`

    **C4. loadFromDB 更新（line 25-33）:**
    - SELECT 查询改为 `SELECT * FROM plugins WHERE status = ?`（包含所有列），从中读取 loader_version
    - 根据 loader_version 调用 evaluateAndActivate 或 evaluateAndActivateEsm
    - 如果 loader_version 为 'esm' 但 esmLoader 未注入，记录错误并跳过该插件

    **C5. installPlugin 增强（line 36-51）:**
    - 新增重载或参数区分：现有 installPlugin(sourceCode) 保持 vm 路径（向后兼容）
    - 新增 installPluginFromZip(zipBuffer: Buffer): 调用 validateAndBundleZip(zipBuffer) → 获取 manifest + bundledCode → `INSERT INTO plugins (..., loader_version, zip_package) VALUES (..., 'esm', zipBuffer)` → evaluateAndActivateEsm(bundledCode, id)

    顶部新增 import:
    ```typescript
    import { EsmLoader, EsmActivationError, EsmLoadTimeoutError } from '../esm-loader/index.js';
    import { validateAndBundleZip } from '../esm-loader/install-utils.js';
    ```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "error TS" || echo "0 type errors"</automated>
    <note>行为验证（PluginRuntime ESM 分支正确性、Kernel DI 注入、DB schema）由 Task 3 的 bundle.test.ts（Test 6 E2E 流程）及现有项目全量 vitest 覆盖。Task 2 仅验证类型正确性和编译通过</note>
  </verify>
  <done>DB schema 新增两个列（幂等 ALTER TABLE）。Kernel 创建 NodeEsmLoader 并注入 PluginRuntime。PluginRuntime 新增 evaluateAndActivateEsm 方法、loader_version 分支、installPluginFromZip 方法。tsc --noEmit 无类型错误。现有 vm 路径代码不变</done>
  <acceptance_criteria>
    - db/index.ts: plugins 表有 loader_version TEXT DEFAULT 'vm' 和 zip_package BLOB 两列
    - kernel/index.ts: esmLoader: NodeEsmLoader 属性存在，PluginRuntime 构造函数传入 esmLoader
    - plugin-runtime/index.ts: 构造函数接收可选的 esmLoader 参数，evaluateAndActivateEsm 方法存在，loadFromDB 读取 loader_version，installPlugin 保持向后兼容
    - `npx tsc --noEmit` 无类型错误（整体项目）
    - 现有 vm 加载路径代码完全未改动（除添加注释标记）
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 创建集成测试（esbuild 打包 + ZIP 解压 + NodeEsmLoader 端到端）</name>
  <files>packages/core/esm-loader/__tests__/bundle.test.ts, packages/core/esm-loader/__tests__/fixtures/sample.zip</files>
  <read_first>
    - packages/core/di/__tests__/service-registry.test.ts — 集成测试模式（describe/it/beforeEach/async）
    - .planning/phases/03-esm/03-RESEARCH.md — Pattern 4（esbuild 验证模式）+ jszip 使用模式
    - .planning/phases/03-esm/03-PATTERNS.md — esbuild 打包模式 + jszip 使用模式
  </read_first>
  <behavior>
    - Test 1: bundlePlugin 打包单文件 → 返回字符串，包含原始代码中的 export
    - Test 2: bundlePlugin 打包含 @openlearn/* import 的代码 → Token import 保留为 external（未内联）
    - Test 3: bundlePlugin 无法打包含非法 npm import（如 import lodash from 'lodash'）→ 抛出错误
    - Test 4: validateAndBundleZip 解压合法 sample.zip → 返回 manifest + bundledCode
    - Test 5: validateAndBundleZip 拒绝缺少 manifest.json 的 ZIP → 抛出错误
    - Test 6: NodeEsmLoader.load(bundledCode) 成功加载 esbuild 输出的 bundle → 返回 PluginModule
    - Test 7: 带有路径穿越（../etc/passwd）的 ZIP → validateAndBundleZip 抛出包含 "path traversal" 的错误
  </behavior>
  <action>
    创建 packages/core/esm-loader/__tests__/fixtures/sample.zip:
    - 使用 Node.js 脚本创建最小合法 ZIP:
      - manifest.json: `{"id":"ext-sample","name":"Sample Plugin","version":"1.0.0","main":"index.js"}`
      - index.js: `export default { manifest: { id: 'ext-sample', name: 'Sample Plugin', version: '1.0.0' }, activate: async function(ctx) { ctx.activated = true; } }`
      - utils.js: `export function helper() { return 42; }`
      - index.js 中使用 `import { helper } from './utils.js'`
    - 使用 jszip 在 Node.js 中动态创建 ZIP Buffer 并写入文件

    创建 packages/core/esm-loader/__tests__/bundle.test.ts:
    - 导入: `import { describe, it, expect, beforeAll } from 'vitest'` + install-utils 函数 + NodeEsmLoader + manifestSchema
    - beforeAll: 创建临时目录 + sample.zip fixture
    - Test 1 (bundle): 使用 bundlePlugin('export const x = 1;', tmpDir) → 结果包含 'export' 或 'const x'
    - Test 2 (external): 使用 bundlePlugin('import { Token } from "@openlearn/core:ITest"; export const x = 1;', tmpDir) → 结果包含 '@openlearn/core:ITest'（未被内联）
    - Test 3 (reject npm): 使用 bundlePlugin('import lodash from "lodash";', tmpDir) → await expect(...).rejects.toThrow()
    - Test 4 (valid ZIP): 使用 validateAndBundleZip(sampleZipBuffer) → expect(result.manifest.id).toBe('ext-sample')，expect(result.bundledCode).toBeTruthy()
    - Test 5 (no manifest ZIP): 创建不含 manifest.json 的 ZIP → await expect(validateAndBundleZip(badZip)).rejects.toThrow()
    - Test 6 (E2E): 从 Test 4 获取 bundledCode → loader.load(bundledCode) → expect(mod.default.manifest.id).toBe('ext-sample')
    - Test 7 (path traversal): 创建含 '../../etc/passwd' 路径的 ZIP → await expect(validateAndBundleZip(badZip)).rejects.toThrow(/path traversal/i)
  </action>
  <verify>
    <automated>npx vitest run packages/core/esm-loader/__tests__/bundle.test.ts --reporter=verbose</automated>
  </verify>
  <done>bundle.test.ts 全部 7 个测试 green。sample.zip fixture 存在且合法。esbuild 打包 + ZIP 解压 + ESM 加载端到端流程验证通过</done>
  <acceptance_criteria>
    - `npx vitest run packages/core/esm-loader/__tests__/bundle.test.ts` 全部 green（≥7 个测试）
    - esbuild bundle({ stdin, format:'esm', external:['@openlearn/*'] }) 正确保留 Token import
    - jszip 解压合法 ZIP → manifest 校验 → esbuild 打包 → NodeEsmLoader.load() 端到端通过
    - 路径穿越 ZIP 被 validateAndBundleZip 拒绝
    - 所有测试在 60 秒内完成（esbuild 打包可能较慢）
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validateAndBundleZip(zipBuffer) — 不受信 ZIP 输入 | 用户上传的 ZIP 包通过 HTTP 或文件系统进入。ZIP bomb 防护（≤10MB）、路径穿越检查（拒绝 .. 路径）、manifest zod 校验三重防护 |
| esbuild bundle — 插件代码编译 | 不受信 JS/TS 代码通过 esbuild stdin 编译。esbuild 的 external 配置确保 Token import 不被解析但保留为运行时 import；esbuild platform: 'neutral' 不注入 Node.js 特定全局 |
| PluginRuntime ESM 分支 — 插件激活 | 不受信 bundle 代码通过 NodeEsmLoader.load() 加载。wrapped* 安全包装器保留（D-03），CapabilityGuard 在激活前检查 |
| DB ALTER TABLE — SQL 注入 | ALTER TABLE 使用固定字符串，无用户输入。try/catch 幂等执行防止重复列错误 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | esbuild bundle | mitigate | external: ['@openlearn/*'] 确保插件不能 import Node.js builtins。platform: 'neutral' 不注入 process/fs/require 等全局。esbuild 超时（默认 30s）防止无限打包循环 |
| T-03-02 | Denial of Service | validateAndBundleZip | mitigate | ZIP bomb 防护：解压前检查未压缩大小总和 ≤ 10MB。esbuild 打包有默认 30s 超时。zjip 解压有内存限制 |
| T-03-03 | Tampering | ZIP 路径穿越 | mitigate | 遍历所有 ZIP 条目，检查 name.includes('..') 或 name.startsWith('/')。非法路径立即拒绝安装。jszip 默认防止绝对路径 |
| T-03-04 | Information Disclosure | esbuild error messages | mitigate | esbuild 编译错误仅在服务端日志中记录。用户可见的错误消息使用 EsmActivationError 包装，不暴露文件系统路径 |
| T-03-05 | Repudiation | 插件安装操作 | accept | 现有 eventBus 审计日志捕获 plugin.install_zip 命令。eventBus.publish 写入 events 表 |
| T-03-06 | Denial of Service | evaluateAndActivateEsm | mitigate | activate() 使用 Promise.race(activatePromise, timeoutPromise(5000)) 超时保护。超时抛出 EsmLoadTimeoutError，插件被 deactivate 并清理资源 |
| T-03-07 | Information Disclosure | DB ALTER TABLE errors | mitigate | ALTER TABLE 使用固定 SQL 字符串，无用户数据。错误被 catch 块吞掉（column already exists），不会通过 API 返回 |
| T-03-08 | Tampering | manifest 字段注入 | mitigate | D-10: zod schema 在 validateAndBundleZip() 中校验 manifest.json，拒绝包含非预期字段的 manifest。parse() 模式（fail-fast）阻止通过 |

</threat_model>

<verification>
### Type Checking
```bash
npx tsc --noEmit
```

### Integration Tests
```bash
# Bundle + ZIP + E2E tests
npx vitest run packages/core/esm-loader/__tests__/bundle.test.ts --reporter=verbose

# Full esm-loader test suite (all 4 plans)
npx vitest run packages/core/esm-loader/__tests__/ --reporter=verbose
```

### DB Schema Verification
```bash
node -e "
  const { db } = require('./packages/core/db/index.js');
  const cols = db.prepare('PRAGMA table_info(plugins)').all();
  const names = cols.map(c => c.name);
  console.log('loader_version:', names.includes('loader_version'));
  console.log('zip_package:', names.includes('zip_package'));
"
```

### Full Test Suite
```bash
npm test
```
</verification>

<success_criteria>
- [ ] esbuild bundlePlugin() 正确打包单文件/多文件代码为 ESM bundle
- [ ] esbuild external: ['@openlearn/*'] 保留 Token import
- [ ] validateAndBundleZip() 解压 ZIP → 校验 manifest → esbuild 打包端到端正确
- [ ] ZIP bomb 防护（>10MB 拒绝）和路径穿越防护（.. 路径拒绝）
- [ ] plugins 表有 loader_version 和 zip_package 列
- [ ] PluginRuntime 支持 ESM 加载分支（loader_version === 'esm'）
- [ ] Kernel 创建 NodeEsmLoader 并注入 PluginRuntime
- [ ] 现有 vm 路径代码不变，新旧并行
- [ ] bundle.test.ts 全部 green（≥7 tests）
- [ ] 全量 vitest 测试 green
</success_criteria>

<output>
Create `.planning/phases/03-esm/03-04-SUMMARY.md` when done
</output>
