---
phase: 10-infra-config
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - vite.config.ts
  - src/index.css
  - packages/core/__tests__/mfe-config.test.ts
  - packages/core/__tests__/mfe-build.test.ts
  - packages/core/__tests__/tailwind-scan.test.ts
  - packages/mfe-whiteboard/package.json
  - packages/mfe-whiteboard/vite.config.ts
  - packages/mfe-courseware/package.json
  - packages/mfe-courseware/vite.config.ts
autonomous: true
requirements:
  - MFE-INF-01
  - MFE-INF-02
  - MFE-INF-03
must_haves:
  truths:
    - "宿主应用成功集成 Module Federation 宿主 Shell 配置，且共享 React 等核心单例"
    - "新建的 whiteboard 与 courseware 微前端子项目具备独立的 Vite 构建与 Module Federation 远程配置"
    - "宿主应用的 Tailwind CSS v4 样式扫描能够成功覆盖并编译子项目的组件样式"
  artifacts:
    - path: "package.json"
      provides: "微前端核心依赖的安装"
    - path: "pnpm-workspace.yaml"
      provides: "微前端子应用工作区注册"
    - path: "vite.config.ts"
      provides: "宿主 Module Federation 强单例共享配置"
    - path: "src/index.css"
      provides: "Tailwind CSS v4 集中式样式扫描规则"
    - path: "packages/core/__tests__/mfe-config.test.ts"
      provides: "共享依赖单例与配置规则测试"
    - path: "packages/core/__tests__/mfe-build.test.ts"
      provides: "编译目标与 Base 路径测试"
    - path: "packages/core/__tests__/tailwind-scan.test.ts"
      provides: "Tailwind CSS 集中扫描编译测试"
    - path: "packages/mfe-whiteboard/package.json"
      provides: "白板子项目包描述与脚本配置"
    - path: "packages/mfe-whiteboard/vite.config.ts"
      provides: "白板子项目 Vite 及 Module Federation 远程配置"
    - path: "packages/mfe-courseware/package.json"
      provides: "课件子项目包描述与脚本配置"
    - path: "packages/mfe-courseware/vite.config.ts"
      provides: "课件子项目 Vite 及 Module Federation 远程配置"
  key_links:
    - from: "src/index.css"
      to: "packages/mfe-*/**/*.{ts,tsx}"
      via: "@source scan directive"
      pattern: "@source.*packages/mfe-"
---

<objective>
搭建 OpenLearnV2 项目的微前端基础设施工程配置。完成 Vite 6 与 Module Federation 2.0 的集成配置，为宿主与远程子项目配置 React、React-DOM 和 Zustand 的强单例共享机制；支持子项目 esnext 编译目标与运行时动态 Base 路径解析；配置宿主 Tailwind CSS v4 样式集中扫描；编写完整的单元与集成测试以验证基础设施的正确性。

Purpose: 微前端架构改造的工程基石，提供可靠的依赖共享与资产构建环境，防止多实例 React 冲突，确保子应用独立构建且与宿主样式完美集成。
Output: 根目录配置修改、新增 whiteboard 与 courseware 微应用目录及其工程配置、新增三个工程及样式扫描验证测试。
</objective>

<execution_context>
@/home/wuxf/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@package.json
@vite.config.ts
@pnpm-workspace.yaml
@src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Workspace &amp; Host Environment</name>
  <files>package.json, pnpm-workspace.yaml, vite.config.ts, src/index.css</files>
  <action>在 monorepo 根目录下运行安装命令，将 @module-federation/vite 写入 devDependencies，将 @module-federation/runtime 与 @module-federation/retry-plugin 写入 dependencies，以完成微前端构建 and 运行时库的安装（对应 D-03 策略）。更新根目录的 pnpm-workspace.yaml，在 packages 列表中注册 'packages/*' 路径（对应 D-01 策略）。修改根目录的 vite.config.ts，引入 federation 插件并配置宿主 Shell，按照 D-05、D-06、D-07 和 D-08 策略将 react、react-dom 和 zustand 声明为共享的强单例，requiredVersion 动态读取 package.json dependencies 字段，并指定 strictVersion 为 false。在 vite.config.ts 中引入 @module-federation/runtime 时作为宿主基础运行时，并声明 build.target 为 esnext，build.modulePreload 为 false。同时，在宿主的 src/index.css 中配置 Tailwind CSS v4 样式的集中扫描规则，使用 @source 指令引入子项目源文件目录以进行全量编译（对应 D-13 策略）。</action>
  <verify>
    <automated>MISSING — Wave 0 must create packages/core/__tests__/mfe-config.test.ts first</automated>
  </verify>
  <done>根目录依赖成功安装并配置，pnpm-workspace.yaml 包含 packages 目录，vite.config.ts 完成宿主 Module Federation 单例共享设置与 esnext 编译选项，src/index.css 中已成功添加 @source 规则。</done>
</task>

<task type="auto">
  <name>Task 2: Create Wave 0 Test Files</name>
  <files>packages/core/__tests__/mfe-config.test.ts, packages/core/__tests__/mfe-build.test.ts, packages/core/__tests__/tailwind-scan.test.ts</files>
  <action>在 packages/core/__tests__/ 目录下创建三个验证测试骨架文件。在 mfe-config.test.ts 中实现单元测试，校验 vite.config.ts 配置中 shared 依赖项的配置参数，断言 react、react-dom 和 zustand 为单例、strictVersion 为 false，并且其 requiredVersion 动态同步自 package.json。在 mfe-build.test.ts 中实现集成测试，运行构建任务并检验输出配置，确保 Host 和各远程应用（packages/mfe-whiteboard, packages/mfe-courseware）的构建目标均为 esnext 且 base 路径使用 auto（对应 D-04 与 D-09）。在 tailwind-scan.test.ts 中编写样式扫描集成测试，验证 Host index.css 文件内容中是否包含针对子应用组件代码目录的 @source 规则配置（对应 D-13）。</action>
  <verify>
    <automated>pnpm test packages/core/__tests__/mfe-config.test.ts</automated>
  </verify>
  <done>成功创建三个测试文件，能完整验证共享依赖选项、构建目标与动态路径配置以及样式扫描规则，测试可被 vitest 执行并给出对应断言结果。</done>
</task>

<task type="auto">
  <name>Task 3: Create and Configure Remote Packages</name>
  <files>packages/mfe-whiteboard/package.json, packages/mfe-whiteboard/vite.config.ts, packages/mfe-courseware/package.json, packages/mfe-courseware/vite.config.ts</files>
  <action>在 packages 目录下新建 mfe-whiteboard 与 mfe-courseware 子应用目录（对应 D-01 策略）。为两个项目分别编写 package.json，继承基础开发脚本并声明各自的包名。在每个子项目的 vite.config.ts 中，使用 @module-federation/vite 插件配置远程应用名称和暴露入口，端口分别固定为 5174 与 5175（对应 D-02 策略），同时将 react、react-dom 和 zustand 声明为共享单例，strictVersion 设为 false，以确保依赖版本宽松共享（对应 D-05、D-07 与 D-08 策略）。为子项目配置 build.target 为 esnext（对应 D-04 策略）以及 base 为 auto，以支持编译时的运行时动态资源路径解析（对应 D-09 策略）。在子项目配置中支持 React CSS Modules（对应 D-14 策略）。完成编写后，执行之前建立的测试文件来自动验证配置与依赖关系。</action>
  <verify>
    <automated>pnpm test packages/core/__tests__/mfe-config.test.ts &amp;&amp; pnpm test packages/core/__tests__/mfe-build.test.ts &amp;&amp; pnpm test packages/core/__tests__/tailwind-scan.test.ts</automated>
  </verify>
  <done>白板与课件子项目创建成功，包依赖及 Module Federation 远程配置正确无误，三个自动化验证测试全部执行通过，未产生编译与版本冲突报错。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 宿主与远程子应用加载边界 | 宿主应用在运行时通过浏览器加载远程子应用的 remoteEntry.js 脚本，需要保证加载源的完整性与安全性，防范跨站脚本注入 (XSS) 风险。 |
| 第三方依赖与构建系统边界 | 使用 pnpm 安装外部 npm 包时，存在供应链投毒及包被恶意篡改的风险。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | remoteEntry.js | mitigate | 生产环境中将子应用静态构建产物与宿主独立托管在同一域名 Origin 的独立子路径下，通过本地静态代理和 HTTPS 加密传输，防范脚本在传输链路中被劫持或篡改（per D-11）。 |
| T-10-02 | Elevation of Privilege | SQLite Database &amp; API | mitigate | 在宿主后台插件注册表中，严格对插件 Entry URL 进行域名白名单正则校验与加签机制，限制非受信任域名的远程脚本执行（per D-10）。 |
| T-10-SC | Tampering | npm installs | mitigate | 对新增的微前端包依赖（@module-federation/* 等）执行 slopcheck 合法性审计，验证包名、版本、下载量与官方仓库地址，防范恶意冒充包投毒。 |
</threat_model>

<verification>
运行以下命令验证 Phase 10 工程基础设施和测试套件是否全部就绪：
- `pnpm install`：确认依赖安装且无锁冲突
- `pnpm test packages/core/__tests__/mfe-config.test.ts`：验证共享依赖强单例配置
- `pnpm test packages/core/__tests__/mfe-build.test.ts`：验证 esnext 与动态 Base 路径配置
- `pnpm test packages/core/__tests__/tailwind-scan.test.ts`：验证宿主集中式 Tailwind CSS v4 样式扫描
</verification>

<success_criteria>
- [ ] 根目录 `package.json` 正确添加微前端构建与运行时所需依赖，`pnpm install` 正常完成。
- [ ] `pnpm-workspace.yaml` 中包含 `packages/*`，子项目可以被 pnpm 正确识别为 workspace 工作区。
- [ ] 宿主及子应用的 `vite.config.ts` 均正确配置 `@module-federation/vite` 插件，强单例（React, React-DOM, Zustand）共享逻辑一致，`strictVersion` 设为 `false`。
- [ ] 所有前端构建目标设为 `esnext`，动态路径 `base` 配置为 `'auto'`。
- [ ] 宿主 `src/index.css` 包含对 `../packages/mfe-*/**/*.{ts,tsx}` 的 `@source` 扫描规则。
- [ ] 三个 Wave 0 单元与集成测试创建完毕，且运行全量通过（All tests passed）。
</success_criteria>

<output>
任务完成后，生成总结文件 `.planning/phases/10-infra-config/10-01-SUMMARY.md`。
</output>
