---
phase: 10-infra-config
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/__tests__/mfe-config.test.ts
  - packages/core/__tests__/mfe-build.test.ts
  - packages/core/__tests__/tailwind-scan.test.ts
autonomous: true
requirements:
  - MFE-INF-01
  - MFE-INF-02
  - MFE-INF-03
must_haves:
  truths:
    - "新建的 whiteboard 与 courseware 微前端子项目具备独立的测试骨架文件"
  artifacts:
    - path: "packages/core/__tests__/mfe-config.test.ts"
      provides: "共享依赖单例与配置规则测试"
      min_lines: 30
    - path: "packages/core/__tests__/mfe-build.test.ts"
      provides: "编译目标与 Base 路径测试"
      min_lines: 20
    - path: "packages/core/__tests__/tailwind-scan.test.ts"
      provides: "Tailwind CSS 集中扫描编译测试"
      min_lines: 20
  key_links: []
---

<objective>
创建 Phase 10 微前端基础设施配置与样式扫描的 Wave 0 测试骨架文件。这些测试文件为后续的配置开发提供 TDD 式的自动化校验，确保核心依赖单例共享（react/react-dom/zustand）、编译目标（esnext）、构建 base（auto）以及 Tailwind CSS v4 样式集中扫描规则在实施后可被自动验证。

Purpose: 遵循测试驱动与 Nyquist 合规性原则，在配置编写前确立自动化测试基线，防止后续实施过程中出现配置偏离或遗漏。
Output: 新增 packages/core/__tests__/mfe-config.test.ts、packages/core/__tests__/mfe-build.test.ts 和 packages/core/__tests__/tailwind-scan.test.ts。
</objective>

<execution_context>
@/home/wuxf/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@/home/wuxf/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-infra-config/10-CONTEXT.md
@.planning/phases/10-infra-config/10-RESEARCH.md
@.planning/phases/10-infra-config/10-PATTERNS.md
@.planning/phases/10-infra-config/10-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Wave 0 Test Skeleton Files</name>
  <files>packages/core/__tests__/mfe-config.test.ts, packages/core/__tests__/mfe-build.test.ts, packages/core/__tests__/tailwind-scan.test.ts</files>
  <action>在 packages/core/__tests__/ 目录下创建三个验证测试骨架文件，作为后续 Task 的自动化验证工具。在编写测试文件时，必须参考 10-PATTERNS.md 中指定的 Analog 文件：对于 mfe-config.test.ts 校验测试，参考 packages/core/__tests__/kernel-plugins.test.ts 的 Vitest 结构与断言；对于 mfe-build.test.ts 和 tailwind-scan.test.ts，参考 packages/core/__tests__/legacy-cleanup.test.ts 的文件存在与结构校验逻辑。测试应定义相应的测试套件（describe/it），但在 Wave 0 阶段，由于真实的配置文件和子项目尚未创建，测试可以先断言基本的待实现逻辑或配置结构（断言文件在未被配置时失败，或作为 stub 断言结构）。具体而言：mfe-config.test.ts 校验宿主和子应用的 shared 字段是否将 react, react-dom, zustand 设为单例；mfe-build.test.ts 校验宿主和子应用构建配置 build.target 为 esnext 且 base 路径为 auto；tailwind-scan.test.ts 验证宿主 index.css 文件内容中是否包含对 packages/mfe-* 的 @source 扫描规则。</action>
  <verify>
    <automated>pnpm test packages/core/__tests__/mfe-config.test.ts</automated>
  </verify>
  <done>成功创建三个测试文件，可被 vitest 正常运行，提供后续任务所需的测试基准。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试运行环境边界 | 本地执行测试时，确保测试代码在受控的开发/测试容器环境中运行，防止测试框架执行未授权的脚本。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Tampering | Test files | mitigate | 严格限制测试文件仅对宿主和微应用配置属性进行只读静态断言，不引入外部网络请求或动态执行不可信代码。 |
</threat_model>

<verification>
运行以下命令验证测试骨架文件是否成功创建且可运行：
- `pnpm test packages/core/__tests__/mfe-config.test.ts`
- `pnpm test packages/core/__tests__/mfe-build.test.ts`
- `pnpm test packages/core/__tests__/tailwind-scan.test.ts`
</verification>

<success_criteria>
- [ ] `packages/core/__tests__/mfe-config.test.ts` 文件成功创建且包含基础 Vitest 结构与 Analog 规范断言。
- [ ] `packages/core/__tests__/mfe-build.test.ts` 文件成功创建且包含基础 Vitest 结构与 Analog 规范断言。
- [ ] `packages/core/__tests__/tailwind-scan.test.ts` 文件成功创建且包含基础 Vitest 结构与 Analog 规范断言。
</success_criteria>

<output>
任务完成后，生成总结文件 `.planning/phases/10-infra-config/10-01-SUMMARY.md`。
</output>
