---
phase: 06-eventbus-semver
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/esm-loader/manifest-schema.ts
  - packages/core/esm-loader/manifest-utils.ts
  - packages/core/esm-loader/index.ts
  - packages/core/esm-loader/__tests__/manifest-schema.test.ts
  - packages/core/esm-loader/__tests__/manifest-utils.test.ts
autonomous: true
requirements: [PLUG-09]

must_haves:
  truths:
    - "manifest.json 的 requires/optional 支持 @scope:IServiceName@^1.0.0 带版本后缀的格式"
    - "manifest.json 的 requires/optional 无 @version 后缀时向后兼容（接受纯 Token 名称）"
    - "manifestSchemaV3 旧版 schema 导出可用，兼容 Phase 3-5 现有代码"
    - "parseRequiresEntry() 正确解析 @scope:IName@^1.0.0 为 { tokenName, versionRange }"
    - "parseRequiresEntry() 处理无 @version 格式时 versionRange 返回 null"
  artifacts:
    - path: "packages/core/esm-loader/manifest-schema.ts"
      provides: "扩展的 manifest schema"
      contains: "requiresItemSchema, manifestSchemaV3"
    - path: "packages/core/esm-loader/manifest-utils.ts"
      provides: "parseRequiresEntry 工具函数"
      contains: "parseRequiresEntry"
  key_links:
    - from: "packages/core/esm-loader/manifest-schema.ts"
      to: "packages/core/plugin-host/index.ts"
      via: "manifestSchema.parse() → PluginHost.activatePlugin() 使用解析后的 manifest.requires 做版本检查"
    - from: "packages/core/esm-loader/manifest-utils.ts"
      to: "packages/core/plugin-host/index.ts"
      via: "parseRequiresEntry() 被 PluginHost 导入用于解析 requires 条目"
---

<objective>
扩展 manifest.json 的 zod schema 使其支持 `@scope:IServiceName@^version` 格式的 requires/optional 条目，同时保留旧版无版本格式的向后兼容性。创建 parseRequiresEntry() 工具函数将 requires 字符串解析为结构化 { tokenName, versionRange } 对象。导出旧版 manifestSchemaV3 供 Phase 3-5 遗留代码继续使用。

Purpose: 插件 manifest 是版本声明的载体——扩展 schema 是版本兼容性验证管线的第二步（第一步 Token.version 在 Plan 06-01，第三步 PluginHost 检查在 Plan 06-03）。parseRequiresEntry 是 Manifest 层和 PluginHost 检查逻辑之间的适配函数。
Output:
- manifestSchema 的 requires/optional 条目正则支持 @version 后缀
- manifestSchemaV3 旧版 schema 导出（无 @version 支持）
- parseRequiresEntry() 工具函数（@scope:IName@^1.0.0 → { tokenName, versionRange }）
- 全部单元测试通过
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-eventbus-semver/06-CONTEXT.md
@.planning/phases/06-eventbus-semver/06-RESEARCH.md

<interfaces>
From packages/core/esm-loader/manifest-schema.ts (current):
```typescript
export const manifestSchema = z.object({
  id: z.string().min(1, { error: 'manifest.id 不能为空' }),
  name: z.string().min(1, { error: 'manifest.name 不能为空' }),
  version: z.string().min(1, { error: 'manifest.version 不能为空' }),
  main: z.string().min(1, { error: 'manifest.main 必须指定入口文件路径' }),
  requires: z.array(z.string()).optional(),
  optional: z.array(z.string()).optional(),
  capabilitiesProposed: z.array(z.string()).optional(),
});
export type Manifest = z.infer<typeof manifestSchema>;
```

Current requires items accept any string. Phase 6 needs regex-constrained strings.

Required regex for @version format:
```
/^@[\w-]+\/[\w-]+:I\w+(?:@[\^~]?\d+\.\d+\.\d+(?:-[\w.]+)?)?$/
```

This matches:
- `@openlearn/core:ICommandBusService` — no version (backward compatible)
- `@openlearn/core:ICommandBusService@^1.0.0` — with version range
- `@openlearn/core:ICommandBusService@~1.2.0` — tilde range
- `@openlearn/core:ICommandBusService@1.0.0` — exact version

ParseRequiresEntry logic:
- `@openlearn/core:ICommandBusService` → { tokenName: '@openlearn/core:ICommandBusService', versionRange: null }
- `@openlearn/core:ICommandBusService@^1.0.0` → { tokenName: '@openlearn/core:ICommandBusService', versionRange: '^1.0.0' }
- `@openlearn/core:ICommandBusService@~1.2.0` → { tokenName: '@openlearn/core:ICommandBusService', versionRange: '~1.2.0' }

Security consideration: Zod regex is linear (no nested quantifiers) — not vulnerable to ReDoS.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend manifest-schema regex for @version + export manifestSchemaV3</name>
  <files>
    packages/core/esm-loader/manifest-schema.ts
    packages/core/esm-loader/index.ts
  </files>
  <read_first>
    packages/core/esm-loader/manifest-schema.ts — 当前完整文件（约 33 行）
    packages/core/esm-loader/index.ts — barrel 导出，确认 manifest-schema 导出格式
  </read_first>
  <action>
    STEP 1A: Replace the entire content of packages/core/esm-loader/manifest-schema.ts.
    The new file extends the existing schema with regex-constrained requires/optional items, adds a v3 backward-compatible schema export, and follows the exact same export pattern.

    Full replacement content:

    ```typescript
    /**
     * manifest-schema.ts — manifest.json 的 zod schema 运行时校验。
     *
     * Phase 3 schema (D-04, D-10):
     *   必需字段 id、name、version、main
     *   可选字段 requires、optional、capabilitiesProposed（纯字符串数组）
     *
     * Phase 6 扩展 (D-09, D-10):
     *   requires/optional 条目支持 @scope:IServiceName@^version 格式
     *   旧版 manifestSchemaV3 导出供 Phase 3-5 遗留代码继续使用
     *
     * 在插件安装/激活时校验 manifest.json，早失败并提供精确错误消息。
     */
    import { z } from 'zod';

    // ── Version 4 schema (Phase 6+) ──────────────────────────────────────────

    /**
     * requiresItemSchema — requires/optional 条目的 zod 正则校验。
     *
     * 支持两种格式：
     * 1. `@scope:IServiceName`（无版本——Phase 3 格式，向后兼容）
     * 2. `@scope:IServiceName@^1.0.0`（带语义化版本范围——Phase 6 格式）
     *
     * 版本范围支持：^x.y.z, ~x.y.z, x.y.z（精确）, x.y.z-pre（pre-release）
     * 正则模式为线性（无嵌套量词），无 ReDoS 风险。
     */
    const requiresItemSchema = z.string().regex(
      /^@[\w-]+\/[\w-]+:I\w+(?:@[\^~]?\d+\.\d+\.\d+(?:-[\w.]+)?)?$/,
      { message: 'requires/optional 条目格式无效。需要 @scope/domain:IServiceName 或 @scope/domain:IServiceName@^x.y.z' }
    );

    /**
     * manifestSchema — 插件 manifest.json 的 zod 运行时校验 schema（Phase 6+ 增强版）。
     *
     * 与 Phase 3 的 manifestSchemaV3 的区别：
     * - requires/optional 条目通过 requiresItemSchema 正则约束，支持 @version
     * - 其他字段与 V3 完全一致
     */
    export const manifestSchema = z.object({
      id: z.string().min(1, { error: 'manifest.id 不能为空' }),
      name: z.string().min(1, { error: 'manifest.name 不能为空' }),
      version: z.string().min(1, { error: 'manifest.version 不能为空' }),
      main: z.string().min(1, { error: 'manifest.main 必须指定入口文件路径' }),
      requires: z.array(requiresItemSchema).optional(),
      optional: z.array(requiresItemSchema).optional(),
      capabilitiesProposed: z.array(z.string()).optional(),
    });

    /**
     * Manifest 类型 — 由 manifestSchema 推导出的 TypeScript 类型。
     */
    export type Manifest = z.infer<typeof manifestSchema>;

    // ── Version 3 schema (Phase 3-5 backward compatibility) ──────────────────

    /**
     * requiresItemV3Schema — Phase 3-5 格式的 requires 条目正则。
     *
     * 仅匹配 @scope:IServiceName（无 @version 后缀）。
     * 供 Phase 8 迁移完成前的遗留代码使用。
     */
    const requiresItemV3Schema = z.string().regex(
      /^@[\w-]+\/[\w-]+:I\w+$/,
      { message: 'requires/optional 条目格式无效。需要 @scope/domain:IServiceName' }
    );

    /**
     * manifestSchemaV3 — Phase 3-5 的旧版 manifest schema（无 @version 支持）。
     *
     * 与 manifestSchema 的唯一区别：requires/optional 使用 requiresItemV3Schema
     * （严格无 @version 后缀格式）。
     *
     * 用途：
     * - Phase 3-5 的代码和测试继续使用此 schema
     * - Phase 8 迁移完成后可移除
     */
    export const manifestSchemaV3 = z.object({
      id: z.string().min(1, { error: 'manifest.id 不能为空' }),
      name: z.string().min(1, { error: 'manifest.name 不能为空' }),
      version: z.string().min(1, { error: 'manifest.version 不能为空' }),
      main: z.string().min(1, { error: 'manifest.main 必须指定入口文件路径' }),
      requires: z.array(requiresItemV3Schema).optional(),
      optional: z.array(requiresItemV3Schema).optional(),
      capabilitiesProposed: z.array(z.string()).optional(),
    });

    /**
     * ManifestV3 类型 — 由 manifestSchemaV3 推导的类型，保留供引用。
     */
    export type ManifestV3 = z.infer<typeof manifestSchemaV3>;
    ```

    STEP 1B: Update barrels (packages/core/esm-loader/index.ts).
    Read the current barrel file first. If it already exports from './manifest-schema.js', ensure the new export `manifestSchemaV3` is also re-exported. Add to the export statement:

    ```typescript
    export { manifestSchema, manifestSchemaV3 } from './manifest-schema.js';
    export type { Manifest, ManifestV3 } from './manifest-schema.js';
    ```

    Adjust the exact syntax to match the existing barrel pattern.

    STEP 1C: Verify TypeScript compilation:
    ```bash
    npx tsc --noEmit
    ```
  </action>
  <verify>
    <automated>
      npx tsc --noEmit
    </automated>
  </verify>
  <done>
    - manifestSchema 的 requires/optional 通过 requiresItemSchema（带 @version 正则）约束
    - manifestSchemaV3 作为独立 schema 导出（无 @version 支持）
    - ManifestV3 类型同步导出
    - 两个 schema 的 id/name/version/main/capabilitiesProposed 字段完全一致
    - tsc --noEmit 通过
  </done>
</task>

<task type="auto">
  <name>Task 2: Create parseRequiresEntry() utility function</name>
  <files>
    packages/core/esm-loader/manifest-utils.ts
    packages/core/esm-loader/index.ts
  </files>
  <read_first>
    packages/core/esm-loader/index.ts — barrel 导出格式
  </read_first>
  <action>
    STEP 2A: Create new file packages/core/esm-loader/manifest-utils.ts.

    ```typescript
    /**
     * manifest-utils.ts — manifest 条目解析工具函数。
     *
     * Phase 6: 解析 requires/optional 条目字符串为结构化 { tokenName, versionRange }。
     *
     * 输入格式:
     *   - `@openlearn/core:ICommandBusService`          → { tokenName, versionRange: null }
     *   - `@openlearn/core:ICommandBusService@^1.0.0`   → { tokenName, versionRange: '^1.0.0' }
     *   - `@openlearn/core:ICommandBusService@~1.2.0`   → { tokenName, versionRange: '~1.2.0' }
     *   - `@openlearn/core:ICommandBusService@1.0.0`    → { tokenName, versionRange: '1.0.0' }
     *   - `@openlearn/core:ICommandBusService@>=1.0.0 <2.0.0` → { tokenName, versionRange: '>=1.0.0 <2.0.0' }
     *
     * 解析策略:
     *   - finds the second '@' character (first '@' is the scope prefix)
     *   - everything before the second '@' is tokenName
     *   - everything after the second '@' is versionRange
     *   - if no second '@' exists, versionRange is null (accept any version)
     */

    /**
     * 解析 requires/optional 条目字符串为 token 名称和版本范围。
     *
     * @param entry - requires 条目字符串，如 '@openlearn/core:ICommandBusService@^1.0.0'
     * @returns { tokenName: string, versionRange: string | null }
     *   - tokenName: Token 标识符（不含 @version 后缀）
     *   - versionRange: 版本范围字符串（如 '^1.0.0'）或 null（当无 @version 后缀时）
     *
     * @example
     * parseRequiresEntry('@openlearn/core:ICommandBusService')
     *   // → { tokenName: '@openlearn/core:ICommandBusService', versionRange: null }
     *
     * @example
     * parseRequiresEntry('@openlearn/core:ICommandBusService@^1.0.0')
     *   // → { tokenName: '@openlearn/core:ICommandBusService', versionRange: '^1.0.0' }
     */
    export function parseRequiresEntry(entry: string): { tokenName: string; versionRange: string | null } {
      // The first '@' is always the scope prefix (e.g., @openlearn).
      // Find the second '@' which indicates the start of the version range.
      const firstAt = entry.indexOf('@');  // scope prefix @
      if (firstAt === -1) {
        // No @ at all — invalid but handle gracefully
        return { tokenName: entry, versionRange: null };
      }

      // Find the second '@' by searching after the '/' which follows the scope
      const slashIndex = entry.indexOf('/');
      if (slashIndex === -1) {
        // No domain separator — invalid format
        return { tokenName: entry, versionRange: null };
      }

      const secondAtIndex = entry.indexOf('@', slashIndex);
      if (secondAtIndex === -1) {
        // No second '@' — no version range
        return { tokenName: entry, versionRange: null };
      }

      return {
        tokenName: entry.slice(0, secondAtIndex),
        versionRange: entry.slice(secondAtIndex + 1),
      };
    }
    ```

    STEP 2B: Update barrel export (packages/core/esm-loader/index.ts).
    Read the current barrel file and add the new export:

    ```typescript
    export { parseRequiresEntry } from './manifest-utils.js';
    ```

    STEP 2C: Verify TypeScript compilation:
    ```bash
    npx tsc --noEmit
    ```
  </action>
  <verify>
    <automated>
      # Quick runtime test
      node -e "
        import('./packages/core/esm-loader/manifest-utils.js').then(({ parseRequiresEntry }) => {
          // No version
          const r1 = parseRequiresEntry('@openlearn/core:ICommandBusService');
          console.assert(r1.tokenName === '@openlearn/core:ICommandBusService', 'tokenName mismatch');
          console.assert(r1.versionRange === null, 'versionRange should be null');

          // Caret version
          const r2 = parseRequiresEntry('@openlearn/core:ICommandBusService@^1.0.0');
          console.assert(r2.tokenName === '@openlearn/core:ICommandBusService', 'tokenName mismatch');
          console.assert(r2.versionRange === '^1.0.0', 'versionRange should be ^1.0.0');

          // Tilde version
          const r3 = parseRequiresEntry('@openlearn/core:ICommandBusService@~1.2.0');
          console.assert(r3.tokenName === '@openlearn/core:ICommandBusService', 'tokenName mismatch');
          console.assert(r3.versionRange === '~1.2.0', 'versionRange should be ~1.2.0');

          console.log('parseRequiresEntry OK');
        });
      "
      npx tsc --noEmit
    </automated>
  </verify>
  <done>
    - manifest-utils.ts 创建并导出 parseRequiresEntry()
    - parseRequiresEntry 正确处理：无 @version 返回 null，有 @version 返回 range 字符串
    - parseRequiresEntry 正确处理 @^、@~、@exact、@>= 等版本前缀
    - esm-loader/index.ts 重新导出 parseRequiresEntry
    - tsc --noEmit 通过
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit tests for @version regex + parseRequiresEntry + manifestSchemaV3</name>
  <files>
    packages/core/esm-loader/__tests__/manifest-schema.test.ts
    packages/core/esm-loader/__tests__/manifest-utils.test.ts
  </files>
  <read_first>
    packages/core/esm-loader/__tests__/manifest-schema.test.ts — 现有测试模式
  </read_first>
  <behavior>
    - Test 1: manifestSchema 接受带 @^version 的 requires 条目
    - Test 2: manifestSchema 接受带 @~version 的 requires 条目
    - Test 3: manifestSchema 接受无 @version 的 requires 条目（向后兼容）
    - Test 4: manifestSchema 拒绝非法 requires 格式（无效字符、缺少冒号等）
    - Test 5: manifestSchemaV3 拒绝带 @version 的 requires 条目（严格向后兼容校验）
    - Test 6: manifestSchemaV3 接受无 @version 的 requires 条目
    - Test 7: parseRequiresEntry 处理无 @version
    - Test 8: parseRequiresEntry 处理 @^version
    - Test 9: parseRequiresEntry 处理 @~version 和 @exact
    - Test 10: parseRequiresEntry 处理 pre-release 标签（@^1.0.0-alpha）
  </behavior>
  <action>
    STEP 3A: Extend packages/core/esm-loader/__tests__/manifest-schema.test.ts.
    Add new describe blocks at the end of the file:

    ```typescript
    import { manifestSchema, manifestSchemaV3 } from '../manifest-schema.js';

    // Add to the top of file alongside existing import

    describe('manifestSchema — Phase 6 @version regex', () => {
      it('should accept requires with @^version suffix', () => {
        const manifest = {
          id: 'ext-quiz',
          name: 'Quiz Generator',
          version: '1.0.0',
          main: 'index.js',
          requires: ['@openlearn/core:ICommandBusService@^1.0.0'],
        };
        expect(() => manifestSchema.parse(manifest)).not.toThrow();
      });

      it('should accept requires with @~version suffix', () => {
        const manifest = {
          id: 'ext-quiz',
          name: 'Quiz Generator',
          version: '1.0.0',
          main: 'index.js',
          requires: ['@openlearn/core:IEventBusService@~1.2.0'],
        };
        expect(() => manifestSchema.parse(manifest)).not.toThrow();
      });

      it('should accept requires with exact version (no prefix)', () => {
        const manifest = {
          id: 'ext-quiz',
          name: 'Quiz Generator',
          version: '1.0.0',
          main: 'index.js',
          requires: ['@openlearn/core:IStorageService@1.0.0'],
        };
        expect(() => manifestSchema.parse(manifest)).not.toThrow();
      });

      it('should accept requires with pre-release version', () => {
        const manifest = {
          id: 'ext-quiz',
          name: 'Quiz Generator',
          version: '1.0.0',
          main: 'index.js',
          requires: ['@openlearn/core:IAIService@^1.0.0-beta.1'],
        };
        expect(() => manifestSchema.parse(manifest)).not.toThrow();
      });

      it('should accept requires without @version (backward compatible)', () => {
        const manifest = {
          id: 'ext-quiz',
          name: 'Quiz Generator',
          version: '1.0.0',
          main: 'index.js',
          requires: ['@openlearn/core:ICommandBusService'],
        };
        expect(() => manifestSchema.parse(manifest)).not.toThrow();
      });

      it('should reject requires with invalid format', () => {
        const manifest = {
          id: 'ext-quiz',
          name: 'Quiz Generator',
          version: '1.0.0',
          main: 'index.js',
          requires: ['invalid-format'],
        };
        expect(() => manifestSchema.parse(manifest)).toThrow();
      });

      it('should reject requires with incomplete version (missing patch)', () => {
        const manifest = {
          id: 'ext-quiz',
          name: 'Quiz Generator',
          version: '1.0.0',
          main: 'index.js',
          requires: ['@openlearn/core:ICommandBusService@^1.0'],
        };
        // ^1.0 is not a valid semver — should be ^1.0.0
        expect(() => manifestSchema.parse(manifest)).toThrow();
      });

      // optional field also supports @version
      it('should accept optional with @^version suffix', () => {
        const manifest = {
          id: 'ext-quiz',
          name: 'Quiz Generator',
          version: '1.0.0',
          main: 'index.js',
          optional: ['@openlearn/core:IAIService@^2.0.0'],
        };
        expect(() => manifestSchema.parse(manifest)).not.toThrow();
      });
    });

    describe('manifestSchemaV3 — Phase 3 backward compatibility', () => {
      it('should accept requires without @version', () => {
        const manifest = {
          id: 'ext-quiz',
          name: 'Quiz Generator',
          version: '1.0.0',
          main: 'index.js',
          requires: ['@openlearn/core:ICommandBusService'],
        };
        expect(() => manifestSchemaV3.parse(manifest)).not.toThrow();
      });

      it('should reject requires with @version suffix', () => {
        const manifest = {
          id: 'ext-quiz',
          name: 'Quiz Generator',
          version: '1.0.0',
          main: 'index.js',
          requires: ['@openlearn/core:ICommandBusService@^1.0.0'],
        };
        expect(() => manifestSchemaV3.parse(manifest)).toThrow();
      });

      it('should accept optional without @version', () => {
        const manifest = {
          id: 'ext-quiz',
          name: 'Quiz Generator',
          version: '1.0.0',
          main: 'index.js',
          optional: ['@openlearn/core:IAIService'],
        };
        expect(() => manifestSchemaV3.parse(manifest)).not.toThrow();
      });

      it('should accept empty requires/optional', () => {
        const manifest = {
          id: 'ext-quiz',
          name: 'Quiz Generator',
          version: '1.0.0',
          main: 'index.js',
          requires: [],
          optional: [],
        };
        expect(() => manifestSchemaV3.parse(manifest)).not.toThrow();
      });
    });
    ```

    STEP 3B: Create packages/core/esm-loader/__tests__/manifest-utils.test.ts.

    ```typescript
    /**
     * Unit tests for parseRequiresEntry utility function.
     *
     * Phase 6: Parses manifest requires/optional entries into structured
     * { tokenName, versionRange } objects for SemVer compatibility checking.
     */
    import { describe, it, expect } from 'vitest';
    import { parseRequiresEntry } from '../manifest-utils.js';

    describe('parseRequiresEntry', () => {
      it('should return tokenName and null versionRange for entry without @version', () => {
        const result = parseRequiresEntry('@openlearn/core:ICommandBusService');
        expect(result.tokenName).toBe('@openlearn/core:ICommandBusService');
        expect(result.versionRange).toBeNull();
      });

      it('should return tokenName and ^versionRange for caret version', () => {
        const result = parseRequiresEntry('@openlearn/core:ICommandBusService@^1.0.0');
        expect(result.tokenName).toBe('@openlearn/core:ICommandBusService');
        expect(result.versionRange).toBe('^1.0.0');
      });

      it('should return tokenName and ~versionRange for tilde version', () => {
        const result = parseRequiresEntry('@openlearn/core:IEventBusService@~1.2.0');
        expect(result.tokenName).toBe('@openlearn/core:IEventBusService');
        expect(result.versionRange).toBe('~1.2.0');
      });

      it('should return tokenName and exact version for no-prefix version', () => {
        const result = parseRequiresEntry('@openlearn/core:IStorageService@1.0.0');
        expect(result.tokenName).toBe('@openlearn/core:IStorageService');
        expect(result.versionRange).toBe('1.0.0');
      });

      it('should handle pre-release version tags', () => {
        const result = parseRequiresEntry('@openlearn/core:IAIService@^1.0.0-beta.1');
        expect(result.tokenName).toBe('@openlearn/core:IAIService');
        expect(result.versionRange).toBe('^1.0.0-beta.1');
      });

      it('should handle complex range (>= ... <)', () => {
        const result = parseRequiresEntry('@openlearn/core:IProcessService@>=1.0.0 <2.0.0');
        expect(result.tokenName).toBe('@openlearn/core:IProcessService');
        expect(result.versionRange).toBe('>=1.0.0 <2.0.0');
      });

      it('should handle optional deps with version range', () => {
        const result = parseRequiresEntry('@openlearn/core:ICapabilityService@^0.5.0');
        expect(result.tokenName).toBe('@openlearn/core:ICapabilityService');
        expect(result.versionRange).toBe('^0.5.0');
      });

      it('should handle hyphenated scope names', () => {
        const result = parseRequiresEntry('@my-scope/sub-domain:IService@^1.0.0');
        expect(result.tokenName).toBe('@my-scope/sub-domain:IService');
        expect(result.versionRange).toBe('^1.0.0');
      });
    });
    ```

    STEP 3C: Run tests:
    ```bash
    npx vitest run packages/core/esm-loader/__tests__/
    ```
  </action>
  <verify>
    <automated>
      npx vitest run packages/core/esm-loader/__tests__/ --reporter=verbose
    </automated>
  </verify>
  <done>
    - manifest-schema.test.ts 新增 ~12 个测试用例覆盖 @version 正则 + manifestSchemaV3
    - manifest-utils.test.ts 创建包含 ~8 个测试用例覆盖 parseRequiresEntry 所有变体
    - vitest 全部通过
  </done>
</task>

</tasks>

<threat_model>
## Phase 6 Security Threats (Plan 06-02)

### Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Plugin manifest data → Zod schema | 未经验证的 manifest.json 字符串进入 Zod parse |

### STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-04 | Tampering | manifest-schema regex | mitigate | Zod regex 使用线性模式（无嵌套量词），无 ReDoS 风险。requiresItemSchema 在 parse 时严格拒绝非符合格式，拒绝恶意载荷 |
| T-6-05 | Tampering | parseRequiresEntry | accept | parseRequiresEntry 纯字符串分割，无执行/求值。输入已通过 Zod schema 校验，无需额外 sanitize |
| T-6-06 | Information Disclosure | manifestSchema 错误消息 | accept | Zod 错误消息只报告格式无效，不泄露内部状态 |
</threat_model>

<verification>
1. `npx tsc --noEmit` 通过
2. `npx vitest run packages/core/esm-loader/__tests__/` 全部通过
3. `node -e "import('./packages/core/esm-loader/manifest-utils.js').then(({parseRequiresEntry}) => {...})"` 返回正确解析
</verification>

<success_criteria>
1. manifestSchema 的 requires/optional 通过 requiresItemSchema 正则约束（支持 @version 和无 @version）
2. manifestSchemaV3 导出且严格拒绝 @version 后缀
3. manifestSchemaV3 向后兼容 Phase 3-5 的纯 Token 名称格式
4. parseRequiresEntry() 对 6 种输入格式返回正确的 { tokenName, versionRange }
5. 所有单元测试通过
</success_criteria>

<output>
Create .planning/phases/06-eventbus-semver/06-02-SUMMARY.md when done
</output>
