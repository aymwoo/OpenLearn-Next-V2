---
phase: 06-eventbus-semver
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - packages/core/di/token.ts
  - packages/core/di/types.ts
  - packages/core/di/service-registry.ts
  - packages/core/plugin-host/errors.ts
  - packages/core/di/index.ts
  - packages/core/plugin-host/index.ts
  - packages/core/di/__tests__/token.test.ts
  - packages/core/di/__tests__/service-registry.test.ts
autonomous: true
requirements: [PLUG-07, PLUG-09]

must_haves:
  truths:
    - "Token 实例携带 version 语义化版本号（默认 1.0.0）"
    - "ServiceRegistry.register() 自动存储 Token 的 version 到 ServiceEntry"
    - "ServiceRegistry.getVersion(tokenName) 返回已注册 Token 的版本号"
    - "ServiceRegistry.resolveByName(tokenName) 通过字符串查询已注册服务"
    - "SemverMismatchError 抛出时包含 pluginId、tokenName、requiredRange、actualVersion、人类可读 message"
    - "semver npm 包在 packages/core 下可正常 import 使用"
  artifacts:
    - path: "packages/core/plugin-host/errors.ts"
      provides: "SemverMismatchError 错误类"
      contains: "class SemverMismatchError"
    - path: "packages/core/di/token.ts"
      provides: "Token 类新增 version 参数"
      contains: "version: string"
    - path: "packages/core/di/types.ts"
      provides: "ServiceEntry 新增 version 字段"
      contains: "version: string"
    - path: "packages/core/di/service-registry.ts"
      provides: "ServiceRegistry 版本存储 + getVersion + resolveByName"
      contains: "getVersion"
  key_links:
    - from: "packages/core/di/service-registry.ts"
      to: "packages/core/di/types.ts"
      via: "import ServiceEntry — register() stores version"
    - from: "packages/core/plugin-host/errors.ts"
      to: "packages/core/di/service-registry.ts"
      via: "SemverMismatchError thrown by version-check logic in Plan 06-03"
---

<objective>
建立 Phase 6 的底层基础能力：Token 语义化版本声明、ServiceRegistry 版本追踪和字符串查询、SemverMismatchError 结构化错误、以及 semver npm 包安装。

Purpose: 为 Plan 06-02 的 manifest-schema 扩展和 Plan 06-03 的 PluginHost 版本检查提供基础设施。Token.version 是语义化版本兼容系统的第一性原理——没有版本信息就无法做兼容性检查。
Output:
- Token 类支持可选的 version 构造函数参数（默认 '1.0.0'）
- ServiceEntry 类型携带 version 字段
- ServiceRegistry.register() 自动从 Token 读取版本存储，新增 getVersion() 方法
- ServiceRegistry.resolveByName() 正式 API
- SemverMismatchError 结构化错误类（带插件/Token/版本上下文）
- semver 包作为直接依赖安装
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-eventbus-semver/06-CONTEXT.md
@.planning/phases/06-eventbus-semver/06-RESEARCH.md

<interfaces>
From packages/core/di/types.ts (existing):
```typescript
export interface ServiceEntry {
  instance: unknown;
  options: RegisterOptions;
}
// Phase 6: add version: string
```

From packages/core/di/service-registry.ts (existing):
```typescript
async register<T>(token: Token<T>, instance: T, options?: RegisterOptions): Promise<void>
async resolve<T>(token: Token<T>): Promise<T>
async resolveByName(name: string): Promise<unknown>  // already implemented
async unregister<T>(token: Token<T>): Promise<void>
list(): Array<{ name: string; instance: unknown }>
```

From packages/core/di/token.ts (existing):
```typescript
export class Token<T> {
  private readonly _phantomService!: T;
  public readonly name: string;
  constructor(name: string) { ... }
}
// Phase 6: add public readonly version: string; constructor(name, version = '1.0.0')
```

From packages/core/plugin-host/errors.ts (existing pattern):
```typescript
export class PluginHostError extends Error {
  constructor(message: string) { super(`[PluginHost] ${message}`); this.name = 'PluginHostError'; }
}
export class PluginActivateError extends PluginHostError { ... }
```

SemverMismatchError 遵循此模式：
- 继承 PluginHostError
- constructor(pluginId, pluginName, tokenName, requiredRange, actualVersion)
- super(message) 生成人类可读描述
- this.name = 'SemverMismatchError'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install semver package + create SemverMismatchError</name>
  <files>
    package.json
    packages/core/plugin-host/errors.ts
    packages/core/di/index.ts
    packages/core/plugin-host/index.ts
  </files>
  <read_first>
    packages/core/plugin-host/errors.ts — 现有错误类层次模式（PluginHostError 基类、PluginActivateError、PluginDeactivateTimeoutError、IllegalStateTransitionError）
    packages/core/di/index.ts — 现有 barrel 导出，确认导出格式
    packages/core/plugin-host/index.ts — barrel 导出区
  </read_first>
  <action>
    STEP 1A: Install semver npm package.
    Run `npm install semver` in the project root. Verify installation by checking package.json contains "semver" in dependencies (at least ^7.6.x) and node_modules/semver exists. Then run a quick import verification:
    ```bash
    node -e "import('semver').then(s => console.log(s.satisfies('1.5.0', '^1.0.0')))"
    ```
    Should output `true`.

    STEP 1B: Create SemverMismatchError in packages/core/plugin-host/errors.ts.
    Follow the exact pattern of existing error classes. Add after `IllegalStateTransitionError`:

    ```typescript
    /**
     * SemverMismatchError — Token 版本不兼容时抛出。
     *
     * 携带结构化字段供 UI 解析 + 人类可读 message 用于日志。
     * 包含插件 id/名称、冲突 Token 名称、要求范围、实际版本。
     * 在 PluginHost.activatePlugin() 和 PluginHost.installPlugin() 中抛出。
     */
    export class SemverMismatchError extends PluginHostError {
      constructor(
        public readonly pluginId: string,
        public readonly pluginName: string,
        public readonly tokenName: string,
        public readonly requiredRange: string,
        public readonly actualVersion: string,
      ) {
        super(
          `Plugin "${pluginName}" (${pluginId}) requires ${tokenName}@${requiredRange}, ` +
          `but host provides ${actualVersion}. ` +
          `Please upgrade the host or use a compatible plugin version.`
        );
        this.name = 'SemverMismatchError';
      }
    }
    ```

    STEP 1C: Export `SemverMismatchError` from the PluginHost barrel (packages/core/plugin-host/index.ts).
    Add it to the existing imports block at the top of plugin-host/index.ts:
    ```typescript
    import {
      ...
      SemverMismatchError,
    } from './errors.js';
    ```
    Then add it to any re-export section if present. If the file doesn't have a barrel-style re-export block, wrap all existing error imports in a merged import statement — the key requirement is that `SemverMismatchError` is accessible to `plugin-host/index.ts` internals. (`SemverMismatchError` is used internally by activatePlugin() in Plan 06-03.)

    Also add a `export { SemverMismatchError }` re-export from packages/core/di/index.ts so consumers can import from the DI barrel. Actually, since SemverMismatchError lives in plugin-host/errors.ts, add a re-export from packages/core/plugin-host/index.ts instead (following the existing export pattern of that file).

    Check packages/core/plugin-host/index.ts for existing export block. If none exists, add a standalone export statement.

    STEP 1D: Verify TypeScript compilation — run `npx tsc --noEmit` and fix any type errors.
  </action>
  <verify>
    <automated>
      # Verify semver installation
      node -e "import('semver').then(s => { console.assert(s.satisfies('1.5.0', '^1.0.0') === true); console.assert(s.satisfies('1.5.0', '^2.0.0') === false); console.log('semver OK'); })"

      # Verify TypeScript compilation
      npx tsc --noEmit
    </automated>
  </verify>
  <done>
    - package.json contains "semver" in dependencies
    - SemverMismatchError class exists in plugin-host/errors.ts with 5 structured fields (pluginId, pluginName, tokenName, requiredRange, actualVersion)
    - SemverMismatchError extends PluginHostError with [PluginHost] prefix
    - SemverMismatchError exports from packages/core/plugin-host/index.ts
    - tsc --noEmit passes without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Token version parameter + ServiceEntry.version + ServiceRegistry version tracking + resolveByName completion</name>
  <files>
    packages/core/di/token.ts
    packages/core/di/types.ts
    packages/core/di/service-registry.ts
  </files>
  <read_first>
    packages/core/di/token.ts — 当前构造函数只接受 name，TOKEN_NAME_RE 正则
    packages/core/di/types.ts — ServiceEntry 当前只有 instance + options
    packages/core/di/service-registry.ts — register() 方法第 84 行 registry.set() 存储格式，resolveByName() 已有完整实现
  </read_first>
  <action>
    STEP 2A: Modify Token class (packages/core/di/token.ts).
    Add `version` as a second optional constructor parameter with default `'1.0.0'`:

    ```typescript
    /** 语义化版本号（semver），默认 '1.0.0'。 */
    public readonly version: string;

    constructor(name: string, version: string = '1.0.0') {
      // ...existing validation unchanged...
      this.name = name;
      this.version = version;  // NEW
    }
    ```

    The existing TOKEN_NAME_RE validation stays unchanged — version is not part of the name validation.
    No validation on version string format (it will be validated by semver at usage time).

    STEP 2B: Modify ServiceEntry type (packages/core/di/types.ts).
    Add `version` field to ServiceEntry interface:

    ```typescript
    export interface ServiceEntry {
      instance: unknown;
      options: RegisterOptions;
      /** 语义化版本号（semver），如 '1.0.0'。由 ServiceRegistry.register() 自动从 Token 读取。 */
      version: string;
    }
    ```

    STEP 2C: Modify ServiceRegistry (packages/core/di/service-registry.ts).
    In register() method, change the registry.set() call on line 84 from:
    ```typescript
    this.registry.set(name, { instance, options: options ?? {} });
    ```
    to:
    ```typescript
    this.registry.set(name, { instance, options: options ?? {}, version: token.version });
    ```

    Then add a new public method `getVersion()` after the existing `resolveByName()` method (after line 124):

    ```typescript
    /**
     * 获取已注册 Token 的版本号。
     *
     * Phase 6: Token 语义化版本兼容系统的方法。
     * 由 PluginHost.activatePlugin() 在版本检查时调用。
     *
     * @param tokenName - Token 标识符字符串（如 '@openlearn/core:ICommandBusService'）
     * @returns 版本号字符串（如 '1.0.0'），或 undefined 当 Token 未注册时
     */
    getVersion(tokenName: string): string | undefined {
      const entry = this.registry.get(tokenName);
      return entry?.version;
    }
    ```

    Note: resolveByName() is already fully implemented in the current code (lines 118-124). No changes needed to it. It correctly queries registry by name and returns the instance. However, ensure that the method signature returns `Promise<unknown>` and has proper JSDoc documenting its Phase 6 role as the Token Registry pattern entry point.

    If resolveByName() needs any JSDoc update, update it. The method itself needs no implementation change.

    STEP 2D: Update barrel export if needed.
    Check packages/core/di/index.ts — ServiceRegistry is already exported. No additional export needed since we're modifying existing exports.

    STEP 2E: Verify with TypeScript compilation:
    ```bash
    npx tsc --noEmit
    ```
  </action>
  <verify>
    <automated>
      # Quick verification via Node.js runtime test
      node -e "
        import('./packages/core/di/token.js').then(({ Token }) => {
          const t1 = new Token('@openlearn/core:ITest');
          console.assert(t1.version === '1.0.0', 'default version should be 1.0.0');
          const t2 = new Token('@openlearn/core:ITest', '2.0.0');
          console.assert(t2.version === '2.0.0', 'custom version should be 2.0.0');
          console.log('Token version OK');
        });
      "
      npx tsc --noEmit
    </automated>
  </verify>
  <done>
    - Token 构造函数接受第二个可选参数 version（默认 '1.0.0'）
    - Token.version 是 public readonly string 属性
    - ServiceEntry 接口新增 version: string 字段
    - ServiceRegistry.register() 在 registry.set() 时存储 token.version
    - ServiceRegistry.getVersion(tokenName) 返回版本号或 undefined
    - ServiceRegistry.resolveByName() 已有完整实现，JSDoc 标记正确
    - tsc --noEmit 通过
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit tests for Token version + ServiceRegistry version tracking + resolveByName</name>
  <files>
    packages/core/di/__tests__/token.test.ts
    packages/core/di/__tests__/service-registry.test.ts
  </files>
  <read_first>
    packages/core/di/__tests__/token.test.ts — 现有测试模式（describe/it/expect 模式）
    packages/core/di/__tests__/service-registry.test.ts — 现有测试模式（mock Token 方式）
    packages/core/di/token.ts — 新增 version 参数后的 API
    packages/core/di/service-registry.ts — 新增 getVersion() 方法 + resolveByName() 现有 API
  </read_first>
  <behavior>
    - Test 1: Token version 默认值：new Token('@scope/domain:IService').version === '1.0.0'
    - Test 2: Token version 自定义值：new Token('@scope/domain:IService', '2.0.0').version === '2.0.0'
    - Test 3: Token version 不影响现有 name 验证逻辑（空名、无效格式仍抛 TokenError）
    - Test 4: ServiceRegistry.register() 存储版本到 ServiceEntry：getVersion(tokenName) 返回 token.version
    - Test 5: ServiceRegistry.getVersion() 对未注册 Token 返回 undefined
    - Test 6: ServiceRegistry.resolveByName() 通过字符串 name 查询已注册服务（Token Registry 模式）
    - Test 7: ServiceRegistry.resolveByName() 对未注册 name 抛出 "No provider" 错误
  </behavior>
  <action>
    STEP 3A: Extend packages/core/di/__tests__/token.test.ts.
    Add a new describe block after the existing tests:

    ```typescript
    describe('Token.version (Phase 6)', () => {
      it('should default to 1.0.0 when no version provided', () => {
        const t = new Token('@openlearn/core:IServiceA');
        expect(t.version).toBe('1.0.0');
      });

      it('should accept custom version string', () => {
        const t = new Token('@openlearn/core:IServiceA', '2.0.0');
        expect(t.version).toBe('2.0.0');
      });

      it('should accept pre-release version strings', () => {
        const t = new Token('@openlearn/core:IServiceA', '1.0.0-beta.1');
        expect(t.version).toBe('1.0.0-beta.1');
      });

      it('should not affect existing name validation', () => {
        expect(() => new Token('')).toThrow(TokenError);
        expect(() => new Token('@openlearn/core:IServiceA', '1.0.0')).not.toThrow();
      });
    });
    ```

    STEP 3B: Extend packages/core/di/__tests__/service-registry.test.ts.
    Add a new describe block at the end of the file:

    ```typescript
    describe('ServiceRegistry — version tracking + resolveByName (Phase 6)', () => {
      it('should store version from Token on register', async () => {
        const registry = new ServiceRegistry();
        const token = new Token<IServiceA>('@openlearn/core:IServiceVersioned', '2.0.0');
        await registry.register(token, makeService('v2'));

        expect(registry.getVersion('@openlearn/core:IServiceVersioned')).toBe('2.0.0');
      });

      it('should store default version (1.0.0) when Token has no explicit version', async () => {
        const registry = new ServiceRegistry();
        const token = new Token<IServiceA>('@openlearn/core:IServiceDefault');
        await registry.register(token, makeService('default'));

        expect(registry.getVersion('@openlearn/core:IServiceDefault')).toBe('1.0.0');
      });

      it('should return undefined for unregistered token name', () => {
        const registry = new ServiceRegistry();
        expect(registry.getVersion('@openlearn/core:IDoesNotExist')).toBeUndefined();
      });

      it('should resolve service by string name (Token Registry pattern)', async () => {
        const registry = new ServiceRegistry();
        const token = new Token<IServiceA>('@openlearn/core:IServiceByName');
        const instance = makeService('byName');
        await registry.register(token, instance);

        const resolved = await registry.resolveByName('@openlearn/core:IServiceByName');
        expect(resolved).toBe(instance);
      });

      it('should throw "No provider" for resolveByName with unregistered name', async () => {
        const registry = new ServiceRegistry();
        await expect(
          registry.resolveByName('@openlearn/core:INonExistent')
        ).rejects.toThrow(/No provider registered for token name/);
      });
    });
    ```

    STEP 3C: Run the tests and confirm they pass:
    ```bash
    npx vitest run packages/core/di/__tests__/
    ```
  </action>
  <verify>
    <automated>
      npx vitest run packages/core/di/__tests__/ --reporter=verbose
    </automated>
  </verify>
  <done>
    - token.test.ts 新增 4 个测试用例（version 默认值、自定义值、pre-release、不影响 name 验证）
    - service-registry.test.ts 新增 5 个测试用例（getVersion 存储、默认版本、未注册返回 undefined、resolveByName 成功、resolveByName 未注册）
    - vitest 全部通过
  </done>
</task>

</tasks>

<threat_model>
## Phase 6 Security Threats (Plan 06-01)

### Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Plugin code ↔ ServiceRegistry | 任何携带 Token 名称的代码可以调用 getVersion() |

### STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-01 | Tampering | Token.version constructor default | mitigate | version 默认值 '1.0.0' 确保现有 Phase 2 Token 向后兼容；未来 breaking change 时必须显式传递新 version |
| T-6-02 | Information Disclosure | ServiceRegistry.getVersion | accept | getVersion 只返回版本号字符串，不暴露实现细节。T-6-03 在 Plan 06-03 中处理 |
| T-6-03 | Denial of Service | semver package install | mitigate | 通过 npm registry 安装，package-lock.json 锁定版本。在 Plan 06-03 中增加 semver.satisfies() 输入验证 |
</threat_model>

<verification>
1. `npx tsc --noEmit` 通过
2. `npx vitest run packages/core/di/__tests__/` 全部通过
3. `node -e "import('semver').then(s => console.assert(s.satisfies('1.5.0', '^1.0.0')))"` 返回 true
</verification>

<success_criteria>
1. Token 构造函数接受 `(name: string, version?: string)`，version 默认 '1.0.0'
2. ServiceEntry 接口包含 `version: string` 字段
3. ServiceRegistry.register() 自动从 Token 读取 version 存到 registry Map
4. ServiceRegistry.getVersion(tokenName) 返回版本号或 undefined
5. ServiceRegistry.resolveByName(name) 通过字符串 name 查询已注册服务实例
6. SemverMismatchError 抛出时携带 pluginId/pluginName/tokenName/requiredRange/actualVersion + 人类可读 message
7. semver 包已安装，import semver from 'semver' 可正常工作
8. 所有单元测试通过
</success_criteria>

<output>
Create .planning/phases/06-eventbus-semver/06-01-SUMMARY.md when done
</output>
