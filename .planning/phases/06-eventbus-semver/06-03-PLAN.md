---
phase: 06-eventbus-semver
plan: 03
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified:
  - packages/core/plugin-host/index.ts
  - packages/core/plugin-runtime/index.ts
  - packages/core/plugin-host/__tests__/plugin-host.test.ts
autonomous: true
requirements: [PLUG-07, PLUG-09]

must_haves:
  truths:
    - "PluginHost.activatePlugin() 在 buildContext 前检查 manifest.requires 的 Token 版本兼容性"
    - "版本不兼容时抛出 SemverMismatchError，激活被拒绝"
    - "Optional 依赖版本不匹配时跳过注入 + console.warn，不阻塞激活"
    - "PluginRuntime.installPlugin() 在安装时做版本预检查"
  artifacts:
    - path: "packages/core/plugin-host/index.ts"
      provides: "SemVer 兼容性检查逻辑"
      contains: "semver check in activatePlugin"
    - path: "packages/core/plugin-runtime/index.ts"
      provides: "安装时版本预检查"
      contains: "semver check in installPlugin"
  key_links:
    - from: "packages/core/plugin-host/index.ts:activatePlugin"
      to: "packages/core/di/service-registry.ts:getVersion"
      via: "getVersion() 查询已注册 Token 的版本"
    - from: "packages/core/plugin-host/index.ts:activatePlugin"
      to: "packages/core/esm-loader/manifest-utils.ts:parseRequiresEntry"
      via: "解析 requires 条目中的 tokenName 和 versionRange"
    - from: "packages/core/plugin-host/index.ts:activatePlugin"
      to: "packages/core/plugin-host/errors.ts:SemverMismatchError"
      via: "版本不匹配时抛出"
---

<objective>
在 PluginHost.activatePlugin() 中插入 Token 版本兼容性检查逻辑，确保插件激活时其 manifest.requires 声明的 Token 版本范围与 ServiceRegistry 中注册的实际版本匹配。在 PluginRuntime.installPlugin() 中增加安装时预检查路径，实现 D-05 要求的"安装时 + 激活时"双重检查。

Purpose: 这是 Phase 6 的核心验证环节——Plan 06-01 和 Plan 06-02 建立了版本声明和注册能力，此计划将这些能力组装为完整的兼容性检查管线。双重检查（D-05）确保用户不会遇到"安装成功但无法激活"的割裂体验。
Output:
- PluginHost.activatePlugin() 在 manifestSchema.parse() 之后、buildContext() 之前插入版本检查
- PluginRuntime.installPlugin() 在激活插件前调用版本预检查
- 完整的集成测试（安装/激活时版本拒绝、optional 跳过、正常通过）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-eventbus-semver/06-CONTEXT.md
@.planning/phases/06-eventbus-semver/06-RESEARCH.md
@.planning/phases/06-eventbus-semver/06-01-SUMMARY.md
@.planning/phases/06-eventbus-semver/06-02-SUMMARY.md

<interfaces>
From Plan 06-01 (created):
- Token class: `new Token<T>(name, version?)` — version defaults to '1.0.0'
- ServiceRegistry: `getVersion(tokenName: string): string | undefined`
- ServiceRegistry: `resolveByName(tokenName: string): Promise<unknown>`
- SemverMismatchError: `new SemverMismatchError(pluginId, pluginName, tokenName, requiredRange, actualVersion)` extends PluginHostError

From Plan 06-02 (created):
- manifestSchema.requires items: accept `@scope:IServiceName@^version` format
- manifestSchemaV3: backward-compatible Phase 3-5 schema
- `parseRequiresEntry(entry: string): { tokenName: string; versionRange: string | null }`

Existing in plugin-host/index.ts:
```typescript
async activatePlugin(pluginId: string, options?: { mode?: 'inline' | 'worker' }): Promise<void>
async installPlugin(sourceCode: string): Promise<Manifest>
```

The activation flow (lines 343-462 in current code):
1. state validation → ACTIVATING
2. load from DB (source_code + manifest)
3. parse storedManifest from JSON
4. EsmLoader.load() module
5. extract manifest/activate/deactivate from module
6. manifestSchema.parse(manifest) — CURRENT LINE ~396
7. buildContext() — CURRENT LINE ~399
8. capability grant
9. activate(ctx) with timeout
10. success/failure handling

Phase 6 inserts SemVer check: between step 6 and step 7.

PluginRuntime.installPlugin() (lines 82-99):
1. delegates to PluginHost.installPlugin() → DB INSERT
2. queries back the pluginId
3. delegates to PluginHost.activatePlugin()

Phase 6 adds pre-check: before PluginHost.installPlugin(), or inside PluginHost.installPlugin() before DB INSERT.
</interfaces>

<code_snippets>
The exact insertion point in activatePlugin() is at ~line 396-399, between manifestSchema.parse(manifest) and buildContext().

```typescript
// STEP 6: manifest schema validation (existing)
manifestSchema.parse(manifest);

// ═══ PHASE 6: SemVer compatibility check (NEW) ═══════════════════════
const requiresList = manifest.requires ?? [];
const optionalList = manifest.optional ?? [];

for (const req of requiresList) {
  const { tokenName, versionRange } = parseRequiresEntry(req);
  const actualVersion = this.serviceRegistry.getVersion(tokenName);

  if (!actualVersion) {
    throw new SemverMismatchError(
      pluginId, manifest.name ?? pluginId,
      tokenName, versionRange ?? '*', 'unregistered'
    );
  }

  if (!versionRange) continue; // accept any version

  try {
    if (!semver.satisfies(actualVersion, versionRange)) {
      throw new SemverMismatchError(
        pluginId, manifest.name ?? pluginId,
        tokenName, versionRange, actualVersion
      );
    }
  } catch (semverErr) {
    if (semverErr instanceof SemverMismatchError) throw semverErr;
    // Invalid version range string — reject explicitly
    throw new SemverMismatchError(
      pluginId, manifest.name ?? pluginId,
      tokenName, versionRange ?? 'invalid', actualVersion ?? 'unknown'
    );
  }
}

for (const opt of optionalList) {
  const { tokenName, versionRange } = parseRequiresEntry(opt);
  const actualVersion = this.serviceRegistry.getVersion(tokenName);

  if (!actualVersion || (versionRange && !semver.satisfies(actualVersion, versionRange))) {
    console.warn(
      `[PluginHost] Optional dependency ${tokenName}@${versionRange ?? '*'} not satisfied ` +
      `(host: ${actualVersion ?? 'unregistered'}) — skipping for plugin "${pluginId}"`
    );
    continue;
  }
}
// ═══════════════════════════════════════════════════════════════════════

// STEP 7: buildContext (existing)
const ctx = await buildContext(...)
```

For installPlugin(), the check goes after extractManifest() and before DB INSERT:

```typescript
async installPlugin(sourceCode: string): Promise<Manifest> {
  const manifest = await this.extractManifest(sourceCode);
  this.ensureUniqueManifestId(manifest.id);

  // ═══ PHASE 6: install-time SemVer pre-check ═════════════════════════
  this.checkSemVerCompatibility(manifest, 'install');
  // ═════════════════════════════════════════════════════════════════════

  const pluginId = uuidv7();
  // ... DB INSERT ...
}
```

The checkSemVerCompatibility should be a private helper method that centralizes the version-checking logic to avoid duplication between installPlugin and activatePlugin. It checks requires (mandatory — throws on mismatch) and optional (warns on mismatch).
</code_snippets>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PluginHost.activatePlugin() SemVer compatibility check</name>
  <files>
    packages/core/plugin-host/index.ts
  </files>
  <read_first>
    packages/core/plugin-host/index.ts — 完整文件，特别是 activatePlugin() 方法（lines 343-462）和 installPlugin() 方法（lines 278-318）
  </read_first>
  <action>
    STEP 1A: Add imports at the top of packages/core/plugin-host/index.ts.

    Add alongside existing imports:
    ```typescript
    import semver from 'semver';
    import { parseRequiresEntry } from '../esm-loader/manifest-utils.js';
    import { SemverMismatchError } from './errors.js';
    ```
    The existing imports already include:
    ```typescript
    import { manifestSchema } from '../esm-loader/manifest-schema.js';
    ```
    Keep that import — it's already present.

    STEP 1B: Add a private helper method `checkSemVerCompatibility` to the PluginHost class.
    Insert this method after the `getExecutionMode` private method (after line 152, before `validateTransition`):

    ```typescript
    /**
     * Phase 6: 检查 manifest 中声明的 Token 版本兼容性。
     *
     * 供 installPlugin() 和 activatePlugin() 双重调用。
     *
     * - manifest.requires 中 Token 版本不兼容 → 抛出 SemverMismatchError
     * - manifest.optional 中 Token 版本不兼容 → console.warn + 跳过（不阻塞）
     * - 未注册的 Token → 视为不兼容（requires 抛错，optional 警告跳过）
     *
     * @param manifest - 插件 manifest（已通过 manifestSchema.parse）
     * @param pluginId - 插件 DB id
     * @param phase - 检查阶段标识（'install' 或 'activate'），仅用于日志
     */
    private checkSemVerCompatibility(
      manifest: { id?: string; name?: string; requires?: string[]; optional?: string[] },
      pluginId: string,
      phase: 'install' | 'activate',
    ): void {
      const pluginName = manifest.name ?? pluginId;
      const requiresList = manifest.requires ?? [];
      const optionalList = manifest.optional ?? [];

      // ── Required dependencies ───────────────────────────────────────────
      for (const req of requiresList) {
        const { tokenName, versionRange } = parseRequiresEntry(req);
        const actualVersion = this.serviceRegistry.getVersion(tokenName);

        if (!actualVersion) {
          throw new SemverMismatchError(
            pluginId, pluginName,
            tokenName, versionRange ?? '*', 'unregistered'
          );
        }

        if (!versionRange) continue; // No version range = accept any version

        try {
          if (!semver.satisfies(actualVersion, versionRange)) {
            throw new SemverMismatchError(
              pluginId, pluginName,
              tokenName, versionRange, actualVersion
            );
          }
        } catch (semverErr) {
          if (semverErr instanceof SemverMismatchError) throw semverErr;
          // Invalid version range string — wrap in SemverMismatchError
          throw new SemverMismatchError(
            pluginId, pluginName,
            tokenName, versionRange, actualVersion
          );
        }
      }

      // ── Optional dependencies ───────────────────────────────────────────
      for (const opt of optionalList) {
        const { tokenName, versionRange } = parseRequiresEntry(opt);
        const actualVersion = this.serviceRegistry.getVersion(tokenName);

        if (!actualVersion || (versionRange && !semver.satisfies(actualVersion, versionRange))) {
          console.warn(
            `[PluginHost] Optional dependency ${tokenName}${versionRange ? '@' + versionRange : ''} not satisfied ` +
            `(host: ${actualVersion ?? 'unregistered'}) — skipping injection for plugin "${pluginId}" (${phase})`
          );
          continue;
        }
      }
    }
    ```

    STEP 1C: Modify activatePlugin() to call checkSemVerCompatibility.
    In the activatePlugin() method, locate the insertion point. The flow currently is:

    Line 396: `manifestSchema.parse(manifest);`
    Line ~398-405: `const ctx = await buildContext(...)`

    Insert the SemVer check AFTER manifestSchema.parse(manifest) and BEFORE buildContext().
    Add this code after line 396:

    ```typescript
      // 6a. Phase 6: Token version compatibility check
      this.checkSemVerCompatibility(manifest, pluginId, 'activate');
    ```

    Important: The manifest variable at this point is the module-exported manifest (not storedManifest). It has already passed manifestSchema.parse() on line 396. Both manifests (storedManifest and module manifest) contain the same requires/optional data, so using either is fine. Using the local `manifest` variable (from module export) is preferred as it matches the just-parsed schema.

    The updated flow becomes:
    ```
    6. manifestSchema.parse(manifest);                    # existing
    6a. this.checkSemVerCompatibility(manifest, pluginId, 'activate');  # NEW
    7. const ctx = await buildContext(...)                 # existing
    ```

    STEP 1D: Modify installPlugin() to call checkSemVerCompatibility.
    In the installPlugin() method, insert the check AFTER manifest is parsed/validated and BEFORE DB INSERT.

    The current flow:
    ```
    const manifest = await this.extractManifest(sourceCode);  # line 280
    this.ensureUniqueManifestId(manifest.id);                  # line 283
    const pluginId = uuidv7();                                 # line 286
    try {
      # DB INSERT ...                                          # line 290+
    ```

    Insert after ensureUniqueManifestId and before pluginId generation:

    ```typescript
      // Phase 6: install-time SemVer pre-check
      this.checkSemVerCompatibility(manifest, '(pending)', 'install');
    ```

    Use '(pending)' as pluginId because the DB id hasn't been generated yet. The manifest.id will be available via manifest.name.

    STEP 1E: Verify TypeScript compilation:
    ```bash
    npx tsc --noEmit
    ```
  </action>
  <verify>
    <automated>
      npx tsc --noEmit
    </automated>
  </verify>
  <done>
    - PluginHost 类新增 checkSemVerCompatibility 私有方法
    - activatePlugin() 在 manifestSchema.parse() 之后、buildContext() 之前调用 checkSemVerCompatibility
    - installPlugin() 在 DB INSERT 前调用 checkSemVerCompatibility
    - requires 不兼容时抛出 SemverMismatchError
    - optional 不兼容时 console.warn + 跳过（不抛错）
    - 导入 semver 和 parseRequiresEntry
    - tsc --noEmit 通过
  </done>
</task>

<task type="auto">
  <name>Task 2: PluginRuntime.installPlugin() delegate with install-time check</name>
  <files>
    packages/core/plugin-runtime/index.ts
  </files>
  <read_first>
    packages/core/plugin-runtime/index.ts — installPlugin() 方法（lines 82-99）
  </read_first>
  <action>
    STEP 2A: Review PluginRuntime.installPlugin().
    Currently (lines 82-99), it delegates to PluginHost.installPlugin() which already has the install-time check inserted in Task 1. PluginRuntime itself does not need additional changes because:

    1. PluginHost.installPlugin() now calls checkSemVerCompatibility() before DB INSERT
    2. PluginRuntime.installPlugin() delegates to PluginHost.installPlugin() on line 85
    3. After PluginHost.installPlugin() succeeds, PluginRuntime activates the plugin on line 93

    So the flow is:
    PluginRuntime.installPlugin()
      → PluginHost.installPlugin() ← SemVer check happens here
        → extractManifest()
        → checkSemVerCompatibility() ← install-time check
        → DB INSERT
      → PluginHost.activatePlugin() ← activation-time check

    This satisfies D-05's requirement for dual checking. No changes needed to PluginRuntime.

    STEP 2B: Add a console.log to PluginRuntime.installPlugin() for traceability.
    After the existing try block (line 97), add:
    ```typescript
      console.log(`[PluginRuntime] Plugin "${manifest.id}" installed with version compatibility verified`);
    ```

    Actually, PluginHost.installPlugin() already logs this (line 306). Skip this step. No changes needed.

    STEP 2C: Verify that PluginRuntime properly passes semver-incompatible errors to callers.
    The existing code on line 97-99:
    ```typescript
      } catch (err) {
        throw err;
      }
    ```
    This re-throws all errors, including SemverMismatchError. When checkSemVerCompatibility throws in PluginHost.installPlugin(), the error propagates through PluginRuntime.installPlugin() to the original caller. This is correct behavior.

    No code changes needed to plugin-runtime/index.ts. The phase 6 check is entirely within PluginHost.
  </action>
  <verify>
    <automated>
      npx tsc --noEmit
      # Then verify the flow by reading the relevant methods
      grep -n "checkSemVerCompatibility" packages/core/plugin-host/index.ts | head -5
    </automated>
  </verify>
  <done>
    - PluginRuntime.installPlugin() 已正确委托给 PluginHost（内含版本检查）
    - SemverMismatchError 从 PluginHost 一路传播到调用方
    - 安装时检查 + 激活时检查 双重覆盖确认
    - tsc --noEmit 通过
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integration tests for install-time + activation-time SemVer check</name>
  <files>
    packages/core/plugin-host/__tests__/plugin-host.test.ts
  </files>
  <read_first>
    packages/core/plugin-host/__tests__/plugin-host.test.ts — 现有测试模式（mock ServiceRegistry、EsmLoader、DB 的方式）
  </read_first>
  <behavior>
    - Test 1: Version check passes when manifest.requires matches registered Token versions
    - Test 2: Version check throws SemverMismatchError when manifest.requires has incompatible @^version
    - Test 3: Version check throws SemverMismatchError when required Token is not registered
    - Test 4: Version check passes when manifest.requires has no @version (accept any)
    - Test 5: Optional dependency with incompatible version: no throw, console.warn called
    - Test 6: Optional dependency with unregistered Token: no throw, console.warn called
    - Test 7: Multiple requires with mixed compatibility — all must match, any failure blocks activation
  </behavior>
  <action>
    STEP 3A: Read the existing plugin-host.test.ts to understand the mock pattern.

    Read the file to see how ServiceRegistry, EsmLoader, and DB are mocked.
    Look for the existing test setup pattern — likely `beforeEach` creating minimal mocks.

    Based on the existing pattern, the key insight is:
    - ServiceRegistry needs a mock `getVersion()` method
    - The manifest needs `requires` and `optional` arrays with @version strings
    - The Db needs to return `source_code` and `manifest` for the plugin being activated

    STEP 3B: Add test cases for Phase 6 SemVer compatibility checking.

    The tests should follow the existing describe/it pattern. Add a new describe block:

    ```typescript
    describe('SemVer compatibility check (Phase 6)', () => {
      // Each test sets up a ServiceRegistry with known version, creates a manifest
      // with requires/optional, and verifies that checkSemVerCompatibility behaves
      // correctly

      it('should pass when required Token version matches range', async () => {
        // Setup: ServiceRegistry has ICommandBusService@1.0.0
        // Manifest requires: @openlearn/core:ICommandBusService@^1.0.0
        // Expect: no throw
      });

      it('should throw SemverMismatchError when required Token version is incompatible', async () => {
        // Setup: ServiceRegistry has ICommandBusService@1.0.0
        // Manifest requires: @openlearn/core:ICommandBusService@^2.0.0
        // Expect: SemverMismatchError with correct fields
      });

      it('should throw SemverMismatchError when required Token is not registered', async () => {
        // Setup: ServiceRegistry does NOT have the required Token
        // Manifest requires: @openlearn/core:INonExistentService@^1.0.0
        // Expect: SemverMismatchError with actualVersion='unregistered'
      });

      it('should pass when required Token has no version range', async () => {
        // Setup: ServiceRegistry has ICommandBusService@1.0.0
        // Manifest requires: @openlearn/core:ICommandBusService (no @version)
        // Expect: no throw (accept any version)
      });

      it('should not throw for optional dependency with incompatible version', async () => {
        // Setup: ServiceRegistry has IAIService@2.0.0
        // Manifest optional: @openlearn/core:IAIService@^1.0.0
        // Expect: console.warn called, no throw
      });

      it('should not throw for optional dependency with unregistered Token', async () => {
        // Setup: ServiceRegistry does NOT have the optional Token
        // Manifest optional: @openlearn/core:INonExistentService@^1.0.0
        // Expect: console.warn called, no throw
      });

      it('should reject activation when any required dependency fails', async () => {
        // Setup: ServiceRegistry has ICommandBusService@1.0.0 but not IEventBusService
        // Manifest requires both — second one fails
        // Expect: SemverMismatchError (first failure blocks everything)
      });
    });
    ```

    IMPORTANT GUIDANCE FOR THE IMPLEMENTATION:
    
    The current plugin-host.test.ts likely tests the full PluginHost class (constructor, activatePlugin, deactivatePlugin etc.) with real or mock dependencies. The checkSemVerCompatibility method is private. There are two approaches:
    
    **Approach A: Test through activatePlugin()**
    Create a PluginHost with a mock ServiceRegistry that has getVersion() returning known versions. Create a manifest JSON in DB that includes requires with @version. Call activatePlugin() and assert it succeeds or throws.
    
    **Approach B: Test checkSemVerCompatibility() directly**
    If possible, refactor to make checkSemVerCompatibility package-private/testable, or test through the public API.
    
    Follow the existing test pattern in plugin-host.test.ts. If the existing tests already create PluginHost instances with mock dependencies, follow that pattern.

    The tests should:
    1. Mock ServiceRegistry.getVersion() to return specific versions
    2. Create a manifest object with requires/optional arrays containing @version strings
    3. Use the existing DB mock pattern to inject the manifest
    
    Since checkSemVerCompatibility is private, the cleanest test approach is to test through activatePlugin():
    - Set up DB to return a row with a manifest containing requires/optional
    - Mock ServiceRegistry to have known versions (or missing tokens)
    - Call activatePlugin() and assert throw/no-throw behavior

    If the existing test suite uses a different approach (e.g., testing internal methods via any cast), follow that same pattern.

    STEP 3C: Run the tests:
    ```bash
    npx vitest run packages/core/plugin-host/__tests__/plugin-host.test.ts
    ```
  </action>
  <verify>
    <automated>
      npx vitest run packages/core/plugin-host/__tests__/plugin-host.test.ts --reporter=verbose
    </automated>
  </verify>
  <done>
    - 至少 5 个集成测试覆盖：
      1. 版本兼容通过
      2. 版本不兼容 → SemverMismatchError
      3. Token 未注册 → SemverMismatchError
      4. Optional 版本不兼容 → console.warn + 不抛错
      5. Multiple requires 混合兼容性
    - 所有测试通过
  </done>
</task>

</tasks>

<threat_model>
## Phase 6 Security Threats (Plan 06-03)

### Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Plugin manifest.requires → semver.satisfies() | 危险输入进入 semver 解析器 |
| Plugin manifest → zod parse → ServiceRegistry | 插件 manifest 可控制 requires 条目内容 |

### STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-01 | Tampering | Manifest requires @version range injection | mitigate | Zod regex (Plan 06-02) 在 parse 阶段拒绝非符合格式的版本字符串；semver.satisfies() 内部也会验证输入。双层防御 |
| T-6-02 | Denial of Service | semver.satisfies() ReDoS via crafted version string | mitigate | Zod regex 是线性模式（无嵌套量词），拒绝 `@` 后含大量特殊字符的载荷。semver.satisfies() 的实现是 O(n) |
| T-6-03 | Spoofing | Token Registry key collision | mitigate | checkSemVerCompatibility 调用 getVersion()，如果两个不同的服务使用相同 Token name 注册（Phase 5 的 registerOrReplace 路径），后注册的覆盖先注册的。这是 ServiceRegistry 的设计约束，需要通过开发者规范防范 |
| T-6-04 | Elevation of Privilege | Force-skip version check via crafted manifest | mitigate | installPlugin() 和 activatePlugin() 都调用 checkSemVerCompatibility，双重检查确保无法绕过。checkSemVerCompatibility 是私有方法，外部无法跳过 |
</threat_model>

<verification>
1. `npx tsc --noEmit` 通过
2. `npx vitest run packages/core/plugin-host/__tests__/plugin-host.test.ts` 全部通过
3. `npx vitest run` 全量测试通过（回归验证）
</verification>

<success_criteria>
1. PluginHost.activatePlugin() 在 buildContext 前检查 manifest.requires 中所有 Token 的版本兼容性
2. 版本不兼容时抛出 SemverMismatchError（包含 pluginId、tokenName、requiredRange、actualVersion）
3. Optional 依赖不兼容时打印 console.warn + 跳过（不抛错）
4. PluginHost.installPlugin() 在 DB INSERT 前调用 checkSemVerCompatibility
5. PluginRuntime.installPlugin() 通过 PluginHost 的委托路径获得双重检查
6. 所有集成测试通过
</success_criteria>

<output>
Create .planning/phases/06-eventbus-semver/06-03-SUMMARY.md when done
</output>
