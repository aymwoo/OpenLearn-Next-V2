---
phase: 02-token
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - packages/core/di/__tests__/interfaces.test.ts
  - packages/core/di/__tests__/storage-service.test.ts
  - packages/core/di/__tests__/ai-service.test.ts
autonomous: true
requirements:
  - PLUG-06
  - PLUG-11

must_haves:
  truths:
    - "D-17: vitest 测试验证 7 个 Token 的命名格式符合 @openlearn/core:IServiceName 规范"
    - "D-17: vitest 测试验证 Kernel 构造函数中 7 个 IService 成功注册并可 resolve"
    - "D-17: vitest 测试验证 StorageService CRUD 操作正确读写 SQLite"
    - "D-17: vitest 测试验证 AIService generateText 在无配置时抛异常"
  artifacts:
    - path: "packages/core/di/__tests__/interfaces.test.ts"
      provides: "Token 命名格式验证 + Kernel 注册流程集成测试"
      min_lines: 80
    - path: "packages/core/di/__tests__/storage-service.test.ts"
      provides: "StorageService get/set/delete 单元测试"
      min_lines: 40
    - path: "packages/core/di/__tests__/ai-service.test.ts"
      provides: "AIService generateText 单元测试"
      min_lines: 30
  key_links:
    - from: "packages/core/di/__tests__/interfaces.test.ts"
      to: "packages/core/di/interfaces.ts"
      via: "import { ...Token } from '../interfaces.js'"
      pattern: "import.*ServiceToken.*from.*interfaces"
    - from: "packages/core/di/__tests__/storage-service.test.ts"
      to: "packages/core/di/storage-service.ts"
      via: "import { StorageService } from '../storage-service.js'"
      pattern: "import.*StorageService.*from.*storage-service"
    - from: "packages/core/di/__tests__/ai-service.test.ts"
      to: "packages/core/di/ai-service.ts"
      via: "import { AIService } from '../ai-service.js'"
      pattern: "import.*AIService.*from.*ai-service"
---

<objective>
为 Phase 02 的所有新增代码编写 vitest 单元测试和集成测试。验证 Token 命名格式、Kernel 注册流程、StorageService 和 AIService 的正确性。

Purpose: 满足 D-17（测试要求）和 ROADMAP SC-1~SC-5——每个成功标准都有对应的自动化验证。
Output: 3 个测试文件，全部通过 `npx vitest run`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-token/02-RESEARCH.md
@.planning/phases/02-token/02-PATTERNS.md

<interfaces>
<!-- 测试依赖的所有模块 — 执行器通过 import 直接使用 -->

来自 packages/core/di/interfaces.ts (Plan 02-01 产物):
```typescript
// 7 个 Token 实例：ICommandBusServiceToken, IEventBusServiceToken, IActionRegistryServiceToken,
//                     ICapabilityServiceToken, IProcessServiceToken, IStorageServiceToken, IAIServiceToken
// 每个 Token.name 格式：@openlearn/core:IServiceName
```

来自 packages/core/di/storage-service.ts (Plan 02-01 产物):
```typescript
export class StorageService implements IStorageService {
  constructor(db: BetterSqlite3.Database) {}
  async get(key: string): Promise<unknown> { ... }
  async set(key: string, value: unknown): Promise<void> { ... }
  async delete(key: string): Promise<void> { ... }
}
```

来自 packages/core/di/ai-service.ts (Plan 02-01 产物):
```typescript
export class AIService implements IAIService {
  constructor(db: BetterSqlite3.Database) {}
  async generateText(prompt: string, options?: { systemInstruction?: string; temperature?: number }): Promise<string> { ... }
}
```

来自 packages/core/kernel/index.ts (Plan 02-02 产物):
```typescript
export class Kernel {
  public readonly eventBus: EventBus;
  public readonly commandBus: CommandBus;
  public readonly actionRegistry: ActionRegistry;
  public readonly capabilityGuard: CapabilityGuard;
  public readonly pluginRuntime: PluginRuntime;
  public readonly processManager: ProcessManager;
  public readonly serviceRegistry: ServiceRegistry;
  public readonly storageService: StorageService;
  public readonly aiService: AIService;
  public readonly db = db;
  constructor() { /* 初始化所有子系统 + 注册 7 个 IService */ }
}
export const kernelContainer = new Kernel();
```

来自 packages/core/di/token.ts:
```typescript
export class Token<T> {
  public readonly name: string;
  constructor(name: string) { /* TOKEN_NAME_RE 验证 */ }
}
```

来自 packages/core/di/service-registry.ts:
```typescript
export class ServiceRegistry {
  async register<T>(token: Token<T>, instance: T, options?: RegisterOptions): Promise<void> { ... }
  async resolve<T>(token: Token<T>): Promise<T> { ... }
  list(): Array<{ name: string; instance: unknown }> { ... }
  has<T>(token: Token<T>): boolean { ... }
}
```

来自 vitest.config.ts:
```typescript
export default defineConfig({
  test: {
    include: ['packages/core/di/__tests__/**/*.test.ts'],
    environment: 'node',
  },
});
```

现有测试风格（来自 packages/core/di/__tests__/service-registry.test.ts:20-75）:
```typescript
import { describe, it, expect } from 'vitest';
import { Token } from '../token.js';
import { ServiceRegistry } from '../service-registry.js';
// 测试模式：describe('描述', () => { it('should ...', async () => { ... }); });
// it.each([...]) 参数化测试
// await expect(promise).rejects.toThrow(/pattern/) 异常断言
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 interfaces.test.ts — Token 命名格式 + Kernel 注册流程测试</name>
  <files>packages/core/di/__tests__/interfaces.test.ts</files>
  <read_first>
    - packages/core/di/__tests__/token.test.ts — 现有 Token 测试风格（describe/it/expect 模式、it.each 参数化测试、异常断言）
    - packages/core/di/__tests__/service-registry.test.ts — 现有 ServiceRegistry 测试风格（集成测试 import Kernel 的模式）
    - packages/core/di/token.ts — Token 类的 TOKEN_NAME_RE 正则和 TokenError 异常类
    - .planning/phases/02-token/02-RESEARCH.md — 第 637-741 行的完整测试示例代码（Token 命名格式 + Kernel 注册流程）
    - .planning/phases/02-token/02-PATTERNS.md — 第 328-393 行的测试模式提取
  </read_first>
  <action>
创建新文件 `packages/core/di/__tests__/interfaces.test.ts`，包含 3 个 describe 块。

**文件头注释**（模仿 service-registry.test.ts:1-19 的 JSDoc 风格）：
说明本测试文件覆盖 ROADMAP SC-1（IService 接口定义）、SC-2（Token 命名格式）、SC-3（Kernel 注册）、SC-4（双路径访问一致性）。

**导入：**
```typescript
import { describe, it, expect, beforeAll } from 'vitest';
import { Kernel } from '../../kernel/index.js';
import { ServiceRegistry } from '../service-registry.js';
import {
  ICommandBusServiceToken,
  IEventBusServiceToken,
  IActionRegistryServiceToken,
  ICapabilityServiceToken,
  IProcessServiceToken,
  IStorageServiceToken,
  IAIServiceToken,
} from '../interfaces.js';
```

**Describe 块 1: `IService Token 命名格式`**

使用 `it.each` 参数化测试（模仿 token.test.ts 中的 it.each 模式），验证 7 个 Token.name 字符串格式：

数据驱动结构（7 个数组项）：
```typescript
const tokens = [
  { name: 'ICommandBusServiceToken', token: ICommandBusServiceToken, expected: '@openlearn/core:ICommandBusService' },
  { name: 'IEventBusServiceToken', token: IEventBusServiceToken, expected: '@openlearn/core:IEventBusService' },
  { name: 'IActionRegistryServiceToken', token: IActionRegistryServiceToken, expected: '@openlearn/core:IActionRegistryService' },
  { name: 'ICapabilityServiceToken', token: ICapabilityServiceToken, expected: '@openlearn/core:ICapabilityService' },
  { name: 'IProcessServiceToken', token: IProcessServiceToken, expected: '@openlearn/core:IProcessService' },
  { name: 'IStorageServiceToken', token: IStorageServiceToken, expected: '@openlearn/core:IStorageService' },
  { name: 'IAIServiceToken', token: IAIServiceToken, expected: '@openlearn/core:IAIService' },
];
```

测试用例：
1. `it.each(tokens)('$name 的 name 应为 $expected', ({ token, expected }) => { expect(token.name).toBe(expected); })`
2. `it('所有 Token 实例应该互不相同', () => { const names = tokens.map(t => t.token.name); const uniqueNames = new Set(names); expect(uniqueNames.size).toBe(tokens.length); })`
3. `it('所有 Token name 应通过 TOKEN_NAME_RE 格式验证', () => { tokens.forEach(t => { expect(() => new (t.token.constructor as any)(t.expected)).not.toThrow(); }); })`

关键：TOKEN_NAME_RE 正则 `/^@[a-zA-Z0-9_-]+\/[a-zA-Z0-9_-]+:[a-zA-Z0-9_]+$/` — 验证所有 7 个 Token name 中无空格、中文、`/`（除 scope/domain 分隔符外）、`.`。

**Describe 块 2: `Kernel IService 注册`**

使用 `beforeAll` 创建单个 Kernel 实例（避免重复初始化）：
```typescript
describe('Kernel IService 注册', () => {
  let kernel: Kernel;
  beforeAll(() => { kernel = new Kernel(); });
  // ...
});
```

测试用例：
1. `it('应该通过 serviceRegistry.resolve 获取所有 7 个 IService（SC-3）', async () => { ... })` — 依次 resolve 7 个 Token，验证每个结果 `toBeDefined()`
2. `it('resolve 返回的实例应与 kernelContainer 直接属性一致（5 个直接注册的子系统，SC-4）', async () => { ... })` — 验证：
   - `serviceRegistry.resolve(ICommandBusServiceToken)` 返回 `kernel.commandBus`
   - `serviceRegistry.resolve(IEventBusServiceToken)` 返回 `kernel.eventBus`
   - `serviceRegistry.resolve(IActionRegistryServiceToken)` 返回 `kernel.actionRegistry`
   - `serviceRegistry.resolve(ICapabilityServiceToken)` 返回 `kernel.capabilityGuard`
   - `serviceRegistry.resolve(IProcessServiceToken)` 返回 `kernel.processManager`
   使用 `toBe()`（引用相等），不是 `toEqual()`（值相等）
3. `it('resolve 返回的 StorageService 应是独立实例（SC-5）', async () => { ... })` — 验证 `serviceRegistry.resolve(IStorageServiceToken)` 返回的实例是 `StorageService` 类的实例（`toBeInstanceOf`），且 `get()` 方法可调用
4. `it('resolve 返回的 AIService 应是独立实例（SC-5）', async () => { ... })` — 验证 `serviceRegistry.resolve(IAIServiceToken)` 返回的实例是 `AIService` 类的实例（`toBeInstanceOf`），且 `generateText()` 方法可调用

**Describe 块 3: `Kernel IService 内省`**

1. `it('serviceRegistry.list() 应包含全部 7 个 Token（SC-3）', () => { ... })` — 调用 `kernel.serviceRegistry.list()`，验证返回数组的 length >= 7，且 name 列表包含所有 7 个 Token name 字符串。使用 `expect(names).toContain('@openlearn/core:ICommandBusService')` 格式。

**验证：** 运行 `npx vitest run packages/core/di/__tests__/interfaces.test.ts`，确认所有测试通过。
  </action>
  <verify>
    <automated>npx vitest run packages/core/di/__tests__/interfaces.test.ts</automated>
  </verify>
  <acceptance_criteria>
    <behavior>测试文件包含 `describe('IService Token 命名格式')`，验证 7 个 Token.name 均为 `@openlearn/core:I...Service` 格式</behavior>
    <behavior>测试文件包含 `describe('Kernel IService 注册')`，验证 7 个 Token 全部可 resolve</behavior>
    <behavior>测试文件验证 resolve 返回实例与 kernel 直接属性为同一引用（`toBe()`）</behavior>
    <behavior>测试文件包含 `describe('Kernel IService 内省')`，验证 list() 包含全部 7 个 name</behavior>
    <test-command>npx vitest run packages/core/di/__tests__/interfaces.test.ts — 所有测试通过</test-command>
  </acceptance_criteria>
  <done>interfaces.test.ts 存在，3 个 describe 块全部通过 vitest。</done>
</task>

<task type="auto">
  <name>Task 2: 创建 storage-service.test.ts 和 ai-service.test.ts 单元测试</name>
  <files>packages/core/di/__tests__/storage-service.test.ts, packages/core/di/__tests__/ai-service.test.ts</files>
  <read_first>
    - packages/core/di/__tests__/service-registry.test.ts — 测试风格和模式（describe/it/expect、async 测试、vitest import）
    - packages/core/di/storage-service.ts — StorageService 类完整实现
    - packages/core/di/ai-service.ts — AIService 类完整实现
    - packages/core/db/index.ts — DB 初始化（了解 plugin_storage 表结构，用于创建测试用 in-memory DB）
    - .planning/phases/02-token/02-RESEARCH.md — 第 789-807 行的测试映射
    - .planning/phases/02-token/02-PATTERNS.md — 第 396-421 行的测试模式提取
  </read_first>
  <action>
创建两个测试文件，验证 StorageService 和 AIService 的正确性。

**文件 1: `packages/core/di/__tests__/storage-service.test.ts`**

文件头注释：说明测试覆盖 IStorageService 的 get/set/delete CRUD 操作。

导入：
```typescript
import { describe, it, expect, beforeEach, afterEach } from 'vitest';
import Database from 'better-sqlite3';
import { StorageService } from '../storage-service.js';
```

使用 in-memory SQLite（`:memory:`）隔离测试：
```typescript
let db: Database.Database;
let storage: StorageService;

beforeEach(() => {
  db = new Database(':memory:');
  // 创建 plugin_storage 表（模仿 db/index.ts 中的表结构）
  db.exec(`CREATE TABLE IF NOT EXISTS plugin_storage (
    plugin_id TEXT NOT NULL,
    key TEXT NOT NULL,
    value TEXT,
    updated_at INTEGER,
    PRIMARY KEY (plugin_id, key)
  )`);
  storage = new StorageService(db);
});

afterEach(() => {
  db.close();
});
```

测试用例（describe `StorageService`）：

1. `it('get 应在 key 不存在时返回 null', async () => { ... })` — `await storage.get('nonexistent')` → `expect(result).toBeNull()`
2. `it('set 后 get 应返回相同的值', async () => { ... })` — `await storage.set('key1', { hello: 'world' })` → `const result = await storage.get('key1')` → `expect(result).toEqual({ hello: 'world' })`
3. `it('set 应能覆盖已有 key', async () => { ... })` — `await storage.set('key1', 'old')` → `await storage.set('key1', 'new')` → `expect(await storage.get('key1')).toBe('new')`
4. `it('delete 后 get 应返回 null', async () => { ... })` — `await storage.set('key1', 'value')` → `await storage.delete('key1')` → `expect(await storage.get('key1')).toBeNull()`
5. `it('delete 不存在的 key 不应抛异常', async () => { ... })` — `await storage.delete('nonexistent')` 不抛异常

**文件 2: `packages/core/di/__tests__/ai-service.test.ts`**

文件头注释：说明测试覆盖 IAIService.generateText 的行为。由于需要真实 API key 或网络，使用 mock 策略。

导入：
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { AIService } from '../ai-service.js';
```

使用 in-memory DB 隔离：
```typescript
let db: Database.Database;
let aiService: AIService;

beforeEach(() => {
  db = new Database(':memory:');
  db.exec(`CREATE TABLE IF NOT EXISTS ai_providers (
    id TEXT PRIMARY KEY,
    name TEXT,
    api_url TEXT,
    api_key TEXT,
    model_name TEXT
  )`);
  aiService = new AIService(db);
});
```

测试用例（describe `AIService`）：

1. `it('generateText 应在无 provider 且无 Gemini key 时抛异常', async () => { ... })`
   - 通过 `vi.stubEnv('GEMINI_API_KEY', '')` 清除 env
   - `await expect(aiService.generateText('hello')).rejects.toThrow('No AI providers or Gemini API key')`

2. `it('generateText 应在无 provider 但 Gemini key 存在时调用 Gemini（不抛 "No AI providers" 异常）', async () => { ... })`
   - 通过 `vi.stubEnv('GEMINI_API_KEY', 'test-key')` 设置 env
   - Mock `import('@google/genai')` 返回 fake genai：`vi.mock('@google/genai', () => ({ GoogleGenAI: vi.fn().mockImplementation(() => ({ models: { generateContent: vi.fn().mockResolvedValue({ text: 'Gemini response' }) } })) }))`
   - `const result = await aiService.generateText('hello')`
   - `expect(result).toBe('Gemini response')`

3. `it('generateText 应优先使用 DB 中的 provider', async () => { ... })`
   - 在 DB 中插入一个 provider：`db.prepare('INSERT INTO ai_providers (id, name, api_url, api_key, model_name) VALUES (?, ?, ?, ?, ?)').run('p1', 'test-provider', 'https://api.test.com', 'key123', 'gpt-4')`
   - Mock `global.fetch` 返回 fake 响应：`vi.spyOn(global, 'fetch').mockResolvedValue({ ok: true, json: async () => ({ choices: [{ message: { content: 'provider response' } }] }) } as Response)`
   - `const result = await aiService.generateText('hello')`
   - `expect(result).toBe('provider response')`
   - `expect(fetch).toHaveBeenCalledWith(expect.stringContaining('https://api.test.com/chat/completions'), expect.any(Object))`

关键：每个测试前重置 mocks（vi.resetAllMocks / vi.unstubAllEnvs），避免测试间泄露。

**验证：** 运行两个测试文件。
  </action>
  <verify>
    <automated>npx vitest run packages/core/di/__tests__/storage-service.test.ts packages/core/di/__tests__/ai-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    <behavior>storage-service.test.ts 验证 get/set/delete 的基本 CRUD 操作和边界情况</behavior>
    <behavior>ai-service.test.ts 验证 generateText 的 3 种场景：无配置抛异常、Gemini fallback、provider 优先</behavior>
    <behavior>测试使用 in-memory SQLite (`:memory:`)，不依赖外部数据库文件</behavior>
    <behavior>AIService 测试使用 vi.mock/vi.stubEnv 隔离网络依赖</behavior>
    <test-command>npx vitest run packages/core/di/__tests__/storage-service.test.ts packages/core/di/__tests__/ai-service.test.ts — 所有测试通过</test-command>
  </acceptance_criteria>
  <done>两个测试文件存在且全部通过 vitest。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试文件 | vitest Node 环境 — 纯内存/CI 执行，无外部攻击面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Information Disclosure | 测试中的 in-memory DB | accept | in-memory SQLite 在测试结束后自动销毁，无持久化数据—无泄露风险 |
| T-02-08 | Elevation of Privilege | AIService 测试中的 mock | accept | mock 仅在测试作用域内有效 — 不影响生产代码 |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | 本 Phase 不安装新外部包 |
</threat_model>

<verification>
1. `npx vitest run packages/core/di/__tests__/interfaces.test.ts` — Token 命名格式 + Kernel 注册集成测试
2. `npx vitest run packages/core/di/__tests__/storage-service.test.ts` — StorageService CRUD 测试
3. `npx vitest run packages/core/di/__tests__/ai-service.test.ts` — AIService generateText 测试
4. `npx vitest run` — 完整测试套件（含 Phase 1 的 34 个测试）
</verification>

<success_criteria>
- [ ] packages/core/di/__tests__/interfaces.test.ts 存在且通过 — 覆盖 Token 命名格式（7 个）、Kernel 注册（7 个 resolve）、双路径一致性、内省 API
- [ ] packages/core/di/__tests__/storage-service.test.ts 存在且通过 — 覆盖 get/set/delete + 边界情况
- [ ] packages/core/di/__tests__/ai-service.test.ts 存在且通过 — 覆盖 3 种 AI 场景
- [ ] 完整测试套件 `npx vitest run` 全部通过（含 Phase 1 现有测试）
</success_criteria>

<output>
Create `.planning/phases/02-token/02-03-SUMMARY.md` when done
</output>
