---
phase: 02-token
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - packages/core/kernel/index.ts
  - packages/core/plugin-runtime/index.ts
autonomous: true
requirements:
  - PLUG-06
  - PLUG-11

must_haves:
  truths:
    - "7 个 IService 在 Kernel 启动时注册到 ServiceRegistry，构造函数结束时所有服务可用"
    - "现有代码通过 kernelContainer.commandBus 等属性直接访问子系统的方式保持不变"
    - "新代码可通过 kernelContainer.serviceRegistry.resolve(IToken) 获取类型安全的服务实例"
    - "PluginRuntime 中 wrappedStorage 和 wrappedAI 保持安全包装但引用独立 IService 实例"
  artifacts:
    - path: "packages/core/kernel/index.ts"
      provides: "Kernel 构造函数中注册 7 个 IService + storageService/aiService 公共属性"
      contains: "ICommandBusServiceToken"
      contains: "storageService"
      contains: "aiService"
    - path: "packages/core/plugin-runtime/index.ts"
      provides: "PluginRuntime evaluateAndActivate 中 wrappedStorage/wrappedAI 引用切换（保持安全包装）"
      contains: "wrappedStorage"
      contains: "wrappedAI"
      contains: "kernel.storageService"
  key_links:
    - from: "packages/core/kernel/index.ts"
      to: "packages/core/di/interfaces.ts"
      via: "import { ...Token, ...Token } from '../di/interfaces.js'"
      pattern: "import.*ServiceToken.*from.*di/interfaces"
    - from: "packages/core/kernel/index.ts"
      to: "packages/core/di/storage-service.ts"
      via: "import { StorageService } from '../di/storage-service.js'"
      pattern: "import.*StorageService.*from.*storage-service"
    - from: "packages/core/kernel/index.ts"
      to: "packages/core/di/ai-service.ts"
      via: "import { AIService } from '../di/ai-service.js'"
      pattern: "import.*AIService.*from.*ai-service"
    - from: "packages/core/plugin-runtime/index.ts"
      to: "packages/core/kernel/index.ts"
      via: "this.kernel.storageService / this.kernel.aiService"
      pattern: "kernel\\.(storageService|aiService)"
---

<objective>
在 Kernel 构造函数中注册 7 个 IService 到 ServiceRegistry，并在 PluginRuntime 中切换 wrappedStorage/wrappedAI 引用到独立的 StorageService/AIService 实例。现有代码路径完全保持不变——kernelContainer.xxx 直接访问继续工作，PluginRuntime 安全包装层保持不变。

Purpose: 完成服务从"硬编码 kernel 属性访问"到"Token DI 注册 + 双路径兼容"的过渡。D-07 要求保留 wrapped* 安全层，D-14 要求在构造函数中注册，D-15 要求按依赖层级顺序注册。
Output: 修改后的 Kernel（含 IService 注册 + 新属性）和 PluginRuntime（引用切换）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-token/02-CONTEXT.md
@.planning/phases/02-token/02-RESEARCH.md
@.planning/phases/02-token/02-PATTERNS.md

<interfaces>
<!-- Phase 01 产物 — 执行器通过 import 直接使用，无需探索代码库 -->

来自 packages/core/di/interfaces.ts (Plan 02-01 产物):
```typescript
export interface ICommandBusService { execute<T>(command: T): Promise<unknown>; registerHandler(commandType: string, handler: CommandHandler): Promise<void>; unregisterHandler(commandType: string): Promise<void>; createCommand<T>(type: string, payload: T, actorId: string, metadata?: CommandMetadata): Promise<PlatformCommand<T>>; setInterceptor(interceptor: (command: PlatformCommand) => Promise<void>): Promise<void>; }
export interface IEventBusService { publish(event: PlatformEvent): Promise<void>; subscribe(eventType: string, subscriber: EventSubscriber): Promise<void>; unsubscribe(eventType: string, subscriber: EventSubscriber): Promise<void>; }
export interface IActionRegistryService { register(descriptor: ActionDescriptor): Promise<void>; unregister(id: string): Promise<void>; getAllActions(): Promise<ActionDescriptor[]>; getAgentTools(): Promise<unknown[]>; getActionByToolName(toolName: string): Promise<ActionDescriptor | undefined>; getActionByCommandType(commandType: string): Promise<ActionDescriptor | undefined>; }
export interface ICapabilityService { grant(actorId: string, cap: string): Promise<void>; revokeAll(actorId: string): Promise<void>; check(actorId: string, requiredCap: string): Promise<boolean>; }
export interface IProcessService { spawn(name: string, taskType: string, payload: unknown): Promise<string>; kill(processId: string): Promise<void>; registerHandler(taskType: string, handler: ProcessHandler): Promise<void>; unregisterHandler(taskType: string): Promise<void>; registerInterval(name: string, intervalMs: number, tickFn: (log: (msg: string) => void) => void): Promise<string>; restore(): Promise<void>; }
export interface IStorageService { get(key: string): Promise<unknown>; set(key: string, value: unknown): Promise<void>; delete(key: string): Promise<void>; }
export interface IAIService { generateText(prompt: string, options?: { systemInstruction?: string; temperature?: number }): Promise<string>; }
export const ICommandBusServiceToken: Token<ICommandBusService>;
export const IEventBusServiceToken: Token<IEventBusService>;
export const IActionRegistryServiceToken: Token<IActionRegistryService>;
export const ICapabilityServiceToken: Token<ICapabilityService>;
export const IProcessServiceToken: Token<IProcessService>;
export const IStorageServiceToken: Token<IStorageService>;
export const IAIServiceToken: Token<IAIService>;
```

来自 packages/core/di/storage-service.ts (Plan 02-01 产物):
```typescript
export class StorageService implements IStorageService {
  constructor(private db: BetterSqlite3.Database) {}
  async get(key: string): Promise<unknown> { /* SQLite plugin_storage with '__kernel__' plugin_id */ }
  async set(key: string, value: unknown): Promise<void> { /* INSERT ON CONFLICT */ }
  async delete(key: string): Promise<void> { /* DELETE */ }
}
```

来自 packages/core/di/ai-service.ts (Plan 02-01 产物):
```typescript
export class AIService implements IAIService {
  constructor(private db: BetterSqlite3.Database) {}
  async generateText(prompt: string, options?: { systemInstruction?: string; temperature?: number }): Promise<string> { /* provider → Gemini fallback */ }
}
```

来自 packages/core/kernel/index.ts (当前结构):
```typescript
export class Kernel {
  public readonly eventBus: EventBus;
  public readonly commandBus: CommandBus;
  public readonly actionRegistry: ActionRegistry;
  public readonly capabilityGuard: CapabilityGuard;
  public readonly pluginRuntime: PluginRuntime;
  public readonly processManager: ProcessManager;
  public readonly serviceRegistry: ServiceRegistry;
  public readonly db = db;
  constructor() {
    this.serviceRegistry = new ServiceRegistry();
    this.eventBus = new EventBus();
    this.commandBus = new CommandBus(this.eventBus);
    this.actionRegistry = new ActionRegistry();
    this.capabilityGuard = new CapabilityGuard();
    this.pluginRuntime = new PluginRuntime(this);
    this.processManager = new ProcessManager(this);
    this.commandBus.setInterceptor(async (command) => { /* capability + high-risk check */ });
  }
}
```

来自 packages/core/plugin-runtime/index.ts (关键区域):
```typescript
// Line 150: private async evaluateAndActivate(sourceCode, pluginId) { ... }
// Line 327-362: const wrappedStorage = { get: createSafeFunction(...), set: createSafeFunction(...), delete: createSafeFunction(...) };
// Line 364-440: const wrappedAI = { generateText: createSafeFunction(...) };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Kernel 构造函数中注册 7 个 IService + 添加存储/AI 公共属性</name>
  <files>packages/core/kernel/index.ts</files>
  <read_first>
    - packages/core/kernel/index.ts — 当前 Kernel 构造函数完整代码（第 1-81 行）
    - packages/core/di/interfaces.ts — 7 个 IService 接口和 Token 实例（Plan 02-01 产物）
    - packages/core/di/storage-service.ts — StorageService 类（Plan 02-01 产物）
    - packages/core/di/ai-service.ts — AIService 类（Plan 02-01 产物）
    - .planning/phases/02-token/02-PATTERNS.md — 第 491-581 行的 Kernel 修改模式（构造函数修改的具体代码结构）
    - .planning/phases/02-token/02-RESEARCH.md — 第 496-574 行的注册示例代码
  </read_first>
  <action>
修改 `packages/core/kernel/index.ts`，分三个区域变更：

**区域 1 — 导入新增（文件顶部，第 1 行附近）：**

在第 7 行 `import { ServiceRegistry } from '../di/index.js'` 之后追加：

- `import { ICommandBusServiceToken, IEventBusServiceToken, IActionRegistryServiceToken, ICapabilityServiceToken, IProcessServiceToken, IStorageServiceToken, IAIServiceToken } from '../di/interfaces.js'` — 7 个 Token 实例
- `import { StorageService } from '../di/storage-service.js'` — StorageService 类
- `import { AIService } from '../di/ai-service.js'` — AIService 类

不需要 import type 接口——Token 构造函数的泛型参数已承载类型信息，直接注册时不需要显式类型断言接口名。

**区域 2 — Kernel 类属性声明（第 11-19 行 区域）：**

在第 18 行 `public readonly serviceRegistry: ServiceRegistry;` 之后、第 19 行 `public readonly db = db;` 之前添加：
- `public readonly storageService: StorageService;`
- `public readonly aiService: AIService;`

**区域 3 — 构造函数（第 21-58 行）：**

重构构造函数中的初始化顺序——将子系统实例创建和 PluginRuntime 初始化按 D-15 层级重排：

旧顺序（当前）：
```
serviceRegistry → eventBus → commandBus(EventBus) → actionRegistry → capabilityGuard → pluginRuntime(Kernel) → processManager(Kernel) → setInterceptor
```

新顺序（D-14: ServiceRegistry 创建后、拦截器前注册 IService；D-15: 按层级创建）：
```
1. this.serviceRegistry = new ServiceRegistry()

2. // Layer 0 — 无依赖
   this.eventBus = new EventBus()
   this.capabilityGuard = new CapabilityGuard()
   this.storageService = new StorageService(this.db)

3. // Layer 1 — 依赖 Layer 0
   this.commandBus = new CommandBus(this.eventBus)
   this.actionRegistry = new ActionRegistry()

4. // Layer 2 — 依赖 Kernel/db
   this.processManager = new ProcessManager(this)
   this.aiService = new AIService(this.db)

5. // PluginRuntime 在所有子系统创建之后初始化
   this.pluginRuntime = new PluginRuntime(this)

6. // ── IService 注册（D-14: 在拦截器之前）──
   // D-16: 不声明 requires/optional 参数
   // D-09: 现有子系统实例直接用 `as any` 类型断言注册
   
   // Layer 0 registrations
   this.serviceRegistry.register(IEventBusServiceToken, this.eventBus as any)
   this.serviceRegistry.register(ICapabilityServiceToken, this.capabilityGuard as any)
   this.serviceRegistry.register(IStorageServiceToken, this.storageService)  // 无需类型断言 — 已 implements IStorageService
   
   // Layer 1 registrations
   this.serviceRegistry.register(ICommandBusServiceToken, this.commandBus as any)
   this.serviceRegistry.register(IActionRegistryServiceToken, this.actionRegistry as any)
   
   // Layer 2 registrations
   this.serviceRegistry.register(IProcessServiceToken, this.processManager as any)
   this.serviceRegistry.register(IAIServiceToken, this.aiService)  // 无需类型断言 — 已 implements IAIService

7. // 拦截器（不变）
   this.commandBus.setInterceptor(async (command) => { ... })
```

**关键细节：**
- `register()` 返回 Promise，但构造函数不能 async——所有 7 个注册是同步完成的（无异步操作），实际安全。register 方法体内部无任何 await，Promise 立即 resolve
- `as any` 用于 5 个现有子系统实例——CommandBus、EventBus、ActionRegistry、CapabilityGuard、ProcessManager 的方法签名是同步的但接口声明 async，类型不匹配需要强制断言
- StorageService 和 AIService 直接注册无需断言——它们已 `implements IService`
- PluginRuntime 移到了 Layer 2 创建之后——构造时可通过 `this.kernel.storageService` 访问新属性
- `eventBus` 移到 Layer 0 最先创建——CommandBus 构造函数需要它
- `processManager` 保持在 `this.pluginRuntime` 之前——与 D-15 Layer 2 一致

**验证：** 运行 `tsc --noEmit`。
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    <source>Kernel 类包含 `public readonly storageService: StorageService` 和 `public readonly aiService: AIService` 属性声明</source>
    <source>构造函数中包含 `this.storageService = new StorageService(this.db)` 和 `this.aiService = new AIService(this.db)`</source>
    <source>构造函数中包含 7 个 `this.serviceRegistry.register(...Token, ...)` 调用，按 Layer 0→1→2 分组</source>
    <source>register 调用中 5 个现有子系统使用 `as any` 类型断言，2 个新类无需断言</source>
    <source>PluginRuntime 初始化 (`new PluginRuntime(this)`) 在所有子系统创建和 IService 注册之后执行</source>
    <source>现有 `kernelContainer.xxx` 属性（commandBus、eventBus 等）全部保持 public readonly，未删除</source>
    <source>拦截器 setInterceptor 代码完全不变</source>
    <source>tsc --noEmit 通过</source>
  </acceptance_criteria>
  <done>Kernel 构造函数中 7 个 IService 全部注册到 ServiceRegistry，storageService/aiService 属性可用，编译通过。</done>
</task>

<task type="auto">
  <name>Task 2: PluginRuntime evaluateAndActivate 中切换存储/AI 引用方式</name>
  <files>packages/core/plugin-runtime/index.ts</files>
  <read_first>
    - packages/core/plugin-runtime/index.ts — evaluateAndActivate 方法中 wrappedStorage（第 327-362 行）和 wrappedAI（第 364-440 行）的现有实现
    - packages/core/kernel/index.ts — Task 1 修改后的 Kernel 类，包含 storageService 和 aiService 属性
    - .planning/phases/02-token/02-PATTERNS.md — 第 502-557 行的 PluginRuntime 适配模式（wrappedStorage/wrappedAI 引用切换前后对比）
    - .planning/phases/02-token/02-RESEARCH.md — 第 503-558 行的 wrappedStorage/wrappedAI 变更示例
  </read_first>
  <action>
修改 `packages/core/plugin-runtime/index.ts` 中 evaluateAndActivate 方法内的 wrappedStorage 和 wrappedAI 创建逻辑。变更点在方法内部，不影响文件其他任何部分。

**核心原则（D-07）：** wrappedStorage 和 wrappedAI 的 `createSafeFunction` 安全包装层完全保留。只是从 `this.kernel.db.prepare(...)` 内联直接操作 DB，改为：
- wrappedStorage：保持直接访问 `this.kernel.db`（因为需要 per-plugin 隔离的 `manifest.id` 作 plugin_id——StorageService 使用 `'__kernel__'` 不适合此场景）
- wrappedAI：委托给 `this.kernel.aiService.generateText(prompt, options)`，保留 createSafeFunction + try-catch 包装

**具体变更位置和内容：**

**A) wrappedStorage（第 327-362 行）— 保持不变**

D-07 和 RESEARCH.md 开放问题 #1 的判断：wrappedStorage 保持直接访问 `this.kernel.db`，不改委托给 StorageService。原因：
- StorageService 使用 `'__kernel__'` plugin_id
- wrappedStorage 需要 `manifest.id` 做 per-plugin 隔离
- 两者隔离模型不同，wrappedStorage 是安全层的一部分，直接 DB 访问是正确的

**B) wrappedAI（第 364-440 行）— 委托给 AIService + 保留安全包装**

将 wrappedAI 的内联 AI 调用逻辑替换为委托给 `this.kernel.aiService`：

旧代码（第 365-440 行）：
```typescript
const wrappedAI = {
  generateText: createSafeFunction(async (prompt: string, options?: { systemInstruction?: string; temperature?: number }) => {
    try {
      // 1. Check provider... (内联完整 AI 调用逻辑)
      // 2. Fallback to Gemini... (内联完整 Gemini 逻辑)
      // 3. Error handling...
    } catch (e: any) {
      console.error(`[Plugin:${manifest.id}] Error in generateText:`, e);
      throw e;
    }
  })
};
```

新代码（替换同一位置）：
```typescript
const wrappedAI = {
  generateText: createSafeFunction(async (prompt: string, options?: { systemInstruction?: string; temperature?: number }) => {
    try {
      return await this.kernel.aiService.generateText(prompt, options);
    } catch (e: any) {
      console.error(`[Plugin:${manifest.id}] Error in generateText:`, e);
      throw e;
    }
  })
};
```

**验证：** 运行 `tsc --noEmit` 检查类型。

**关键检查点：**
- `this.kernel.aiService` 属性在 PluginRuntime 构造时已存在（Kernel Task 1 中 PluginRuntime 在所有子系统之后创建）
- `createSafeFunction` 包装保持完全不变——安全层不受影响
- `console.error` 带 `[Plugin:${manifest.id}]` 标签保持——PluginRuntime 的错误日志惯例不变
- wrappedAI 的旧内联实现（约 75 行代码）被替换为 1 行委托调用——减少代码体积正是 D-06 的目标
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    <source>plugin-runtime/index.ts 中 wrappedAI.generateText 委托给 `this.kernel.aiService.generateText(prompt, options)`</source>
    <source>wrappedAI 仍然包裹在 `createSafeFunction` 中 — 安全层不受影响</source>
    <source>wrappedAI 的 `try-catch` + `console.error(`[Plugin:${manifest.id}]...`)` 保持存在</source>
    <source>wrappedStorage 实现完全不变 — 仍然直接访问 `this.kernel.db` + `manifest.id` 做 per-plugin 隔离</source>
    <source>插件源文件中不包含 `import('@google/genai')` — AI 逻辑已提取到 AIService</source>
    <source>插件源文件中不包含 `fetch(provider.api_url)` — AI provider 逻辑已提取到 AIService</source>
    <source>tsc --noEmit 通过</source>
  </acceptance_criteria>
  <done>PluginRuntime 中 wrappedAI 委托给独立的 AIService 实例，wrappedStorage 保持不变，编译通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kernel → ServiceRegistry | Kernel 构造函数内部 — 仅受信任代码访问 |
| PluginRuntime → AIService | 通过 kernel.aiService 属性访问，createSafeFunction 包装 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | ServiceRegistry.register 类型断言 (`as any`) | accept | `as any` 仅在 Kernel 构造函数内部使用（受信任代码），不暴露给外部 API。5 个现有子系统的同步方法在 IService async 接口下运行时行为正确——同步代码自动包装为 resolved Promise |
| T-02-05 | Elevation of Privilege | wrappedAI → AIService 委托 | mitigate | createSafeFunction + try-catch 包装完全保留——插件无法绕过 PluginRuntime 安全层直接访问 AIService |
| T-02-06 | Information Disclosure | PluginRuntime 中的 kernel.storageService | accept | storageService 通过 kernel 属性暴露，但 PluginRuntime 不将其暴露给插件——插件只拿到 wrappedStorage（per-plugin 隔离）。直接访问路径仅限 PluginRuntime 类内部 |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | 本 Phase 不安装新外部包 |
</threat_model>

<verification>
1. `npx tsc --noEmit` — 所有修改文件通过类型检查
2. 手动检查：Kernel 构造函数中 7 个 register 调用全部存在且按 Layer 分组
3. 手动检查：PluginRuntime 中 wrappedAI 使用 `this.kernel.aiService.generateText()`
4. 现有功能完整性验证：`npm run dev`，登录后验证课程创建、白板操作、插件安装等现有功能正常（PLUG-11）
</verification>

<success_criteria>
- [ ] Kernel 构造函数中 7 个 IService 注册到 ServiceRegistry，按 Layer 0/1/2 顺序
- [ ] Kernel 类有 public readonly storageService 和 aiService 属性
- [ ] PluginRuntime 中 wrappedAI 委托给 AIService，wrappedStorage 保持不变
- [ ] 现有 kernelContainer.commandBus 等属性全部保留
- [ ] PluginRuntime 中 createSafeFunction 包装完全不变
- [ ] tsc --noEmit 零错误
</success_criteria>

<output>
Create `.planning/phases/02-token/02-02-SUMMARY.md` when done
</output>
