# Implementation Plan - Task 12-01-04

## Goal
Implement the whitelisted read-only DI proxy `MfeServiceRegistryProxy` in `src/mfe/MfeContextProvider.tsx`.

## Proposed Changes

### 1. `src/mfe/MfeContextProvider.tsx`
- Refactor class `MfeServiceRegistryProxy` to check the `DI_WHITELIST` for any token before accessing.
- Throw an error if a token is not in the whitelist: `new Error("Access Denied: Service token ... is private to the Host Shell")`.
- If it is whitelisted, delegate:
  - `resolve`: `this.serviceRegistry.resolve(token)`
  - `get`: `(this.serviceRegistry as any).services.get(token)`
  - `has`: `this.serviceRegistry.has(token)`

## Verification
- Run `npx vitest run src/mfe/__tests__/bridge.test.tsx -t "DI Proxy"` to verify whitelisting security tests pass.
