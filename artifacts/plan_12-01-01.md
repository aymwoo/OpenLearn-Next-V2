# Implementation Plan - Task 12-01-01

## Goal
Implement the strongly typed `MfeContext` in `src/mfe/types.ts` and create the integration test scaffold `src/mfe/__tests__/bridge.test.tsx`.

## Proposed Changes

### 1. `src/mfe/types.ts`
- Import `PlatformEvent` from standard platform package paths.
- Update `MfeContext` interface to support strongly-typed event bus and service registry.

### 2. `src/mfe/__tests__/bridge.test.tsx`
- Implement jsdom environment.
- Create tests for state sync, DI proxy, EventBus wrapper & socket bridge.

## Verification
- Run `npx vitest run src/mfe/__tests__/bridge.test.tsx`.
