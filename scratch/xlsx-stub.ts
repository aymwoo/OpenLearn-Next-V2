// xlsx stub for vitest — provides a no-op read/write so tests don't fail on optional import
export function read() { return []; }
export function write() {}
export function utils() { return {}; }
export default { read, write, utils };
